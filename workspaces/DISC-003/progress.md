# DISC-003 progress

Status: **IN PROGRESS / NOT ACCEPTED**.

This slice converts every DISC-002 candidate family into an explicit reconciliation record. The current ledger contains 32 families: all 32 now have pinned reviewed `partial` evidence and zero remain queued. Zero queued families does not mean the source audit is complete or accepted: unresolved findings, generated/dynamic/platform traversal, and verifier/controller review remain open.

Implemented in this slice:

- `tools/reconcile_surfaces.py` validates one-to-one candidate coverage and prevents silent feature-scope shrinkage.
- Evidence references must resolve to the locked repository and carry exact Git blob SHAs.
- Reconciled records require source, caller, test, and spec evidence; partial records require pinned source plus explicit unresolved work.
- `sources/disc-003-reconciliation.manifest.json` hash-binds the reconciliation to the DISC-002 rules, evidence catalog, and canonical Ralph plan.
- Upstream status distinguishes V2 implementation, shared legacy, partial/planned behavior, reference-only 9router implementation, and unresolved candidates.
- `sources/backlog-exhaustion.json` classifies all 86 live non-controller-accepted stories exactly once as stale-local implementation, explicit blocker, dependency-constrained client/UI work, or unresolved/decomposition-blocked work. `tools/validate_backlog_exhaustion.py` derives accepted state from live Ralph, verifies stale implementation cards/worklogs/commits, binds mapped rows to reviewed pinned evidence IDs, keeps no-surface rows explicit rather than inventing ownership, checks the `FEATURES.md` status mirror, and rejects any DISC-003 acceptance/release-state drift.
- `tools/reconcile_surfaces.py --check-manifest` fails when the checked-in DISC-003 manifest no longer matches reconciliation/rules/evidence/Ralph inputs, and `tools/validate_repository.py` composes that check with backlog exhaustion plus plan validation. Pull-request CI, the leased controller verifier, and `auto_drive` use this canonical repository gate; bootstrap meta-tests pin those entrypoints so accidental command removal or bypass is visible.

Current evidence-backed findings include Session V2 parity gaps, process-local BackgroundJob semantics versus durable subagents, tool/permission boundaries, scoped plugin lifecycle with deferred reload/external activation, ModelsDev CLI/cache behavior, Integration OAuth/key HTTP surfaces, scoped reference metadata/guidance, deterministic HTTP/WebSocket recorder fixtures, typed Effect layer/SQLite runtime adapters, repository parsing/cache materialization, canonical browser-safe event manifests/wire contracts, generated client/server contract identity, current-to-legacy event/config compatibility, typed control-plane route boundaries, location-scoped configuration discovery, replayable scoped State transforms, 60-minute idle location-service caching, global/bootstrap and npm/install metadata behavior, confined CodeMode execution, bounded/scoped process and PTY lifecycle, SQLite/EventV2/snapshot/tool-output storage behavior, deterministic share merge/secret validation with remote sync callers, heterogeneous hosted enterprise remote functions, browser app routing/protocol/terminal transports, TUI renderer/provider/plugin lifecycle, Electron desktop sidecar/IPC/WSL runtime behavior, and 9router account persistence/routing/token-refresh/translation/dashboard/settings/network-proxy reference semantics. The newer OpenCode records deliberately separate deterministic replay from live/network/filesystem/environment recorder behavior, accepted BASE/DB runtime semantics from unresolved OPS ownership, pure repository parsing from clone/fetch/filesystem cache effects, canonical Schema identity from runtime consumers, generated contract identity from provider/network integrations, legacy wire translation from GlobalBus/runtime effects, HttpApi error/DTO translation from Git/session/project control-plane mutations, accepted BASE configuration/lifecycle behavior from unresolved OPS-001/004/005/008 runtime/cache/install/control-plane ownership, path-derived CodeMode/process/storage reference behavior from already accepted TOOL/SEC/DB owners, pure share merge semantics from DB/network/account/auth/subscriber lifetime and hosted Cloudflare/GitHub/Discord infrastructure, and client/UI source parity from target architecture/dependency authorization.

Completion remains blocked on full caller/test/spec traversal, especially dynamic registration, remaining generated/SDK paths, platform gates, client/UI/desktop architecture and dependency ownership, broader compatibility/config mappings, exact configuration-runtime task decomposition, container/observability/install ownership, task-level SHARE/enterprise remote decomposition with bounded resource policy, remaining 9router dashboard/proxy details, and complete translation/provider/format/streaming coverage. Reviewed client, accepted-owner, and remote families remain partial evidence rather than authority to reopen accepted product work or introduce UI, process, credentials, network, filesystem, packaging, or platform side effects. The normalized exhaustion ledger is a fail-closed bookkeeping guard, not a replacement for missing task contracts: blocker/unresolved rows without unique ownership intentionally do not receive placeholder task cards or worklogs, while existing UI-014..018 task cards remain dependency-constrained and unstarted.
