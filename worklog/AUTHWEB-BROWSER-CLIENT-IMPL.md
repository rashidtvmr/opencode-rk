# AUTHWEB-BROWSER-CLIENT-IMPL — isolated candidate

## Claim and source evidence

- Session `ses_f3c4de578ffelQv59xDXmOs03B`; branch `lane/AUTHWEB-BROWSER-CLIENT-IMPL`, base `0da7bcb316e241f4ae3ed53a3e94ea9d4ab0362b`; product ownership **only** `web/src/lib/api.ts`. Test/scratchpad/own ledger row are separate coordination artifacts. No controller or frozen tests editable.
- At `0da7bcb:crates/server/src/daemon_auth.rs:150-175`, `/api/*` requires Bearer, returning 401 absent/403 invalid. At `0da7bcb:crates/cli/src/main.rs:668-747`, CLI opens only bare `http_origin` even though it mints a token; full browser journey still has no credential delivery.
- At `0da7bcb:web/src/lib/api.ts:171-188`, shared `request()` sends no Authorization. At `:574-612` attachment upload/delete and `:995-1012` streaming turn bypass `request()` and also omit Authorization. These four fetch sites are the owned implementation boundary.
- Test author froze two independent loopback-HTTP behavioral RED tests: `web/src/lib/api.auth.test.mjs` SHA-256 `f007aa291204204d366506c2c8b5149099317bb103da6fe915f8e4409e5a6a62` (listSessions 401) and `web/src/lib/api.direct-auth.test.mjs` SHA-256 `4c7201da9246717ccea089ddcf4f7c0befe749403be985e9d3a630926e3a879c` (attachment delete 401). Orchestrator independently reran both **0/2 RED** on exact base in 109 ms. Never modify either test, even disputed assertions.
- The direct-fetch test's author claimed ID `AUTHWEB-DIRECT-FETCH` rather than assigned `AUTHWEB-DIRECT-FETCH-RED`; its line 84-88 is tautological. This is a separate test-contract/claim discrepancy, **not** evidence that the product passed. Keep independent direct-fetch HTTP observation, but do not claim parent acceptance.

## Contract, lifetime, failure and bounds

- Browser client may consume a syntactically exact 64-hex `#oc2-token` fragment on its same-origin page, remove it via `history.replaceState` before first fetch, and retain it in module memory for the tab lifetime. Never put a token in query parameters, request body, error messages, console logs or persistent storage; attach `Authorization: Bearer <token>` only to same-origin relative `/api/` paths. Malformed/missing fragment cannot fabricate a credential; server must continue to reject unauthorized requests. Keep explicitly supplied headers and abort signals.
- Browser refresh after clearing the fragment loses the in-memory token; CLI handoff, refresh/re-authentication, origin pinning, credential rotation, and existing-session attach are **cross-file integration work**, not proven by this one-file candidate. Parent Web UI must remain blocked until the CLI and real server caller use a reviewed delivery contract.
- Use bounded 64-character token; no retained request/response bodies added; no OS process, database, or long-lived worker. Frozen Node tests use disposable ephemeral 127.0.0.1 HTTP servers and fake token only.

## Results / remaining

- Implemented only `web/src/lib/api.ts`: strict 64-hex fragment matching, same-origin URL clearing via `history.replaceState` before assigning in-memory token, and `Headers`-based Bearer injection for exactly the four relative `/api/` fetch sites. Other headers/body/signal remain preserved. No token in storage/query/log or full transcript; malformed/absent fragment yields no injected credential and server's existing 401/403 remains authoritative.
- The unchanged Node HTTP tests initially failed **0/2 RED**, 401 at both real `listSessions` and real `deleteDraftAttachment` on exact base. After product edit, `rtk node --experimental-strip-types --test web/src/lib/api.auth.test.mjs web/src/lib/api.direct-auth.test.mjs` **2/2 GREEN**, exit 0 (110ms). Hashes unchanged: `f007aa291204204d366506c2c8b5149099317bb103da6fe915f8e4409e5a6a62` and `4c7201da9246717ccea089ddcf4f7c0befe749403be985e9d3a630926e3a879c`. Both tests use actual bounded loopback HTTP servers, fake token and clean teardown. No user DB.
- `rtk pnpm install --offline --frozen-lockfile --ignore-scripts` succeeded in the disposable worktree, no lockfile changes. Scoped `rtk pnpm exec tsc --ignoreConfig --noEmit --lib dom,dom.iterable,es2022 --target es2022 --module esnext --moduleResolution bundler --skipLibCheck --strict src/lib/api.ts` passed. Existing `pnpm run typecheck` fails five TS6133 unused-import errors exclusively in untouched `src/lib/canvas-model.test.ts`; `git diff` verifies it is identical to base. Focused `rtk pnpm exec vitest run src/App.attachments.test.tsx src/App.turn.test.tsx --maxWorkers=1` **2/2** pass in 1.91s. `git diff --check` passes. Node tests <0.12s; Vitest one worker <2s; no Cargo, subprocess or DB.
- This is **not** Web UI completion: CLI at `crates/cli/src/main.rs:668-747` still opens a bare origin without providing any fragment; tab refresh clears the in-memory credential. Cross-file authentication delivery, refresh/rotation, second-client reuse, real server journey, ownership of disputed direct-fetch test/claim and 51-error canonical guard need integration-owner resolution. Leave implementation claim `blocked` with exact note; submit isolated candidate for independent verification only, not acceptance or main merge.
