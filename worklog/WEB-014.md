# WEB-014 worklog — chat navigation, search, long-history UX (GREEN-only, NOT ACCEPTED)

Claim: bounded newest-page-first history endpoint with opaque message-ID
cursor exists; archive durable; pin/share/content-search/temp-chat have
no persistence contract (actions disabled, not faked). Browser paging
tests unexecuted (no policy-compliant vitest/tsc). NOT ACCEPTED.

## Source evidence

- `tasks/WEB-014.md:30-42` — boundary: newest page in chronological render
  order, opaque persisted message-ID cursor, starts from 50 recent;
  archive real; sidebar filter title-only client-side; pin/share/search/
  temp-chat disabled; provider oldest-page bug explicitly NOT fixed.
- `crates/server/src/lib.rs:361-386` (`HistoryPageParams`, `list_history_page`):
  default limit 50, clamp 1..100; unknown cursor →
  `SessionError::HistoryCursorNotFound` → 404, never silent ordinary chat.
- `crates/sessions/src/lib.rs:605-625` (`history_page`): fork-manager
  and storage paths; `MessageNotFound` mapped to `HistoryCursorNotFound`.
- `crates/server/tests/session_history_api.rs:66-155` — T01 paging
  no-gaps/no-duplicates, T02 unknown cursor 404.
- Upstream pins: opencode `95daf90670b7c039c436c85537da5fbfe2205b41`,
  9router `17c4cc76877bd1755030a8414f8d0083f48dcccf`
  (`sources/upstream.lock.json:9-27`). No upstream paging reference
  claimed by card.
- `ralph.json`: WEB-014 `not-started`. Base rev `248f519`.

## Observed scenario (verify-only rerun, no product edit)

- `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 110 cargo test
  -p opencode-rk-server --test session_history_api`
  → 2 passed (`web_014_t01_recent_history_pages_without_gaps_or_duplicates`,
  `web_014_t02_unknown_history_cursor_is_not_found`), 0 failed, 0.02s.
- Product code predates lane → GREEN-only, NOT valid RED. No RED
  fabricated. Browser regression authored but runner-unverified
  (no vitest/tsc), per card.

## Target boundary

- Owned: this worklog only. No product/test file touched.
- Explicitly NOT owned: history endpoint, sessions paging, provider-context
  read path, browser sidebar/search code.

## Tests

- None frozen by this lane. Prior evidence:
  `crates/server/tests/session_history_api.rs` (161 lines, 2 tests)
  covers T01 paging + T02 cursor-404 only.
- T02 temp-chat/T03 a11y/T04 bounds+T05 reload: no frozen suite;
  pin/share/content-search/temp-chat have no daemon contract.

## Decisions

- `ponytail:` unavailable states stay disabled, never converted to
  persisted chat (card T02). Provider oldest-page bug out of scope.
- Cursor is opaque persisted message-ID; `before` parse failure = 404.

## Remaining unknowns

- Pin/share/content-search/temp-chat persistence contracts + browser
  paging/shortcut/focus tests need owning lane + vitest/tsc policy path.
- Frozen T01..T05 RED/GREEN owned by future lane; verifier decides.
- Story stays NOT ACCEPTED per `tasks/WEB-014.md:42`.
