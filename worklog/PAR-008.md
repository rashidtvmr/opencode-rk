# PAR-008 — web turn adapter verification

Claim: `crates/server/src/web_turn_adapter.rs` (516 lines, 5 in-file tests). Claimed via tools/completion_claims.py session ses_f41fada5cffeTIgTRJIerEKVPs.

Source evidence (HEAD f231a37):
- `crates/server/src/lib.rs:74` `pub mod web_turn_adapter;` (prewired by integrator).
- Owned file `crates/server/src/web_turn_adapter.rs:1-516`, `#![forbid(unsafe_code)]`, std only, zero stub markers (grep todo/unimplemented/ignore = 0).
- Boundaries: web_turn_adapter.rs only + scratchpad + ledger. No lib.rs edits, no test edits.

Observed scenario: implementation already landed by prior lane (commit aea6210 integrate). This lane is re-verification only: no code changes needed.

Target boundary: owned file only. Disjoint from PAR-009 (app_protocols.rs) same crate.

Tests: 5 in-file `#[cfg(test)]` (capability_matches_current_server_reality, capability_report_not_hardcoded, attachment_reaches_adapter_only_when_authorized, artifact_edit_version_allowed_but_run_apply_gated, unsupported_journeys_report_honest_unavailable). Frozen, zero edits.

Decisions: no implementation change; verify at HEAD and record evidence. Prior adapter detail in worklog/PAR-008-wadapter.md.

Remaining unknowns: none for this lane; router wiring is integrator-owned (already prewired).

Evidence (HEAD f231a37, verify-only, zero test edits):
- `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 180 cargo test -p opencode-rk-server --lib web_turn_adapter` → 5 passed 0 failed (195 filtered out).
- Owned file untouched: `git diff --stat` clean for web_turn_adapter.rs.
