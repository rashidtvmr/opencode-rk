# SDK-002 scratchpad

- claim: SDK-002, session ses_f384fede7ffenpg6X2t2lZtIyS, status in-progress via tools/completion_claims.py.
- source evidence:
  - tasks/SDK-002.md:1-91 (contract, bounds, frozen T01-T05).
  - crates/server/tests/sdk_spawns.rs:1-422 (frozen; `#[path="../src/sdk_spawns.rs"]`, FixturePort-only, secret-redaction asserts).
  - crates/server/src/sdk_spawns.rs:1-336 (pre-existing bounded Spawner/ChildHandle impl; caller-supplied SpawnPort, Rc<RefCell<Shared>> live table, kill-on-drop).
  - PLAN.md:5-6, docs/TDD.md:2-5, docs/SECURITY.md (capability/secret rules).
  - lib.rs:47 already declares `pub mod sdk_spawns` (integrator wiring present).
- observed: owned file AND frozen tests both pre-exist on disk (commit 1be93d3 landed wave); no RED to author — pre-green suspected, will verify without test edits.
- target boundary: only crates/server/src/sdk_spawns.rs + worklog/SDK-002.md + own ledger row. No lib/test/Cargo edits.
- tests: focused `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 timeout 180 rtk cargo test -p opencode-rk-server --test sdk_spawns -- --test-threads=1`.
- decisions: validate() rejects control chars in addition to NUL (card requires NUL; control-char rejection is stricter superset, secret-safe, no test conflict). next_id monotonic, never reused. await_startup error normalized to StartupTimeout; port.spawn errors pass through (StartFailed).
- unknowns: caller wiring gap — Spawner used by real daemon caller? lib.rs only declares module; check no caller wires it (expected gap for integrator).
