# WEB-004 scratchpad

Claim: WEB-004 claimed by ses_f387af8aaffeFWlAQiQn1gYKIU, scratchpad worklog/WEB-004.md. Ledger confirms in-progress.

Source evidence:
- `crates/server/src/control_plane_exposure.rs` (whole file, 142 lines): pure exposure gate, pre-existing implementation.
- Frozen tests `crates/server/tests/control_plane_exposure.rs` (T01-T05, `#[path]` include so no lib.rs touch needed).
- Task card `tasks/WEB-004.md`; `crates/server/src/lib.rs:23` already has `pub mod control_plane_exposure` (integrator pre-wired, not touched).

Observed scenario: ran `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 cargo test -p opencode-rk-server --test control_plane_exposure` → 5 passed, 0 failed (T01 loopback-allowed+order spy, T02 remote-deny-before-auth 403/remote_denied, T03 grant+header-spoof independence, T04 health-exempt+socket-local, T05 side-effect-free+redacted). Warning only: pre-existing unused-import warning elsewhere.

Target boundary: owned file only. No edits needed — implementation already satisfies contract:
- Default `{ allow_remote: false, bind: Loopback }` (lines 35-42); remote opt-in only.
- `check_exposure`/`check_peer` (71-100): loopback always Ok, socket always Ok, non-loopback needs grant; deterministic, no DNS/I/O/clock.
- `check_with_headers` (84-91) discards forwarded headers; T05 source-scan bans fs/net/tokio/clock/env.
- `is_gated_route` (105-107): only POST /control/*; GET /health exempt.
- Deny: 403 + `remote_denied` + fixed JSON body (DENY_BODY ≤512) + family-only log label (v4/v6/socket).
- O(1) pure sync, no side effects, caller owns lifetime.

Decisions: zero implementation edits (frozen tests green as-is; editing would risk the freeze). No test edits.

Remaining unknowns: none for this lane.

Integrator wiring proposal (not a claim — lib.rs already wired at :23, and tests bypass lib.rs via #[path]): no wiring action needed. Daemon bootstrap/bind enforcement, auth parsing, workspace routing, generated clients stay with their owners per card.

Tests: `cargo test -p opencode-rk-server --test control_plane_exposure` → 5/5 green, zero test edits.
