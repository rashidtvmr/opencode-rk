# WEB-004 worklog

Status: IMPLEMENTED / NOT ACCEPTED (verifier decides).

## Claim
Control-plane mutation routes unreachable from non-local peers without explicit
bounded remote grant; loopback always works; deny happens before auth parse and
before domain service.

## Source evidence
- Pinned OpenCode `95daf90670b7c039c436c85537da5fbfe2205b41`; handler blob
  `4c13afdb38a9f7452d6540fb1adf05129e9d649c` (scope only, no remote entrypoint).
- Route policy blob `c44db1edbbfb43fc000b18fe8afbddbe0a2933a6`.
- `sources/disc-003-reconciliation.json` id `opencode.server-control-plane`
  residual: reconcile WEB-004 separately from INT-008 pure compat.
- Classification: discovered surface residual; deliberate safer deviation:
  default-deny remote for control-plane mutations with explicit grant.

## Observed scenario
Module `crates/server/src/control_plane_exposure.rs` at HEAD `248f519`.
Pure O(1) gate over (peer, cfg); no DNS/I/O/clock/dial; forwarded headers
accepted-then-ignored via `check_with_headers`; socket peer local;
deny 403/remote_denied fixed template <= 512 bytes; family-only log label.
NOT wired into live axum `router()` in `lib.rs` — gate enforced by unit-test
contract, not by middleware yet (integrator gap, same class as WEB-003).

## Target boundary
`Exposure { allow_remote: false, bind: Loopback }` default;
`check_exposure(peer, cfg)`; order [exposure, auth, service] on allow,
[exposure] on deny; `GATED_PREFIXES = ["/control/"]`; GET /health exempt;
same (peer, cfg) => same verdict.

## Tests
Frozen: `crates/server/tests/control_plane_exposure.rs`
SHA-256 `4174a3dda0e117f8922ce0e7867e9e379d183964dacf0ca5fd37d8ff45601431`.
RED: compiling suite failing on missing exposure-gate module.
GREEN (2026-09-15, HEAD `248f519`):
`cargo test -p opencode-rk-server --locked --test control_plane_exposure`
=> 5 passed, 0 failed.

## Decisions
No code change (shared-router wiring belongs to integrator).
Stub scan clean; secret safety (family-only logs) verified by T05.

## Remaining unknowns
Live-router middleware wiring + bind-enforcement ownership (daemon config).
