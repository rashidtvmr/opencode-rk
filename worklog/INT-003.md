# INT-003 worklog (verification pass, in-progress lane)

## Claim

Verify the INT-003 provider-specific method dispatch boundary slice: bounded
static method table + caller-driven dispatch in
`crates/providers/src/int_methods.rs`, frozen tests in
`crates/providers/tests/int_methods.rs` (path-include, never touches
integrator-owned `lib.rs`).

## Source evidence

- Candidate base revision: `248f519`.
- Pinned OpenCode commit `95daf90670b7c039c436c85537da5fbfe2205b41`.
- Task card: `tasks/INT-003.md` (Status NOT STARTED in card; `ralph.json` lane
  `INT-003 in-progress`).
- Upstream selectors (read-only): `packages/core/src/integration.ts:63-200`,
  `:221-364`, `:380-515`; `packages/server/src/handlers/integration.ts:1-104`;
  `packages/protocol/src/groups/integration.ts:8-130`;
  `packages/core/test/integration.test.ts:101-348` (no direct provider-refresh
  test per gap file `refreshDirectIntegrationTestEstablished: false`).
- Gap: `sources/integrations-ownership-gap.json`
  (`credential-oauth-attempt-provider-lifecycle`, `integrationAuthSurfaceOverlap`);
  `sources/disc-003-reconciliation.json` findings `opencode.integration-auth` /
  `opencode.integrations` (both partial).
- Classification: partial upstream; this card is the deferred provider-method
  dispatch slice. Deliberate resource-bounded deviation: bounded static table +
  caller-driven dispatch, no background fibers, no unbounded attempt map.

## Target boundary

- Product: `crates/providers/src/int_methods.rs` only.
- Tests: `crates/providers/tests/int_methods.rs` only.
- No edits made by this worker: `git status --short` clean before and after.
- Explicitly not claimed: INT-004, INT-005, INT-006, INT-008, INT-002,
  INT-010 (per card TDD step 7).

## Contract as built

- `ProviderMethodKind`: `Key | OAuth | Env | ProviderNamed { provider, method }`;
  ids validated non-empty `a-z0-9-_`, max 64 bytes; `classify()` maps well-known
  names, rejects invalid with `InvalidId`.
- `register(provider, method, handler_id)`: duplicate replaces in place
  (len unchanged); `TableFull` with no mutation at 64 providers / 16
  methods-per-provider.
- `dispatch(provider, method, input) -> MethodReceipt { handler_id, accepted }`:
  exactly one handler; `UnknownProvider`/`UnknownMethod`; input bytes/handle
  beyond 4 KiB rejected with `InvalidId` before lookup. No network/secret/env/
  event/execution.
- `providers()` / `list_methods()` sorted; order-insensitive determinism.
- Caps frozen: `MAX_PROVIDERS=64`, `MAX_METHODS_PER_PROVIDER=16`,
  `MAX_ID_LEN=64`, `MAX_INPUT_BYTES=4096`.
- `MethodInput` borrows caller bytes; `Debug` redacts both fields; `Default`
  table allocates nothing until first register.

## Tests

- Frozen SHA-256 (test): `95ce3d9f13629377531c4bedec6a77eb4e8d8e70ead000282a52b9c821eb3820`;
  git object `fada7a360db43fd50c228677f97162fc43eab8e5`. No edits after hashing.
- Frozen SHA-256 (product): `7d71921b33053bd93b7138b803b0e2e33a6154c2fe67379d559ec9f79726c117`.
- RED: NOT independently witnessed by this worker (artifacts pre-existed at
  `248f519`; tree clean; no RED claimed).
- GREEN (witnessed): focused run => 5/5 pass (`int_003_t01..t05`), incl. 10k
  bulk-dispatch stability (T03) and zero-handler-exec on error (T04/T05).
- Full provider regression + `cargo check --workspace` (1m15s, pre-existing
  warnings only) + `git diff --check` clean — same run as INT-001/002 lanes.
- `rustfmt --check` on lane product file reports one pre-existing diff hunk
  (line ~212); NOT reformatted (integrator-owned).
- `tools/lane_gate.py` not applicable (storage-only gate).
- Stub scan: zero hits.

## Decisions

- No `lib.rs`/`Cargo.toml`/schema/migration edits.
- No reformatting of lane files.
- One heavy command at a time, `CARGO_BUILD_JOBS=2` (host ~1.8 GiB available).

## Remaining unknowns (addendum 2026-09-15, owned RED witnessed)

- RED (temp stub, restored byte-identical): unknown-method check replaced with
  `.unwrap()`; log `/tmp/opencode/int003_red.log` => 3 passed, 2 failed
  (T04/T05 bite via panic on unknown method; T01-T03 pass: happy/det/cap paths
  unaffected). First stub draft failed to compile (mismatched `Option` arms);
  discarded, not counted as RED; compiling RED is the one logged.
- Restore hash `7d71921b33053bd93b7138b803b0e2e33a6154c2fe67379d559ec9f79726c117`
  matches pre-stub; zero `TEMP STUB`; owned paths `git status` clean.
- GREEN: focused 3-target run => 5/5 each (`/tmp/opencode/int00x_green.log`);
  full `-p opencode-rk-providers` => lib 51 + 62 targets ok, 0 failed.
- rustfmt drift kept: `int_methods.rs:212` providers-collect one-line fmt; NOT
  reformatted per freeze.
- Verifier/controller acceptance external; not claimed.
- Cards still say NOT STARTED while `ralph.json` says in-progress; controller
  owns state transitions.
