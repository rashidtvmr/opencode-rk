# WEB-003 worklog

Status: IMPLEMENTED / NOT ACCEPTED (verifier decides).

## Claim
Concrete HTTP routes project from one authoritative Protocol table; every
served route exists in Protocol; handlers thin; domain/storage free of HTTP types.

## Source evidence
- Pinned OpenCode `95daf90670b7c039c436c85537da5fbfe2205b41`.
- `packages/server/src/api.ts` (blob `981ad28db93d253ac02e231b6dbc28f034fe5c35`).
- `packages/client/src/contract.ts` (blob `413fea9dc338b345d40e1dc4b4cf7a962eba1376`).
- `packages/client/README.md` (blob `8c53e47c7e681a43de56e307b355a8bc65d210a7`).
- Contract test blob `64a2e958ce2d86e6f66e03d5b9353dbd1ffb358a`.
- Protocol SSE endpoint `packages/protocol/src/groups/event.ts`
  (blob `a6fc5692eae7cf631678b04eb58b9615bca163c4`).
- Route policy blob `c44db1edbbfb43fc000b18fe8afbddbe0a2933a6`.
- Classification: discovered surface; deviation: checked-in Rust table, not codegen.

## Observed scenario
Two near-identical modules at HEAD `248f519`:
`crates/server/src/protocol_api.rs` (fn `no_direct_git_fs_net_imports`) and
`crates/server/src/route_table.rs` (fn `no_direct_io_imports`). Both encode the
same seed table (GET /health, POST /control/move_session, GET /events SSE),
MAX_ROUTES=256, thin-dispatch spy, sorted deterministic list.
Neither module is wired into `crates/server/src/lib.rs` router — projection
invariant is enforced by unit tests, not by the live axum `router()`.
Live `router()` in `lib.rs:59` serves `/api/*` + SPA fallback, a broader set
than the 3-row Protocol seed (known gap, owned by integrator/surface audit).

## Target boundary
`PROTOCOL_ROUTES` static slice; `served_routes()` sorted; `check_served` rejects
rogue route naming (method, path) and duplicate keys; `route_kind("/events")==Sse`;
dispatch calls exactly one domain method; no I/O/clock/secrets in table.

## Tests
Frozen: `crates/server/tests/protocol_api.rs`
SHA-256 `5f67e04bdd68a4cfa4bd97c741a5252b1757a35a9faa89741ca365e6948113b9`.
Mirror: `crates/server/tests/route_table.rs`
SHA-256 `a14cd4cb227a96d6d337c12c446e7b43144305afc4b8ba8fa5fc03bda7c155f6`
(function-name difference only).
RED: compiling suite failing on missing protocol-table module.
GREEN (2026-09-15, HEAD `248f519`):
`cargo test -p opencode-rk-server --locked --test protocol_api` => 5 passed.
`--test route_table` covered in combined run => 5 passed.

## Decisions
No code change in this lane (not allowed to edit shared `lib.rs`/router).
Record gap: live router superset not yet projected from PROTOCOL_ROUTES;
needs integrator decision (extend table vs scope live `/api/*` routes in).

## Remaining unknowns
Integrator wiring of `mod protocol_api` + router projection enforcement.
