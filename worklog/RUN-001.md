# RUN-001 scratchpad

Claim: RUN-001 via tools/completion_claims claim, session ses_f387af8bcffe4ODf6rkmz706r7, status in-progress.
Owned: crates/sessions/src/runner.rs + this file. Frozen: crates/sessions/tests/runner.rs (untouchable).

## Source evidence
- tasks/RUN-001.md: contract (RunKind/RunnerState/RunnerSet/RunGuard/BusyError/MAX_STEPS=64, single-flight 1 normal + 1 shell, RAII drop-as-cancelled, cancel_all, InvalidInput vs Busy split, no threads/I/O).
- crates/sessions/src/runner.rs:235 — pre-existing impl, `#![forbid(unsafe_code)]`, Arc<Mutex<Inner>> slot map, BusyError + RunnerError{InvalidInput,StepBudget,Busy}, MAX_STEPS=64, MAX_SESSION_ID_LEN=128.
- crates/sessions/src/lib.rs:48 `pub mod runner;` — integrator wiring already present (read-only observed, not edited).
- crates/sessions/tests/runner.rs:90 — 5 frozen tests T01..T05, `use opencode_rk_sessions::runner::{RunKind, RunnerError, RunnerSet, MAX_STEPS}`.

## Observed vs contract
- start(&self)->Result<RunGuard,RunnerError> vs card &mut self/BusyError: interior-mutability superset; BusyError preserved with From->RunnerError::Busy; tests assert RunnerError::Busy. Accept, no change.
- status() returns Idle/Running only; Busy variant pub but unreturned — matches card's status clause (Idle vs Running). Accept.
- cancel_all(&self): aliasing edge (live guard + cancel_all + restart = 2 guards one lane, late drop steals slot) exists but NO frozen test covers it; fixing needs ownership redesign outside minimal scope. Recorded, not changed.
- Empty entry on busy-reject path: entry pre-exists on contention, invalid returns before insert — set-unchanged holds. Accept.
- Idle retains bounded outcome counters only, no handles — matches card. Accept.
- No thread::spawn/process/net/fs in runner.rs; Mutex only. Accept.

## Tests
- Focused: `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 rtk cargo test -p opencode-rk-sessions --test runner` (no `timeout` bin on this macOS host; rtk passthrough used).
- Fresh-lane result: `cargo test: 5 passed (1 suite, 0.00s)` (T01 happy, T02 busy, T03 cancel/drop, T04 validation/steps, T05 no-side-effect).
- Side-effect scan: grep for thread::spawn|Command::new|std::process|std::net|std::fs|std::thread|tokio:: in runner.rs => no matches (exit 1).
- Prior-lane history (preserved): base rev 248f519; 4-suite batch 20 passed; RED probe (runner.rs hidden => E0583 at lib.rs:48, restored byte-identical c8ecbde1...01306); frozen sha256 eb01782c...2c1b8; ralph RUN-001 still not-started in controller state.

## Decisions
- Repair real code only if RED. Else verify-only lane: zero edits to runner.rs and tests.

## Remaining
- Run focused test, record exact tail.
- Ledger update honest (completed only if 5/5 green, zero test edits).
- Commit owned files (worklog + claims.json; runner.rs only if touched) + push. Report integration wiring separately, no parent completion claim.
