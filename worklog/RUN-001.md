# RUN-001 worklog (verify-only, dedicated)

## Claim
`crates/sessions/src/runner.rs` (235 lines) satisfies `tasks/RUN-001.md` (per-session runner slots). Frozen suite `crates/sessions/tests/runner.rs` 5/5 GREEN. Valid compiling-RED history. Verifier decides.

## Source evidence
- Base rev `248f519`. `tasks/RUN-001.md:8-9` owns `runner.rs` only.
- Impl: RunKind, RunnerState, RunnerSet, RunGuard, BusyError, MAX_STEPS, start/status/cancel_all. `RunnerError::Busy{session}` keeps session id; drop = Cancelled. Wired `sessions lib.rs:48`.

## Observed scenario
Compiling RED → impl → 5/5 GREEN (bundle record).

## Target boundary
- Frozen test untracked, 5 #[test]; sha256 `eb01782c59569ed34176ec74fc7c39dbe2de937eed38dd2a63394a5fcd92c1b8` (bundle prefix `ba048213e927250f`; drift noted, GREEN).
- Zero edits.

## Tests
- Rerun sessions 4-suite batch → 20 passed, 0 failed. Serial JOBS=2 THREADS=2 timeout 120.

## Decisions
- Guards synchronous RAII, spawn no thread; cancel_all/drop releases slots immediately.

## Remaining unknowns
- Acceptance verifier-owned. ralph.json untouched.

## Rerun 2026-09-16 (REL lane, verify-only, RED probe, no product edit)
- Rev `248f519d53ed29cbf7fef7ceb2b5010fb1a4224b`. Frozen suite `crates/sessions/tests/runner.rs` (sha256 `eb01782c...2c1b8`) 5/5 GREEN: `cargo test -p opencode-rk-sessions --test runner` → `ok. 5 passed; 0 failed`.
- RED probe: `runner.rs` temporarily hidden → compile `E0583 file not found for module runner` at `lib.rs:48` (suite fails to build = RED for missing behavior); restored byte-identical (`c8ecbde1...01306` before/after).
- Impl `crates/sessions/src/runner.rs` 235 lines: `RunKind{Normal,Shell}`, `RunnerState{Idle,Running{kind},Busy}`, `RunnerSet::new/start/status/cancel_all`, RAII `RunGuard::{complete,cancel,steps_remaining,take_step}` + kill-on-drop, `BusyError{session}` via `RunnerError::Busy`, `MAX_STEPS=64`, `MAX_SESSION_ID_LEN=128`.
- Bounds: one-normal-plus-one-shell cap, serial, no lib.rs/ralph.json/frozen edits this lane (lib.rs `pub mod runner` + tests pre-existing untracked).
- No product edit. Verifier decides acceptance; ralph RUN-001 still `not-started` in controller state.
