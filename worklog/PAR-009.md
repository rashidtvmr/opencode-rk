# PAR-009 — app protocols verification

Claim: `crates/server/src/app_protocols.rs` (794 lines, 16 in-file tests). Claimed via tools/completion_claims.py session ses_f41fada5cffeTIgTRJIerEKVPs.

Source evidence (HEAD f231a37):
- `crates/server/src/lib.rs:14` `pub mod app_protocols;` (prewired by integrator).
- Owned file `crates/server/src/app_protocols.rs:1-794`, `#![forbid(unsafe_code)]`, serde+std only, zero stub markers (grep todo/unimplemented/ignore = 0).
- WIRE_VERSION=1, MAX_FRAME_BYTES=262144, MAX_QUEUE_ITEMS=256, MAX_QUEUE_BYTES=4194304.

Observed scenario: implementation already landed by prior lane (commit aea6210 integrate). This lane is re-verification only: no code changes needed.

Target boundary: owned file only. Disjoint from PAR-008 (web_turn_adapter.rs) same crate.

Tests: 16 in-file `#[cfg(test)]` (envelope_roundtrip_ok, wrong_version_fails_closed, oversized_frame_fails_closed, malformed_ids_fail_closed, malformed_wire_bytes_fail_closed, digests_validate_hex_bounds, idempotency_key_deterministic, stale_cursor_requires_resync, future_cursor_requires_resync, empty_log_cursor, cursor_next_never_aliases, ephemeral_deltas_never_durable, event_frame_header_checks, queue_full_fails_closed, queue_byte_cap_fails_closed, errors_carry_no_input). Frozen, zero edits.

Decisions: no implementation change; verify at HEAD and record evidence.

Remaining unknowns: none for this lane.

Evidence (HEAD f231a37, verify-only, zero test edits):
- `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 180 cargo test -p opencode-rk-server --lib app_protocols` → 16 passed 0 failed (184 filtered out).
- Owned file untouched: `git diff --stat` clean for app_protocols.rs.
