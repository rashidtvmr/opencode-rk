# INT-007 worklog

## Claim

Verify the INT-007 typed-integration-handler projection candidate:
`crates/providers/src/handler_projection.rs` + frozen tests
`crates/providers/tests/handler_projection.rs` (T01..T05). No product edits
made; verification + mutation-probe evidence only.

## Source evidence

- Candidate base revision: `248f519`.
- Pinned OpenCode commit `95daf90670b7c039c436c85537da5fbfe2205b41`.
- `sources/disc-003-reconciliation.json` finding `opencode.integrations`
  (partial); `sources/integrations-ownership-gap.json` partition
  `typed-integration-http-handler`
  (`packages/server/src/handlers/integration.ts:1-104`,
  `packages/client/src/contract.ts:19-46`,
  `packages/client/test/contract-identity.test.ts:24-45`) + surface-overlap
  and ownership-blocker sections (per card).
- Contract read: `tasks/INT-007.md`, `docs/TDD.md`, `docs/SECURITY.md`.
- Card's suggested boundary is `crates/integration/...` (crate does not
  exist); actual placement `crates/providers/src/handler_projection.rs`
  alongside INT-004 is the integrator's additive-fragment choice. Additive
  only; `lib.rs` wiring predates this lane, untouched.
- Product sha256: `1a39f608b02d3258b973a2af54b349772f164c9e342210e01d105b89101240ec`.
- Tests sha256: `2ba919a4fe1db31fbb530c1a329ffac2f9f26bd7db81200d9b55ca3e47d7c759`.

## Target boundary

- Pure sync `project(op, meta, items) -> HandlerResponse`; no I/O, threads,
  clock, secrets, env, persistence, events, transport.
- Constants verified: `MAX_ITEMS = 128`, `MAX_ID_LEN = 64`, `MAX_LABEL_LEN = 64`.
- Normalization rules verified in code: sorted `List` + truncate flag;
  `Get` present/absent; `KeyAck`/`OAuthStart` meta validation;
  `OAuthComplete{None}` => `CodeRequired` regardless of auth flag (no
  mutation signal); `Some` + `auth_failed` => normalized `AuthFailed`;
  `AttemptPoll` missing => `NotFound`.
- Charter check: id/label charset `[A-Za-z0-9][A-Za-z0-9._-]*`,
  1..=64 bytes, matches card.

## Tests

- Frozen test sha256 unchanged: `2ba919a4...d7c759`.
- Mutation RED probe (temporary, restored byte-identical IDENTICAL):
  returned `AuthFailed` instead of `CodeRequired` for missing code =>
  `cargo test -p opencode-rk-providers --test handler_projection` =>
  **4 passed / 1 failed** (`int_007_t02_code_required_vs_auth_failed`
  FAILED). Evidence: `/tmp/opencode/int007_red.log`. Proves the
  code-required-vs-auth distinction is actually tested.
- GREEN after restore: 5 passed / 0 failed.
  Evidence: `/tmp/opencode/int007.log`.
- Full crate regression shared: 55 targets ok, 312 passed, 0 failed.
- `git diff --check` clean; provider check clean (pre-existing warnings only).

## Decisions

- No product edits: candidate implements the full projection matrix,
  byte-identical auth normalization, sorted-prefix truncation, sentinel-safe
  no-mutation behavior, and secret-free logging surface.
- No scope expansion: raw secrets, env, provider callbacks, persistence,
  attempt lifecycle (INT-004), events (INT-008), transport stay excluded.

## Alias/placement decision (2026-09-15 lane: INT-005/006/007)

- No aliases needed for INT-007: module `handler_projection.rs` already exposes
  the exact card names (`Method`/`NonSecretMeta`/`HandlerOp`/`HandlerResponse`/
  `HandlerError`/`project`/`MAX_ITEMS`/`MAX_ID_LEN`/`MAX_LABEL_LEN`).
- Placement deviation retained: card suggests `crates/integration/...` (crate
  does not exist); actual `crates/providers/src/handler_projection.rs` is the
  additive-fragment home alongside INT-004. No move; `lib.rs` wiring predates.
- Shared alias-lane evidence: RED stub-swap (`CodeRequired` -> `AuthFailed`) =>
  4 passed / 1 failed (`int_007_t02` FAILED), restored byte-identical
  (`diff -q` IDENTICAL), GREEN 5/5 after restore.
  Evidence: `/tmp/opencode/int007_red.log`, `/tmp/opencode/int007_green.log`.
- Frozen test hash unchanged: `2ba919a4...d7c759`; product hash unchanged:
  `1a39f608...01240ec`.

## Remaining unknowns

- None for this boundary. Verifier/controller acceptance remains external
  and is not claimed.
