# INT-002 worklog (verification pass, in-progress lane)

## Claim

Verify the INT-002 caller-driven key/OAuth connection projection slice: pure
in-memory table in `crates/providers/src/int_connect.rs`, frozen tests in
`crates/providers/tests/int_connect.rs` (path-include, never touches
integrator-owned `lib.rs`).

## Source evidence

- Candidate base revision: `248f519`.
- Pinned OpenCode commit `95daf90670b7c039c436c85537da5fbfe2205b41`.
- Task card: `tasks/INT-002.md` (Status NOT STARTED in card; `ralph.json` lane
  `INT-002 in-progress`).
- Upstream selectors (read-only): `packages/core/src/integration.ts:221-364`
  connection paths, handler `packages/server/src/handlers/integration.ts:19-104`,
  spec `packages/protocol/src/groups/integration.ts:8-130`, test
  `packages/core/test/integration.test.ts:101-348`; gap file
  `credential-oauth-attempt-provider-lifecycle` partition with
  `integrationAuthConstraints` (10-min lifetime, 1-min retention, 30-s scrub,
  5-min refresh window) — none executed here by design.
- Classification: partial upstream + deliberate safer deviation
  (caller-supplied code only, nothing retained; explicit caps).

## Target boundary

- Product: `crates/providers/src/int_connect.rs` only. Distinct lane file from
  the older `integration_connection.rs` (owned-string variant, separate slice);
  this lane borrows `code: Option<&str>` and retains nothing.
- Tests: `crates/providers/tests/int_connect.rs` only.
- No edits made by this worker: `git status --short` clean before and after.
- Excluded: INT-001 registry internals, INT-004 attempt lifecycle, EXT-007,
  authorize/refresh/callback execution, env, persistence, network, HTTP, events.

## Contract as built

- `ConnectInput { integration: &str, method_id: &str, code: Option<&str> }`;
  charset `[A-Za-z0-9][A-Za-z0-9._-]*` 1..=64; code 1..=512 printable ASCII.
- `connect(&mut self, input, known: &dyn Fn(&str,&str) -> Option<MethodKind>)`:
  oracle consulted exactly once per call before validation; unknown/disallowed
  pair => `NotFound` (normalized); OAuth without code => `CodeRequired`, nothing
  recorded; key or OAuth-with-code => `Active` with monotonic `ConnId` from 1.
  Code bytes used for verdict only, never cloned/stored.
- `get` / `list` (sorted by `ConnId`) / `disconnect` (unknown => `false`).
- `ConnectionInfo` has no code/secret/token field by construction; `Debug` for
  `ConnectInput` and `ConnectionTable` redacts code/ids-only.
- Caps frozen: `MAX_CONNECTIONS=64`, `MAX_CODE_LEN=512`.

## Tests

- Frozen SHA-256 (test): `202eb24b822364c2d87206847614a272637e531052df804d686ad041e8eb68f4`;
  git object `167f257afc07defdb706d98dc6557158a6d19eaf`. No edits after hashing.
- Frozen SHA-256 (product): `5b19f2df62ef9730cfe13e65d9fc1dc5ee26d1e8de8b2d266e67e29df8376629`.
- RED: NOT independently witnessed by this worker (artifacts pre-existed at
  `248f519`; tree clean; no RED claimed).
- GREEN (witnessed): focused run => 5/5 pass (`int_connect_t01..t05`).
- Full provider regression: `cargo test -p opencode-rk-providers` => all green,
  0 failed (lib 51 + all targets).
- `cargo check --workspace` passed (pre-existing unrelated warnings only).
  `git diff --check` clean. `rustfmt --check` on lane product file: clean (no
  diff reported for `int_connect.rs`).
- T05 asserts oracle call count == connect count (no hidden retries), zero code
  bytes in logs/table debug, no child/sockets/files side effects.
- `tools/lane_gate.py` not applicable (storage-only gate).
- Stub scan: zero hits.

## Decisions

- No `lib.rs`/`Cargo.toml`/schema/migration edits.
- Memory budget respected: one heavy command at a time, `CARGO_BUILD_JOBS=2`.

## Remaining unknowns (addendum 2026-09-15, owned RED witnessed)

- RED (temp stub, restored byte-identical): oracle forced `None`
  (`known(...).and(None)`); log `/tmp/opencode/int002_red.log` => 0 passed,
  5 failed (all connect paths bite; count proves no silent pass-through).
- Restore hash `5b19f2df62ef9730cfe13e65d9fc1dc5ee26d1e8de8b2d266e67e29df8376629`
  matches pre-stub; zero `TEMP STUB`; owned paths `git status` clean.
- GREEN: focused 3-target run => 5/5 each (`/tmp/opencode/int00x_green.log`);
  full `-p opencode-rk-providers` => lib 51 + 62 targets ok, 0 failed
  (`/tmp/opencode/int00x_fullreg.log`). `int_connect.rs` rustfmt clean.
- Verifier/controller acceptance external; not claimed.
- Fresh-worktree RED reconstruction left to controller.
