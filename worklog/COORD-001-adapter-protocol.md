# COORD-001 scratchpad: adapter protocol

- Claim: define native harness adapter protocol binding `TrustedAdapter` roles/capabilities/cancellation/bounds to real APIs; mark unbuilt host bindings BLOCKED.
- HEAD: `5af7884cf7637c0760d985da7a03f2f99ccd0c78` (completion base `9cc0a34` per lease; verified via `git rev-parse HEAD`).
- Source evidence:
  - `tools/completion_scheduler.py:79-94` `TrustedAdapter` Protocol: `execute(task)->Candidate`, `verify(task,candidate)->Verification`, `integrate(task,candidate)->Integration`, `verify_integrated(task,candidate,revision)->Verification`.
  - Dataclasses `tools/completion_scheduler.py:33-64`: `Task(id,owned_path,frozen_tests_sha256,test_obligations,dependencies)`, `Candidate(task_id,revision,worker,changed_paths)`, `Verification(task_id,revision,verifier,frozen_tests_sha256,test_count,test_obligations,passed)`, `Integration(candidate_revision,integrated_revision,integrated)`.
  - Scheduler entry `tools/completion_scheduler.py:158-160`: `run_rolling(tasks,adapter,*,capacity=20,max_unverified=20,max_attempts=3,stage_timeout=3600)`; gates at `:168-176` (capacity 1..20, unverified 1..20, attempts 1..3, finite timeout); cancellation join at `:253-260`.
  - Scheduler-side guards (trusted, host-independent): `validate_candidate` `:136-140` (one-file grant `changed_paths==(owned_path,)`), `validate_proof` `:143-155` (passed True, revision match, frozen hash match, `verifier!=worker`, count>=obligations, obligations subset).
  - Legacy prototype (NOT release authority): `tools/ralph_loop.py:489-505` `run_worker(worker,prompt,workdir,timeout_s)` shells `opencode run --agent <id> --format json <prompt>`; `tools/ralph_loop.py:508-528` `run_verification(commands,workdir)`; worktree `create_worktree` `:396-418`, ff-only `finalize_worktree` `:421-448`. COMPLETE_APP.md: old loop not approved acceptance authority until audit repaired.
  - Controller targets `config/completion-controller.json:1-24`: target/max 20, maxUnverified 20, maxHeavy 1, attempts 3, refill on-each-completion, 8GiB/2GiB reserve, `allowBudgetIncrease:false`, `respectHarnessConcurrencyLimit:true`, note forbids emulating missing native delegation with 20 heavy CLI processes.
  - Card: `python3 tools/completion_plan.py --card COORD-001` (5 tests T01..T05); `tools/completion_plan.py --check` = SPEC OK (not product acceptance).
  - Scheduler negative tests `tests/completion/test_scheduler.py:79-199`: refill-before-slowest, integration-failure blocks dependents, post-merge fail not accepted, zero-test/frozen-change/revision/self-verify/omitted-obligation/out-of-scope rejected, same-path serial, retry cap 3, ambiguous integration never blindly retried, cancellation joins owned calls, stage timeout blocked. These test the primitive, not a native host binding.
- Observed scenario: `docs/ADAPTER_PROTOCOL.md` (old) documents only legacy `run_worker`/`run_verification` prototype + aspirational sandbox section; no `TrustedAdapter` role binding, capability schema, identity rule, output bound, or BLOCKED host-API table. `tools/harness_adapter/` absent (glob no match).
- Target boundary: owned file `docs/ADAPTER_PROTOCOL.md` only (+ this scratchpad). Docs/stdlib only. No secrets/DB/cargo-workspace.
- Tests: doc task, no product RED suite. Evidence commands: `timeout 30 python3 tools/completion_plan.py --check`; `python3 -c ast` signature dump above; `timeout 60 python3 -m unittest discover -s tests/completion -p 'test_*.py'` (read-only, optional confirm scheduler still green; not run by this lane to avoid heavy parallel load — verifier runs).
- Decisions: rewrite doc around real `TrustedAdapter` shapes; keep legacy prototype as explicitly non-authoritative mapping; every section carries concrete API shape + evidence command; unimplemented host bindings (native delegate API, OS grant, test-author service, VCS integrator, credential/lease providers) marked BLOCKED with exact missing-surface reason, never passed.
- Remaining unknowns: actual native subagent spawn API (host has none vendored — BLOCKED); OS isolation backend selection per platform (BLOCKED); test-author service identity/endpoint (BLOCKED); integrator VCS credentials/serialized lane owner (BLOCKED); provider concurrency/rate source `config/controller.settings.json` values at runtime (operator-owned).
