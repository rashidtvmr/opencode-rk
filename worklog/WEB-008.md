# WEB-008 worklog — edit/retry/regenerate/branch semantics

Claim: pure branch/retry model implemented; frozen suite GREEN 5/5.
Source evidence:
- Upstream pins: opencode `95daf90670b7c039c436c85537da5fbfe2205b41` (`sources/upstream.lock.json`). Dependency SESS-018 typed fork boundary: native `ForkProvenance{parent_session_id, fork_message_seq}` in `crates/sessions/src/branch_v2.rs` (:25-26); durable schema stores seq, NOT `fork_message_id` (card contract).
- Impl: `crates/sessions/src/web_008_lane.rs` — `fork_from_message` (:140, inclusive copy, atomic pre-checks), `fork_provenance` (:198), `fork_depth` (:217), `branch_child_title` (:242, `Branch: ` + 512B cap), `begin_retry`/`fail_retry`/`cancel_retry`/`complete_retry` (:255-326, single turn slot `MAX_ACTIVE_TURNS=1`), snapshot round-trip (:384-504). Bounds: `MAX_FORK_DEPTH=8`, `MAX_FORK_COPY_MESSAGES=500`, `MAX_TITLE=512`.
- Server boundary: `SessionService::branch_from_message`/`prepare_retry_branch` in `crates/sessions/src/lib.rs` (:717-837) + `POST /api/sessions/{id}/messages/{message_id}/branch` in `crates/server/src/lib.rs` (:587-609); regression `session_branch_api` 4/4 GREEN.
Observed scenario: frozen RED predates this lane (commit `3db7402`); re-verified GREEN 5/5 this rev. RED re-proof pattern (same as WEB-007 lane) available on request; frozen tests untouched.
Target boundary: `crates/sessions/src/web_008_lane.rs` (owned, `#[path]`-included, not in `lib.rs`).
Tests: `crates/sessions/tests/web_008_lane.rs` SHA-256 `f63116f7…031b` (this rev). Frozen — not edited.
Decisions: pure state, no IO/clock/network; system/tool rows not branch boundaries; child title deterministic+bounded; failures atomic (no partial child).
Remaining unknowns: project-aware scope preservation is future work per card ("later project-aware implementation"); verifier decides NOT ACCEPTED resolution.
