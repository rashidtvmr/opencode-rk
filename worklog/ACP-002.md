# ACP-002 worklog

Status: IMPLEMENTED / NOT ACCEPTED (verifier decides).

## Claim
ACP file capabilities over one caller-supplied root: text read/write under
1 MiB via broker traits; Terminal/Auth/ModeSwitch/HistoryRestore/UpdateStream
rejected with typed Unsupported naming the variant.

## Source evidence
- Commit `248f519`; task `tasks/ACP-002.md`; PLAN.md 5-6; TDD.md 2-5; SECURITY.md 14-32.
- Upstream pin `95daf90670b7c039c436c85537da5fbfe2205b41` vs observed
  `07619a09d6da7945ea3fdbb4f8ae5ce8dc2b6eeb` never equated; DISC-003 IN PROGRESS.
- Local absence pre-slice: `crates/server/src/acp_files.rs` did not exist; sibling
  `repo_ops.rs` (git planner) and `control_plane_inputs.rs` (MoveSession decode)
  own other contracts, not re-owned. Sibling ACP-001 owns framing/session/modes.
- Classification: discovered scope under REQ-045; deliberate safer deviation
  (explicit 1 MiB cap + root confinement; upstream states neither).

## Observed scenario
Module `crates/server/src/acp_files.rs`: `Capability` closed 7-variant enum;
`is_supported` true only FileRead/FileWrite; `require` else
`Unsupported{capability}`; `read_text`/`write_text` enforce rel rules
(non-empty, <=512 chars, no absolute/drive/UNC/backslash, no empty/./..
segments) then 1 MiB cap then UTF-8 (read) via `FileReader`/`FileWriter`
traits only. No fs/net/thread/globals; errors carry paths/names, never content.

## Target boundary
`Capability`, `FileReader`, `FileWriter`, `AcpFileError`, `MAX_ACP_FILE_BYTES`,
`MAX_REL_CHARS`, `is_supported`, `require`, `read_text`, `write_text`.
`crates/server/tests/acp_files.rs` uses `#[path]` include; lib.rs wiring left
to integrator (lane never edits shared lib.rs per ownership lock).

## Tests
- Frozen: `crates/server/tests/acp_files.rs`
  SHA-256 `8c248471caa82230ae682256fbe1703fe06adbb5d921f6ef86847873296717b4`
- Impl: `crates/server/src/acp_files.rs`
  SHA-256 `bd64b42082357c6ac2136eb152e0ff28b09e36b291ad9f8ceeebe031c62b6b58`
- RED probe (2026-09-16, temp stub in owned file, restored byte-identical):
  `is_supported` widened to also accept Terminal => `cargo test
  -p opencode-rk-server --test acp_files` => 3 passed, 2 failed
  (`acp002_t03_unsupported_named_no_side_effect` panic at tests:121,
  `acp002_t05_isolation_and_safety` panic at tests:243). Restored via backup;
  `diff -q` IDENTICAL, hash matches pre-probe.
- GREEN (restored impl):
  `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 120 cargo test
  -p opencode-rk-server --test acp_files -- --test-threads=2`
  => 5 passed, 0 failed.

## Decisions
Fixture-only `FixtureFs`/`RefusingWriter` broker harness; oversize write
rejected before trait call (atomic, fixture bytes unchanged); `text.len()`
byte cap; per-call sync, caller owns root/traits/lifetimes.

## Remaining unknowns
Production trait wiring to real FS (integrator); lib.rs `mod acp_files;`
registration (integrator lane). Frozen tests never edited.
