# Worklog APP-002 (daemon_client lane)

Claim: APP-002 claimed OK via cc.claim session ses_orch_APP002, scratchpad worklog/APP-002.md, status in-progress.
DEP RISK: APP-002 deps AUD-001(blocked)+AUD-006(completed). AUD-001 blocked => NOT pickable per ready_tasks, but claim() does not check deps and succeeded for the orchestrator session. Proceeding ONLY with owned scope below; no attempt to unblock AUD-001.

Source evidence (HEAD 022577c):
- crates/server/src/daemon.rs:43-48 pid_alive (/proc exists, 0 dead).
- crates/server/src/daemon.rs:107-116 BackendDescriptor {pid, http_origin, schema_version, auth_token}; empty token = legacy/stale.
- crates/server/src/daemon.rs:137-165 read_backend_descriptor: symlink_metadata pre-read gate, size cap, serde parse, schema==WIRE (contracts lib.rs:14 =1), pid alive, loopback port, auth_token non-empty else Ok(None).
- crates/server/src/daemon_auth.rs:32-34 TOKEN_BYTES=32, TOKEN_HEX_LEN=64; :53-63 from_published rejects empty/non-64-hex; :71-81 verify_bearer; :151-176 require_bearer gates /api/*, /health public.
- crates/server/src/lib.rs:112-171 router_with_auth(state, Some(auth)) gates /api/*; health returns schema_version/status/runtime only.
- crates/cli/src/chat.rs:400-402 probe_daemon = GET /health == 200 only; :404-432 spawn/readiness, owned child killed on exit.
- crates/cli/src/main.rs:23 mod daemon_client (declared, no uses); :532-578 serve path uses server read/publish directly, no bearer send-side, publishes via publish_backend_descriptor (empty token) — NOT owned, left for RC/serve lane.
- crates/cli/src/daemon_client.rs: pre-existing pure decision module (13 tests green at baseline). GAP: BackendDescriptor copy lacks auth_token; no bearer send-side; no credential-bound discovery (legacy blank-token descriptor would validate+reuse); no actionable version error text; no stop/restart credential policy; no concurrent-startup decision.

Observed: client cannot attach Authorization header; credential never bound to discovery.
Target boundary: crates/cli/src/daemon_client.rs ONLY. No server files, no main.rs. std-only, no sockets/files, rustc --test standalone. Existing 13 tests frozen (zero edits); new tests additive.

Tests (new, additive in mod tests):
- T-concurrent: 20 threads try_become_owner => exactly 1 Owner, 19 Attacher (one daemon).
- T-credential-bound: parse_authenticated_descriptor refuses forged/stale-shaped/missing/blank/non-hex token bytes; legacy 3-field descriptor refused (no credential to send).
- T-file-gate-before-credential: bad meta (symlink/other-user/oversize) refuses even with wellformed token bytes.
- T-occupied-port: decide_lifecycle_authed(None, Some(200)) => RefuseOccupiedPort; exhaustive match, no kill variant; LifecycleAction enum UNCHANGED.
- T-version-actionable: ServerNewer/ClientNewer => Some(text with versions + upgrade/restart action); Compatible => None.
- T-credential-policy: Rotate => minted; Preserve wellformed => old; Preserve malformed/None => minted.
- T-bearer-send: authorization_header == "Bearer <token>" exact; token shape 64 hex enforced by bind.
- T-authed-reuse: Some(valid auth)+200 => Reuse(inner); Some+500 => StartNew; None+None => StartNew.

Decisions: BackendDescriptor 3-field struct FROZEN (existing tests construct it literally); auth carried in new AuthenticatedDescriptor wrapper. DescriptorReject gains NO new variants (Malformed reused) to avoid breaking existing matches. LifecycleAction UNCHANGED (existing test matches exhaustively). Token shape mirrors daemon_auth TOKEN_HEX_LEN=64 without importing (dep-free).

Remaining: serve/main.rs bearer wiring + descriptor publish-with-auth belong to serve/RC lane, not this file.
