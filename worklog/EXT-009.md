# EXT-009 worklog

## Claim

Own the skill/command namespacing partition only: bounded in-memory
`(kind, name)` registry with later-source precedence, deterministic
sorted projections, caller-owned lifetimes. No discovery, filesystem,
network, execution, or policy gating.

## Source evidence

- Candidate base revision: `248f519d53ed29cbf7fef7ceb2b5010fb1a4224b`.
- Pinned OpenCode commit: `95daf90670b7c039c436c85537da5fbfe2205b41`
  (`sources/upstream.lock.json`).
- `tasks/EXT-009.md`: REQ-005, five test obligations, explicit caps
  (`MAX_NAMES=512`, `MAX_NAME_LEN=64`), excluded partitions.
- Product boundary: `crates/tools/src/plugin_namespace.rs` (wired in
  `crates/tools/src/lib.rs`), canonical tests
  `crates/tools/tests/plugin_namespace.rs`. Fallback lane pair
  (`ext_namespacing_lane`) is a byte-identical copy incl. via `#[path]`;
  no shared-file edits needed by lanes.
- Classification: partial upstream + resource-bounded deviation (caps).

## Target boundary

- Product implementation: `crates/tools/src/plugin_namespace.rs` only
  (already wired; no `lib.rs`/`Cargo.toml`/schema edits by this slice).
- Independent tests: `crates/tools/tests/plugin_namespace.rs` only.
- Status/evidence: this worklog and `tasks/EXT-009.md`; controller/verifier
  acceptance untouched.

## Tests

- Canonical: `cargo test -p opencode-rk-tools --test plugin_namespace`
  => 5 passed / 0 failed (`/tmp/opencode/ext_canonical.log`).
- Lane mirror: `--test ext_namespacing_lane` => 5/5
  (`/tmp/opencode/ext_lane_green.log`, earlier `/tmp/opencode/ext009.log`).
- Frozen SHA-256 (tests, read-only, never edited here):
  `b87649f024e24173ad7da14b8a1716df45807ebc5323c2597a2a68fcf5f911f0`.
- No RED re-run: implementation predates this lease (shipped in HEAD
  commit `248f519`); RED baseline owned by prior lane, not fabricated here.
- Lib regression: `cargo test -p opencode-rk-tools --lib` => 49/49
  (`/tmp/opencode/tools_lib.log`).

## Decisions

- No product-code change by this slice: implementation already satisfies
  all five obligations (happy path, precedence, validation/overflow
  atomicity, determinism, side-effect safety).
- No cross-lane edits; pre-existing dirty files
  (`crates/sessions/src/share_merge.rs`, `share_queue.rs`,
  `crates/providers/src/mcp_transport.rs`) left untouched; only
  `plugin_transform.rs` (EXT-011) edited, see EXT-011 worklog.

## Re-verification lease (2026-09-15)

- Base `248f519`; no product-code edit. `plugin_namespace.rs` ==
  `ext_namespacing_lane.rs` byte-identical, SHA-256 `5eaa922a...` both.
- Canonical + lane mirror 5/5 each in combined 40/40 run, exit 0
  (`/tmp/opencode/ext009_012_green.log`); lib 49/49
  (`/tmp/opencode/tools_lib.log`).
- Hashes disk vs frozen HEAD: tests disk `17682207...` / frozen `b87649f0...`;
  lane tests disk `9657b23f...` / HEAD `1c33a504...`. Fmt-only drift, GREEN.
  Tests untouched; HEAD values authoritative.
- Dedupe refused (integrator-owned; `#[path]` isolation; no `lane2` rule
  found): see EXT-011 worklog. No shared-file edits.

## Remaining unknowns

- None for this slice. Verifier acceptance external, not claimed.

## Re-verify 2026-09-16 (DB/DISC/EXT lane)
- Impl: `crates/tools/src/plugin_namespace.rs` (124L), wired `lib.rs:41`. Tests: `crates/tools/tests/plugin_namespace.rs` (`#[path]`, 5 fns ext009_t01..t05).
- GREEN: bounded `--test plugin_namespace` => 5 passed / 0 failed.
- RED probe (isolated /tmp/opencode/redprobe/p_namespace copy): removing overflow guard `if self.entries.len() >= MAX_NAMES` => ext009_t03 FAILED, 4/1. Sensitive.
- Stub/IO grep clean. No edits made (worklog only).
