# WEB-011 worklog — attachments, screenshots, local Library

Claim: bounded attachment/Library model implemented; frozen suite GREEN 5/5.
Source evidence:
- Upstream pins unchanged. Dependency: V2 payload/blob/message-parts storage; provider attachment adapter (unavailable — send disabled while attached, per card; no fake-text degradation).
- Impl: `crates/server/src/web_attachments.rs` — `validate_attachment` (:142, side-effect-free reject), `ingest` (:259, FNV-1a content-addressed dedupe, one blob), `resolve` (:292), `remove_attachment` (:304, refcount; shared bytes survive), `stage_temp`/`abort_temp` (:319-333), `add_to_library`/`attach_reference` (:341-398), `export_snapshot`/`import_snapshot` (:402-509, unknown digests restore as length-markers so refs stay resolvable). Bounds: 8MiB/file, 8/turn, 64MiB temp, 256 library, 256MiB retained. `safe_name` (:123) rejects path separators, `.env`/dotfiles, empty/overlong.
- Server boundary: `POST/GET/DELETE /api/sessions/{id}/attachments…` in `crates/server/src/lib.rs` (:74-83, :427-485); legacy-session ingest path exists, format-2 returns `DraftAttachmentUnavailable` explicitly.
Target boundary: `crates/server/src/web_attachments.rs` (owned, `#[path]`-included, not in `lib.rs`).
Tests: `crates/server/tests/web_attachments.rs` SHA-256 `328c4d65…5222d` (this rev). Frozen — not edited.
Decisions: std-only; digest algorithm FNV-1a (non-crypto dedupe key, not a security boundary); snapshot format is line-based, `BadSnapshot` on any deviation.
Remaining unknowns: unified format-2 blob store, screenshot capture, paste/drop + Library refs, provider adapter; verifier decides.
