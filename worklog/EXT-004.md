# EXT-004 worklog

## Claim

Explicit deferred-activation boundary for reload/watch: bounded auditable record of reload requests + watcher interest; zero filesystem watching, zero host launch, zero external activation.

## Source evidence

- Candidate base revision: `248f519` (HEAD); lane history `42b471c` for `plugin_deferred.rs`.
- Pinned OpenCode commit: `95daf90670b7c039c436c85537da5fbfe2205b41` (`sources/upstream.lock.json:9`, `tasks/EXT-004.md:51`).
- DISC-003 finding `opencode.extensibility` in `sources/disc-003-reconciliation.json:1`: "reload/watch behavior and deferred external plugin activation remain design work." EXT-004 owns exactly that reload/watch + deferred-activation partition; not lifecycle (EXT-001), builtins (EXT-002), host/hook/skill/command/TUI partitions.
- Gap file `sources/req017-extensibility-ownership-gap.json`: touches only the reload/deferred dimension of the six unresolved partitions; already-owned EXT-013/TOOL-007/UI-010 not re-owned (`tasks/EXT-004.md:40-46`).
- Classification: partial/planned upstream (reload/watch deferred) + deliberate deviation (bounded deferred-event log with explicit caps) per `tasks/EXT-004.md:51-54`.

## Target boundary

- Product: `crates/tools/src/plugin_deferred.rs` only (tracked, clean vs HEAD; `pub mod plugin_deferred;` in `crates/tools/src/lib.rs:36`).
- Tests (frozen, must not edit): `crates/tools/tests/plugin_deferred.rs` (SHA-256 `6766eac911b093e572b064cf66f1eeb39c9b1e68542b48f1ba292739da2a258e`).
- No Cargo dependency change. No shared-file edits by this lane (parallel duplicate `ext_deferred_lane.rs` untouched).

## Contract decisions

- `DeferredReason { ExternalActivationDeferred, WatchDeferred, ReloadDeferred }`; `DeferredEvent { seq, plugin: Option<PluginId>, reason }`; `EXT4_MAX_DEFERRED=128`, `EXT4_MAX_WATCHERS=32`; `WatchLabel` 1..=64 chars `[A-Za-z0-9][A-Za-z0-9._-]*`.
- `DeferredLog::new()` empty, no I/O/threads. `request_reload`/`note_external` cap-check then push with monotonic `seq` from 1 (never reused after drain); never touch any registry; unknown ids still record. `add_watcher` validates label, rejects dup, cap-checks watchers then events, records interest + appends `WatchDeferred` event (auditable). `remove_watcher` unknown => `false`, history append-only untouched. `list_deferred` sorted by seq. `drain(n)` removes oldest n (0 no-op, over-len empties, never panics). Sync, non-blocking, caller-owned lifetime. Events hold seq/id/reason only; labels short names only.

## Tests

- Frozen file: `crates/tools/tests/plugin_deferred.rs`, SHA-256 `6766eac911b093e572b064cf66f1eeb39c9b1e68542b48f1ba292739da2a258e`.
- RED: pre-existing from wave-6 lane `42b471c`; not re-established destructively (same `#[path]` constraint).
- GREEN (this lane, verified): `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 110 cargo test -p opencode-rk-tools --test plugin_deferred` => 5 passed / 0 failed (T01 seqs 1,2,3 + reasons + determinism replay; T02 watcher ids 1,2, dup/bad-label rejects, remove true then false, no OS watchers via fd/thread scan; T03 fill-128 then Overflow, drain(10) seqs 1..=10, drain(0) no-op, drain(MAX) empties, seq 129 monotonic, watcher cap 32; T04 fake-registry byte-identical + Ready intact + unknown-id still records; T05 disabled constructs nothing, no watchers/children/files, no secret bytes).
- Trio + `cargo check -p opencode-rk-tools` ok.
- No `todo!/unimplemented!/stubs`; only `dlopen` hit is a doc comment ("zero `dlopen`"); no fs/net/process/notify/tokio/rusqlite refs in product code (grep clean).

## Implementation

- No code changes by this lane: module already implements the full EXT-004 contract natively (cap-checked push, monotonic seq, watcher+event audit pairing, append-only history on remove, saturating drain).

## Remaining unknowns / gaps

- `crates/ext` crate / `crates/ext/src/deferred.rs` / `cargo test -p opencode-rk-ext` in task card do not exist; code lives in `crates/tools`. Needs controller disposition.
- Parallel duplicate `ext_deferred_lane.rs` mirrors this file (modulo header/`forbid(unsafe_code)`); dedupe owned by integrator.
- Verifier/controller acceptance external; not claimed.

## DEDUP lease (this session)
- `ext_deferred_lane.rs` converted to re-export shim over canonical `plugin_deferred.rs`. Frozen `#[path]` test untouched, 5/5 green. See `worklog/EXT-DEDUP.md`.

## Re-verify 2026-09-16 (DB/DISC/EXT lane)
- Impl: `crates/tools/src/plugin_deferred.rs` (192L), wired `crates/tools/src/lib.rs:36`. Tests: `crates/tools/tests/plugin_deferred.rs` (`#[path]`, 5 fns ext004_t01..t05).
- GREEN: bounded `--test plugin_deferred` => 5 passed / 0 failed.
- RED probe (isolated copy): neutralizing overflow guard `return Err(BoundaryError::Overflow)` => ext004_t03 FAILED, 4/1. Sensitive.
- Stub/IO grep clean. No edits made (worklog only).
