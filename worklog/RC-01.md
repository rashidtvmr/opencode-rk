# RC-01 authenticated daemon API — worklog

Claim: task RC-01, session ses_orch_RC01d (+ses_orch_main inline finish), status in-progress.
Scratchpad: worklog/RC-01.md
Owned scope: crates/server/src/ ONLY. No cli edits.
Base: c1866be

## Source evidence
- `sources/completion/audits/AUD-001.json:247-265` repairChild RC-01: no
  Authorization/bearer/require_auth/auth_middleware in crates/server/src;
  /health + all /api routes in lib.rs:104-155 unauthenticated.
- `crates/server/src/lib.rs:71-78` axum imports; `lib.rs:100-159` AppState + router.
- `crates/server/src/daemon.rs:107-112` BackendDescriptor {pid, http_origin,
  schema_version}; `daemon.rs:242-263` publish_backend_descriptor.
- `crates/cli/src/daemon_client.rs:50-56` client descriptor copy; custom strict
  parser ignores unknown fields (forward compatible with new auth_token field).
- 40 test call sites use `router(AppState { sessions, catalog })` literally —
  AppState gains NO new field; `router()` keeps signature, delegates to
  `router_with_auth(state, None)`.

## Observed scenario
- Gap: any local process can call daemon /api; discovery trusts loopback+pid.

## Target boundary
- New module `daemon_auth.rs`: token mint (std-only /dev/urandom hex),
  constant-time `verify_bearer`, axum `require_bearer` middleware gating /api,
  /health stays public (liveness only, no state).
- `router_with_auth(state, Option<DaemonAuth>)`; `router()` = unauthenticated
  (frozen tests untouched); serve() path mints token, publishes in descriptor,
  serves authenticated router.
- `BackendDescriptor.auth_token: String` (serde default "" = legacy → treated
  stale by readers).
- TDD: RED (no-cred 401, wrong-cred 403, no state change; health public),
  freeze, implement, GREEN.
- Cmd: CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 180 cargo test -p opencode-rk-server --lib + --test daemon_auth_api
- No stubs/todo.

## Tests
- (ran below)

## Decisions
- /health public: liveness probe only, exposes schema_version/status/runtime,
  no session/tool state. Documented in module docs.
- Constant-time compare manual (no subtle dep); lengths compared via fold.
- Token 32 bytes /dev/urandom hex 64 chars; non-Unix fallback = fail-closed
  error (no Math.random-style weak mint).

## Remaining unknowns
- cli send-side (Authorization header in daemon_client/http_request) is cli
  scope — left for integrator/APP-002 lane; server rejects unauthenticated.
