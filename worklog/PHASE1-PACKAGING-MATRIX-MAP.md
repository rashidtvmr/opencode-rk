# PHASE1-PACKAGING-MATRIX-MAP - packaging truth matrix (audit only, no product edits)

Claim: session ses_f2e2a561bffec3AQW2qsR9zUk8, scratchpad worklog/PHASE1-PACKAGING-MATRIX-MAP.md.
HEAD audited: 1f4a9e6ce4fa96ea2bf8b01bb034160a9d9c60a9 (branch plan/packaging-matrix).
Scope: read-only audit. No product/test/workflow/controller edits per task. Only this worklog + ledger row written.

## 1. Source evidence (exact path:symbol)

Build/artifact core:
- Cargo.toml:1-12 workspace members (cli, server, opentui-bridge...); version 0.1.0-alpha.1; rust-version 1.85; release lto thin, strip symbols, panic abort.
- rust-toolchain.toml:1-4 channel stable minimal + clippy, rustfmt. No pinned version, no targets section.
- crates/cli/Cargo.toml:10-24 bins oc2 + opencode-rk same src/main.rs; comment line 15 artifacts ship only oc2 (see scripts/install-oc2.*).
- crates/cli/src/main.rs:62 name="oc2" (clap literal, fixed from legacy); :378-379 daemon health version env!("CARGO_PKG_VERSION"); :766-784 open_web_browser cfg windows/macos/unix; resolve_data_dir HOME/USERPROFILE fallback.
- crates/cli/src/install_commands.rs:29 BINARY_NAME oc2; :31 LEGACY_BINARY_NAME opencode-rk; :131-165 InstallPlatform 5 variants + triple() linux-x64 linux-arm64 macos-x64 macos-arm64 windows-x64 + install_platforms() len 5; :167-178 release_artifact_ok fail-closed, packaged_output_names_oc2.
- crates/opentui-bridge/build.rs:1-68 native link gate on CARGO_FEATURE_NATIVE; TARGET triple libdir native/lib/<triple>; macos static opentui_static or dylib opentui; linux static or so; windows GNU libopentui.dll.a + opentui.dll (line 47-51 comment MSVC consumes opentui.lib, GNU consumes libopentui.dll.a); panic otherwise with expected names per target_env msvc vs gnu (line 53-58). No target triple allowlist, only os/env dispatch.
- crates/opentui-bridge/native/artifacts.json:1-177 schema_version 1; repo rashidtvmr/opentui commit c01292fd0837bafd07ce458c74416b2b375a41ab tree 261e8ea4; zig 0.16.0 archive zig-aarch64-macos sha b23d70... size 52238004; 7 entries: aarch64-apple-darwin dylib 798f30..., x86_64-apple-darwin dylib a7756f..., aarch64-unknown-linux-gnu so e85a45..., x86_64-unknown-linux-gnu so 9f074a..., x86_64-pc-windows-gnu pair opentui.dll 0be2ef... + libopentui.dll.a 1dce09..., aarch64-apple-darwin static libopentui_static.a a3c80e... (88 members, 369/369 exports, linkage-static.patch 2da616...); verification 2026-09-23 builder verify-only all passed, independent 7/7 matched, runtime_execution only aarch64-apple-darwin static bridge/parity/PTY passed, other targets not run (line 169); unsupported_targets x86_64-pc-windows-msvc refused (line 171-176).
- crates/opentui-bridge/native/build_opentui.sh:1-977 pinned fail-closed builder; matrix lines 19-31 (5 Rust triples, MSVC unsupported exit 1); host gate line 753-754 Darwin/arm64 only; target table fns 138-187; macho verify 234-316 (file+lipo+nm ABI+rpath/install-name), elf 388-474 (objdump arch+NEEDED allowlist+RPATH), pe 564-634 (MZ/AMD64/DLL/PE32+ check, export table via python3 stdlib, complete DLL-to-importlib correspondence, name must be opentui.lib or libopentui.dll.a); static only aarch64-apple-darwin (line 701-703).
- crates/opentui-bridge/native/sbom.json SPDX-2.3 7 artifacts + CONTAINS relations + BUILD_TOOL_OF Zig; NOTICES 18635B, sbom 35942B (measured via wc -c).
- Vendored bytes on disk (ls -la + shasum verified this session): aarch64-apple-darwin/libopentui.dylib 6.5M 798f30... match; libopentui_static.a 7.0M a3c80e... match; x86_64-apple-darwin/libopentui.dylib 6.9M a7756f... match; aarch64-unknown-linux-gnu/libopentui.so 25.4M e85a45... match; x86_64-unknown-linux-gnu/libopentui.so 25.4M 9f074a... match; x86_64-pc-windows-gnu/opentui.dll 6.5M 0be2ef... match; libopentui.dll.a 99.1K 1dce09... match. All 7 hashes match artifacts.json.

Installers:
- scripts/install-oc2.sh:1-427 BIN oc2; detect_platform lines 73-89 only Linux/Darwin x64/arm64 (linux-x64 linux-arm64 macos-x64 macos-arm64), Windows absent exit 64; LIB_SUFFIX .so/.dylib, NATIVE_NAME libopentui<.so/.dylib>, EXPECTED_NATIVE native/lib/<platform>/NATIVE (line 185-191); checksum gate pre-write sha256sum/shasum exit 65/69 (194-202); legacy opencode adjacency refusal exit 73 (205-208); tar two-pass allowlist MAX_ARCHIVE_MEMBERS=4 (15), members exactly oc2 + native/lib/<platform>/libopentui.* + dirs, rejects absolute/..//control (215-261); probe bound MAX_ARCHIVE_MEMBER_PAYLOAD=MAX_ARCHIVE_PAYLOAD=134217728 PROBE 134217729 (18-20,263-325); staged --version/--help identity gates exit 74 require oc2 reject opencode-rk (327-364); symlink-dir refusal exit 74, atomic mv + rollback_install (366-427); uninstall removes oc2 + native lib only (154-175).
- scripts/install-oc2.ps1:1-70 Bin oc2.exe; arch gate AMD64 only exit 64 (41-42); Get-FileHash SHA256 pre-write exit 65 (45-46); opencode.exe adjacency refusal exit 73 (48-52); Expand-Archive no member/type/probe/identity bounds, copies $stage/oc2.exe only, no native DLL handling, no --version/--help gate except post-copy `& $target --version` print-only (54-70); uninstall removes oc2.exe only (22-32).
- scripts/install-opencode2.sh / .ps1: legacy opencode2 installers (BIN opencode2/opencode2.exe), no native closure, no member allowlist, no probe bounds; out of oc2 scope, retained.
- worklog/FIX-PACKAGING.md: post-copy --version/--help gates landed in install-oc2.sh; frozen packaging_identity.rs (aliases/download/stale refs) out of lane scope untouched.

CI/workflows:
- .github/workflows/ci.yml: planning job ubuntu-latest validate_repository.py + bootstrap unittest; rust matrix os [ubuntu-latest, windows-latest] (line 33), no macos runner, no target triple matrix, no --target builds, check/test/clippy/fmt with CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2. Unsigned: no signing, no provenance/attestation, no artifact upload, no release job.
- .github/workflows/completion.yml: completion-controller only (completion_plan.py --check + tests/completion unittest). No release.yml exists (release/ dir absent, tests/release/ absent).
- tasks/completion/delivery.json SHIP-001 deps APP-010 TUI-011 paths release + .github/workflows/release.yml journey reproducible Linux/macOS/Windows artifacts with signing where required; tests explicitly allow blocked signing ("use actual authorized identities or remain blocked").

Revision/version injection:
- No GIT_COMMIT/OC2_E2E_REVISION wiring in product source. Only consumer: crates/cli/tests/installed_default_entrypoint.rs:356-361 revision_receipt() reads OC2_E2E_REVISION env or option_env!("GIT_COMMIT"); test rerun_same_executable_and_revision_is_deterministic requires it (panics if missing). No build.rs injects GIT_COMMIT; no --version reports revision. Doctor version = CARGO_PKG_VERSION 0.1.0-alpha.1 only (main.rs:379,398).

Sandbox/PTY/API acceptance:
- crates/cli/src/diagnostics.rs:213-220 sandbox_backend_name landlock or unavailable; honest (no not-yet-implemented lie). Linux-only Landlock; macOS/Windows always unavailable string. No Seatbelt/AppContainer backend.
- crates/server/src/daemon_auth.rs:123-147 random_bytes unix /dev/urandom; windows returns NoRandomness error fail-closed ("OS randomness backend not implemented on Windows"); non-unix-non-windows same. Daemon bearer mint impossible on Windows native build.
- crates/cli/src/daemon_client.rs:203-211 pid_alive /proc on linux else /bin/kill -0 (macOS ok, Windows no kill binary); daemon.rs:47-51/156-164 same pattern + unix-only socket/lock paths.
- crates/cli/src/tui_entry.rs:38-55 UnixRawMode gated all(feature native, unix); :503/:527/:698 unix-only stdin helpers; native renderer path cfg(feature native); Windows native TUI raw-mode path absent.
- crates/cli/tests/installed_default_entrypoint.rs:1 #![cfg(target_os=macos)] - installed PTY/API acceptance exists ONLY for macOS (script(1) PTY, 5 tests: bare route, singleton reuse+bearer 401/200, setup without secret leak, alt-screen 1049h/l restore, revision determinism). No windows/linux installed-entrypoint test files. ui014_turn_worker_pty + native_tui_parity p07 unicode (日本語 renders) cover buffer-level unicode, not installer paths.
- Unicode paths: install-oc2.sh header claims spaces/non-ASCII quoted; APP-010 worklog records spaces T4 exit 0; no non-ASCII installer test exists in repo (grep unicode in tests only hits native_tui_parity memory names + UTF-8 export asserts).

## 2. Target-by-target truth matrix (supported: macOS arm64/x64, Linux x64/arm64, Windows GNU x64)

| Target | Rust triple | Current build evidence | Missing native closure | Archive members (installer-enforced) | Installer bounds | Unicode paths | Revision injection | Unsigned CI jobs | Installed PTY/API acceptance | External signing/notarization blockers |
|---|---|---|---|---|---|---|---|---|---|---|
| macOS arm64 | aarch64-apple-darwin | Vendored dylib 798f30 6.5M + static a a3c80e 7.0M; builder verify passed; runtime_execution passed (bridge/parity/PTY); build.rs links static preferred else dylib | None for link (static exists). Release archive layout still declares native/lib/macos-arm64/libopentui.dylib (sh EXPECTED_NATIVE uses platform slug not triple), dylib rpath/install-name verified | oc2 + native/lib/macos-arm64/libopentui.dylib (+dirs, max 4) | Full: checksum pre-write, 2-pass allowlist, stream probe 128MiB/member+total, --version+--help oc2 gates exit74, rollback, legacy refusal 73 | sh quoted; spaces proven (APP-010 T4); non-ASCII untested | None: version string only 0.1.0-alpha.1; no GIT_COMMIT/OC2_E2E_REVISION emit | ci.yml has NO macos runner; zero artifact/sign jobs | YES: installed_default_entrypoint.rs macos-only 5/5 via script(1) PTY + bearer 401/200 + alt-screen + determinism (needs OC2_E2E_REVISION) | Apple signing+notarization absent; SHIP-001 allows blocked. Gatekeeper will quarantine unsigned tarball binary+dylib. Needs Developer ID + notarytool credentials (human grant, unwired). |
| macOS x64 | x86_64-apple-darwin | Vendored dylib a7756f 6.9M; builder verify passed; runtime NOT run (line 169 other targets not run) | Same as arm64 minus static option (no x64 static artifact; build.rs falls to dylib link) | oc2 + native/lib/macos-x64/libopentui.dylib (+dirs, max 4) | Same full sh bounds as arm64 | Same: spaces proven, non-ASCII untested | Same none | Same: no macos runner at all, so x64 mac build never CI-exercised | Partial: macos-gated test compiles on x64 mac but no x64 runner evidence; cross-arch PTY unproven | Same Apple blocker as arm64. |
| Linux x64 | x86_64-unknown-linux-gnu | Vendored so 9f074a 25.4M; builder verify passed (ELF arch+NEEDED allowlist+no RPATH); runtime not run | None for link. glibc floor 2.17 pinned (zig x86_64-linux-gnu.2.17). musl/alpine not supported | oc2 + native/lib/linux-x64/libopentui.so (+dirs, max 4) | Same full sh bounds | Same | Same none | YES but unsigned: ubuntu-latest check/test/clippy/fmt only; no --target cross build, no artifact packaging, no provenance | NONE: no linux installed-entrypoint test; PTY/API acceptance only macos file | No signing required for tarball; provenance/SBOM attach unwired (sbom.json exists but no release job consumes it). |
| Linux arm64 | aarch64-unknown-linux-gnu | Vendored so e85a45 25.4M; builder verify passed; runtime not run | None for link. Same glibc 2.17 floor | oc2 + native/lib/linux-arm64/libopentui.so (+dirs, max 4) | Same full sh bounds | Same | Same none | NO arm runner: ubuntu-latest is x64 only; arm64 so never linked/tested in CI | NONE | Same as linux x64. Extra: no CI arch coverage at all. |
| Windows GNU x64 | x86_64-pc-windows-gnu | Vendored pair opentui.dll 0be2ef 6.5M + libopentui.dll.a 1dce09 99K; builder verify passed-as-pair, 431/431 export/import correspondence; runtime not run | build.rs OK (import+runtime present). daemon_auth random_bytes FAILS CLOSED on windows (no BCrypt backend) so daemon bearer mint broken on native Windows build; pid_alive /bin/kill path absent; tui raw-mode unix-only | ps1 enforces NONE: copies oc2.exe only, no DLL member check, no allowlist, no probe, no identity gate | WEAK: hash pre-write + AMD64 gate + legacy refusal only; no member/type/probe/identity/rollback bounds | PowerShell LiteralPath used; spaces/non-ASCII untested | Same none | PARTIAL: windows-latest cargo check/test runs (host triple MSVC, not GNU target); no --target=x86_64-pc-windows-gnu build, so GNU pair never linked in CI | NONE: no windows installed test; macos-only PTY file cfg'd out | Windows signing absent; SHIP-001 allows blocked. SmartScreen will flag unsigned exe+dll. Needs EV/code-sign cert + signtool (human grant, unwired). MinGW DLL + MSVC-built exe mixing also unverified. |

Never MSVC: supported Windows is x86_64-pc-windows-gnu only. artifacts.json unsupported_targets + build_opentui.sh refuse x86_64-pc-windows-msvc exit 1. build.rs msvc branch is defensive dead text for unsupported ABI, not a supported target.

## 3. Cross-cutting gaps (blockers first)

1. No release pipeline: release/ dir, tests/release/, .github/workflows/release.yml all absent. SHIP-001 paths unwired. No artifact assembly, no checksums manifest, no provenance, no upload.
2. No revision injection: nothing emits GIT_COMMIT/OC2_E2E_REVISION; determinism test requires env the packaging never sets. --version cannot identify build.
3. Windows GNU native closure broken at runtime: daemon_auth::random_bytes windows arm returns Err always (daemon_auth.rs:134-141); bearer-gated API unusable on Windows even if exe links. pid liveness + raw-mode also unix-only.
4. ps1 installer far behind sh: no native DLL install, no member/type/probe bounds, no identity gates, no rollback. Windows archive layout undefined (sh uses native/lib/<slug>/, ps1 expects bare oc2.exe).
5. CI proves no release target: no macos runner, no arm runner, no --target cross builds (gnu pair, mac x64, linux arm64 never linked in CI), unsigned everything, no artifact jobs.
6. Archive layout slug-vs-triple skew: vendored dirs use Rust triples (lib/<triple>/), installer EXPECTED_NATIVE uses platform slugs (native/lib/<platform>/). Release assembly must define mapping; currently no code does.
7. sbom.json/NOTICES/artifacts.json exist and hashes verified 7/7 this session, but no release job consumes them; Vào checksum manifest + provenance unwired.
8. Unicode install paths: sh quoting + spaces test only; non-ASCII installer proof missing on all targets; ps1 LiteralPath untested.

## 4. One-file RED/implementation lanes (each owns exactly one file; shared lib.rs pre-wired by integrator)

- LANE-PKG-001 release workflow: owns .github/workflows/release.yml only. RED: workflow-exists + matrix-has-5-targets + artifact-upload validator (python, tests/bootstrap style). Deps: none (scaffolding). NOTE: runner-proof cannot be fabricated; lane delivers definition + local act/dry-run receipt only.
- LANE-PKG-002 archive assembly script: owns scripts/build-release-archive.sh only (slug-to-triple mapping, tar with exact members, sha256 manifest). RED: shell test asserting member allowlist per platform against fixtures. Deps: LANE-PKG-001 (workflow calls it).
- LANE-PKG-003 ps1 parity: owns scripts/install-oc2.ps1 only (DLL member, hash, identity gates, rollback). RED: extend packaging_identity.rs? NO - frozen. New validator scripts/tests/packaging-ps1-check.py (new file owned by lane? one-file rule: lane owns ps1, RED lives in validation/ owned by separate lane). Split: LANE-PKG-003a owns validation/packaging_ps1_vectors.py (RED vectors), LANE-PKG-003b owns scripts/install-oc2.ps1 (GREEN).
- LANE-PKG-004 revision injection: owns crates/cli/build.rs (new file;.Style: single-file lane creates it; lib.rs untouched). Emits GIT_COMMIT + OC2_E2E_REVISION-aware version suffix; --version prints revision. RED: tests/release/revision_vectors.py? New thin test crates/cli/tests/packaging_revision.rs owned by same lane as second commit? One-file rule forces split: RED lane owns the test file, impl lane owns build.rs. Deps: none.
- LANE-PKG-005 windows daemon randomness: owns crates/server/src/daemon_auth.rs only. RED: frozen? No frozen windows test exists; new RED test must be authored in same-file #[cfg(test)] mod (allowed: owned file contains its tests). Uses BCrypt-free fallback? Real fix needs winapi/B perceived; lane proposes GetTickCount-free approach: fail-closed stays, lane instead documents blocked + wires capability report. Honest outcome may be blocked lane, not fake randomness.
- LANE-PKG-006 installed acceptance linux/windows: owns crates/cli/tests/installed_entrypoint_linux.rs (new) then ..._windows.rs; each one file. RED compiles+runs installed binary via disposable roots (mirrors macos file without script(1)). Deps: LANE-PKG-002 (needs archive layout).
- LANE-PKG-007 sbom/checksum manifest consumer: owns scripts/write-release-manifest.py only (binds artifacts.json+sbom hashes into release CHECKSUMS). Deps: LANE-PKG-002.
- Workflow dependency order: LANE-PKG-001 -> 002 -> (003a -> 003b, 004red -> 004impl, 007) -> 006 -> SHIP-001 integration. No lane fabricates runner proof; CI receipts attach only from real runs.

## 5. Verification performed this lane (audit-only; no product edits)

- shasum -a 256 crates/opentui-bridge/native/lib/*/* : 7/7 match artifacts.json (evidence lines above).
- wc -c sbom.json 35942 NOTICES 18635.
- git remote -v origin github rashidtvmr/opencode-rk; branch plan/packaging-matrix HEAD 1f4a9e6.
- No test/build commands run (audit lane; running cargo test would exceed read-only scope and 8GiB budget prudence). No frozen tests touched.

## 6. Remaining unknowns / explicit non-claims

- No runner proof fabricated: macOS/Linux-arm64/Windows-GNU installed runs unproven; stated as missing.
- Static-link release alternative (aarch64 static exists) vs dylib closure: undecided; installer currently mandates dylib layout.
- glibc 2.17 floor vs distro coverage, Apple Developer ID / Windows cert identity, checksum manifest format: operator decisions, not made here.
- install-opencode2.* legacy scripts intentionally unaudited beyond noting legacy scope.
