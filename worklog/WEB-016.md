# WEB-016 worklog — voice and dictation (GREEN-only, NOT ACCEPTED)

Claim: `voice_capture.rs` pure state machine, wired read-only
(`pub mod voice_capture`, no routes/handlers); daemon capability stays
`voice.available=false`; no mic/device/process/audio I/O; no fake
transcript. Capture/transcription/realtime/retention absent. NOT ACCEPTED.

## Source evidence

- `tasks/WEB-016.md:29-37` — boundary: capability unavailable, disabled
  labelled control, text chat usable, never requests mic, no fake
  transcript; NOT ACCEPTED.
- `crates/server/src/voice_capture.rs:1-366` — pure state machine
  (`MicGrant`/`MicState`/`VoiceConfig`/`VoiceSession`); `start`
  (:230-243), `unmute` (:255-269), `push_utterance` (:284-312) gate on
  grant+adapter; `adapter=false` → `VoiceError::NoAdapter`, zero
  transcript side effects; `live_tracks()` counter only, max 1, no FDs.
- `crates/server/src/lib.rs:34` — `pub mod voice_capture;` (wired since
  WEB-016-DECISION; corrects WEB-013-PROV-016.md:46-49 "NOT wired").
  `lib.rs:183-186` — capability `available:false`,
  reason "no native transcription or realtime audio adapter is installed".
- No-mic/process/audio proof: `grep -E
  'Command::new|std::process|tokio::process|cpal|rodio|webrtc|getUserMedia|MediaRecorder|alsa|pulse'
  crates/server/src/voice_capture.rs` → no matches.
- `docs/SECURITY.md:1,9-22,62-72`; `crates/security/src/lib.rs:151-251`
  — no mic/audio `OperationIntent`; `*` cannot bypass human-only grant.
  `crates/server/Cargo.toml:9-23` — no audio deps.
- `crates/server/tests/voice_capture.rs:1-154` — `#[path]`-included
  frozen T01..T05 (happy-path/edit, denied/nodevice/noadapter explicit,
  labels/keyboard, bounds/lifecycle, reconnect-no-dupe).
- Upstream pins: opencode `95daf90670b7c039c436c85537da5fbfe2205b41`,
  9router `17c4cc76877bd1755030a8414f8d0083f48dcccf`
  (`sources/upstream.lock.json:9-27`). No upstream audio reference
  claimed by card.
- `ralph.json`: WEB-016 `not-started`. Base rev `248f519`.

## Observed scenario (verify-only rerun, no product edit)

- `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 110 cargo test
  -p opencode-rk-server --test voice_capture`
  → 5 passed (`web016_t01`..`web016_t05`), 0 failed, 0.00s.
- Prior lane (WEB-016-DECISION): `cargo check -p opencode-rk-server` →
  0 errors, 10 pre-existing warnings.
- Product code predates tests → GREEN-only, NOT valid RED (T01 happy
  path exercises state machine with `adapter=true` fixture, not
  hardware). No RED fabricated.

## Target boundary

- Owned: this worklog only. No product/test file touched.
- Explicitly NOT owned: `voice_capture.rs`, `lib.rs` wiring,
  capability block, mic grant flow, browser voice control.

## Tests

- None frozen by this lane. Prior evidence:
  `crates/server/tests/voice_capture.rs` (154 lines, 5 tests, unit-style
  `#[test]`, no tokio).
- T01 hardware happy path untestable: no native adapter installed.

## Decisions

- Wire read-only, NOT live (per WEB-016-DECISION): routes/`getUserMedia`
  bridge/turn integration rejected — would invent broker authority and
  request mic without human grant (SECURITY.md §1/§5).
- `ponytail:` mic `OperationIntent` + human-grant flow deferred to
  integration proposal; no audio dep added (lazy/minimal rule).

## Remaining unknowns

- Real native transcription/realtime adapter; mic grant issuer flow.
- Frozen T01..T05 hardware RED/GREEN owned by future lane; verifier decides.
- Story stays NOT ACCEPTED per `tasks/WEB-016.md:37`.
