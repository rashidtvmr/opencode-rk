# TUI-002 scratchpad — safe ownership wrapper + terminal lifecycle
Session: ses_orch_TUI002. Owned scope: crates/cli/src/terminal_host.rs ONLY.

## Claim
- claimed TUI-002 via tools/completion_claims.py as ses_orch_TUI002, scratchpad worklog/TUI-002.md. TUI-001 completed (dep pickable).

## Source evidence (base 022577c + working tree)
- Base HEAD file: `crates/cli/src/terminal_host.rs` (376 lines at HEAD): RawModeGuard/RestoreFlag, EventCoalescer (MAX_QUEUED_EVENTS=512, MAX_PASTE_BYTES=32768), RendererDomain/RendererOwner (!Sync/!Send via Cell + PhantomData<*const ()>), HostError. No event_channel/admit_paste/Tokio bridge at base.
- Working-tree (uncommitted, prior lane partial): +232/-13 vs HEAD adds `admit_paste`, `event_channel`/`BridgeSender`/`BridgeReceiver` (tokio::sync::mpsc, bound clamped 1..=512, try_push never blocks, try_drain coalesces), 6 new tests (reacquire, admit_paste bound, bridge clamp/feeder/full/oversize). VERIFIED via `git diff HEAD -- crates/cli/src/terminal_host.rs`.
- `crates/opentui-sys/src/bridge.rs` (TUI-001, untracked at HEAD, present in tree): Renderer !Send+!Sync, OwnedBuffer exactly-once Drop, suspend/resume/restore_terminal_modes, ZeroSize/InvalidHandle/CreateFailed typed errors. Reference only — not owned.
- `crates/cli/src/native_host.rs` (untracked, other lane): owns own stty RawModeGuard (enter raw/-echo, drop restores sane), blocks on stdin.read, no terminal_host wiring. Out of scope — do not touch.
- Workspace tokio (root Cargo.toml:38): features include sync + macros + rt-multi-thread, so `tokio::sync::mpsc` + `#[tokio::test]` available to cli bin.
- `crates/cli/src/main.rs`: `mod terminal_host;` registered (line 46).

## Observed scenario
- terminal_host at HEAD+tree covers: bounded queue + coalesce, paste bound, single-owner acquire/release, drop-marks-restore, bridge non-blocking feeder + drain coalesce + full/oversize errors, forbid(unsafe_code).
- Remainder gaps vs TUI-002 card (5 tests):
  1. create/drop exactly-once: single reacquire covered; N-cycle create/drop + drop-idempotence not covered.
  2. bounded bursts no starvation: single-burst covered; large resize-burst end-to-end (coalescer + bridge drain) bound + non-blocking proof not covered; async recv path untested.
  3. cross-thread misuse: !Send/!Sync by construction claimed in docs, zero static assertions; sanctioned Send path (BridgeSender across threads) untested.
  4. cancel/error restore+join: drop-marks-restore covered; explicit-mark-then-drop idempotence, disarm transfer, sender-drop => recv None (join signal), empty-drain not covered.
  5. forbid(unsafe_code): present line 1; keep + verify no `unsafe` token.

## Target boundary
- Edit ONLY crates/cli/src/terminal_host.rs (impl + inline #[cfg(test)] tests). No native_host, no bridge.rs, no Cargo.toml, no controller files.
- forbid(unsafe_code) retained. No threads spawned, no IO/clock/FFI/syscalls. tokio::sync::mpsc only.
- Commit ONLY: crates/cli/src/terminal_host.rs + worklog/TUI-002.md + tasks/completion/claims.json.

## Tests (RED plan — all inside owned file mod tests)
Existing (keep frozen, never edit): resize_bursts_coalesce_to_latest, non_resize_events_break_coalescing, queue_rejects_beyond_cap_without_growth, oversize_paste_rejected_without_enqueue, renderer_double_acquire_rejected_until_released, raw_guard_marks_restore_on_drop, try_acquire_release_reacquire, admit_paste_enforces_byte_bound, bridge_constructor_clamps_bound, bridge_feeder_returns_push_outcome, bridge_queue_full_errors_without_blocking, bridge_rejects_oversize_paste_without_enqueue.
New RED (remainder):
- R1 renderer_create_drop_cycles_exactly_once (N=8 acquire/drop, held bit exact each cycle; double-acquire mid-cycle still Busy).
- R2 resize_burst_end_to_end_stays_bounded (512-tick fill + resize storm via bridge sender/drain: drain len <= bound, trailing resizes coalesced, try_push after full still QueueFull not block).
- R3 bridge_sender_is_send_across_threads (sender clone moved to thread, try_push from thread OK, drain sees it; proves sanctioned cross-thread path) + static Send asserts for TerminalEvent/BridgeSender/PushOutcome.
- R4 cancel_join_recv_none_after_senders_dropped (#[tokio::test]: drop sender => recv()==None; empty try_drain == []).
- R5 restore_idempotent_explicit_mark_then_drop + disarm (mark_restored before drop stays restored; disarmed guard does not double-mark semantics: is_armed false after disarm, flag state preserved).
RED must compile and FAIL on missing impl (R4 needs recv semantics already ok? force fail via new disarm API + N-cycle helper? Design so RED fails: disarm() and cycles helper don't exist yet).

## TDD execution (actual)
- RED attempt: appended 5 remainder tests to owned mod tests, ran
  `cargo test -p opencode-rk-cli --bin opencode-rk terminal_host` BEFORE any impl change:
  17 passed, 0 failed. Reason: prior lane's uncommitted tree already supplied
  admit_paste/event_channel/recv semantics the new tests exercise (verified via
  `git diff HEAD`). No compilable failing assertion available without inventing a
  missing API.
- Deviation (minimal, honest): no new impl API added for RED theater; instead
  hardened 2 `expect("back/last checked above")` sites to `if let Some` (same
  semantics, no panic path), kept forbid(unsafe_code), zero test edits after write.
- GREEN (frozen): 17/17 pass, 269 filtered out. Re-ran after refactor: 17/17.
- Frozen file sha256: 93c3216a144452ab25d5801bba31c97539bd39c6814804d1a283ebebefe19df1
- Log: /tmp/opencode/tui002.log (tail appended with final run + hash).
- `unsafe` token count in owned file: 1 = the `#![forbid(unsafe_code)]` line only.

## Remaining unknowns
- RESOLVED: `cargo test -p opencode-rk-cli --bin opencode-rk terminal_host` builds and runs in dirty tree (17/17). No rustc fallback needed, no blocking file.

## Decisions
- No new impl API: prior-lane tree already supplied admit_paste/event_channel/recv; RED could not fail compilably without invented API, so remainder closed via tests + panic-path hardening only.
- Replaced 2 `expect("...checked above")` with `if let Some` (same semantics, no panic path for verifier/clippy).
- No new files, no deps, no unsafe. native_host wiring untouched (other lane).
