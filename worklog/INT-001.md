# INT-001 worklog (verification pass, in-progress lane)

## Claim

Verify the INT-001 secret-free scoped integration registry slice: pure in-memory
registry in `crates/providers/src/int_registry_lane.rs`, frozen tests in
`crates/providers/tests/int_registry_lane.rs` (path-include, never touches
integrator-owned `lib.rs`).

## Source evidence

- Candidate base revision: `248f519` (`feat: add native harness feature batches`).
- Pinned OpenCode commit `95daf90670b7c039c436c85537da5fbfe2205b41`.
- Task card: `tasks/INT-001.md` (Status NOT STARTED in card; `ralph.json` lane
  `INT-001 in-progress`).
- Upstream selectors (read-only context): `packages/core/src/integration.ts`
  scoped registry ranges, `packages/server/src/handlers/integration.ts:1-104`,
  `packages/protocol/src/groups/integration.ts:8-130`,
  `packages/core/test/integration.test.ts:101-348`; gap file
  `sources/integrations-ownership-gap.json` (`typed-integration-http-handler`).
- Classification: partial upstream + deliberate resource-bounded deviation
  (explicit caps; upstream attempt map uncapped per gap file).

## Target boundary

- Product: `crates/providers/src/int_registry_lane.rs` only (additive lane file;
  `lib.rs` untouched — integrator wires exports).
- Tests: `crates/providers/tests/int_registry_lane.rs` only (includes module by
  path).
- No edits made by this worker: `git status --short` clean before and after.
- Explicitly excluded per card: INT-004 attempt machine, INT-002 connection APIs,
  credential/secret storage, env, callbacks, network, HTTP, persistence, events.

## Contract as built

- `ScopeToken(u64)` monotonic from 1; `open_scope() -> Result<ScopeToken,
  RegistryError>` (card contract line shows infallible signature but failure
  section + T04 require `Err(Scope)` on cap — implementation follows the failure
  section).
- `register(scope, desc)`: validates name/methods, `Duplicate` in same scope,
  `Overflow` on visible-name cap; last-scope-wins reads; unchanged on error.
- `close_scope(scope) -> Result<u64, RegistryError>`: strict LIFO; unknown or
  non-innermost => `Err(Scope)`, unchanged.
- `get` / `list` (sorted by name, innermost value per name). Deterministic.
- Caps frozen: `MAX_INTEGRATIONS=64`, `MAX_METHODS_PER_INTEGRATION=8`,
  `MAX_SCOPES=16`, `MAX_NAME_LEN=64`, `MAX_METHOD_ID_LEN=64`, `MAX_LABEL_LEN=128`.
- No secret/network/clock/thread/FS/env behavior by construction.

## Tests

- Frozen SHA-256 (test): `cbd315d408dcf8bf98883578ab6f4e694970faf76d016ce4b172d381d701696b`;
  git object `54a2d2daa42f6e7a6b223f0ae7a476e3331f9d3d`. No edits after hashing.
- Frozen SHA-256 (product): `4964464390860f7da32dab6a5a13e3e42cbd147008ec3af5b4a33ca333737aeb`.
- RED: NOT independently witnessed by this worker. Implementation and tests both
  pre-existed in tree at `248f519`; working tree clean so no RED reconstruction
  was possible without mutating frozen artifacts (forbidden). No RED claimed.
- GREEN (witnessed): `cargo test -p opencode-rk-providers --test
  int_registry_lane --test int_connect --test int_methods` => 5/5 INT-001 pass.
- Full provider regression: `cargo test -p opencode-rk-providers` => lib 51
  passed + all integration targets 5/5 each (incl. `integration_registry`
  INT-004, `integration_connection`, `int_methods`), 0 failed.
- `cargo check --workspace` passed (only pre-existing warnings in unrelated
  foundation code). `git diff --check` clean.
- `rustfmt --check` on lane product file reports one pre-existing diff hunk
  (line ~160); lane file NOT reformatted (frozen-artifact-adjacent; formatting
  is integrator-owned).
- `tools/validate_plan.py` reports 133 pre-existing errors (unknown prefixes,
  stale accounting) — unrelated to this slice, not caused by this worker.
- `tools/lane_gate.py` is storage-format-2-only (`crates/storage/**` lanes);
  not applicable.
- Stub scan: zero `todo!/unimplemented!/stub/placeholder/mock-` hits in lane
  product + test files.

## Decisions

- No `lib.rs`/`Cargo.toml`/schema/migration edits (integrator-owned per card).
- No reformatting of lane files (avoid touching frozen-adjacent artifacts).
- Memory at GREEN start: ~1.8 GiB available; ran one heavy command at a time
  with `CARGO_BUILD_JOBS=2`.

## Remaining unknowns (addendum 2026-09-15, owned RED witnessed)

- RED (temp stub, restored byte-identical): `get()` forced `None`;
  log `/tmp/opencode/int001_red.log` => 2 passed, 3 failed
  (T01/T02/T04 bite; T03/T05 pass: validation/safety paths unaffected).
- Restore hash `4964464390860f7da32dab6a5a13e3e42cbd147008ec3af5b4a33ca333737aeb`
  matches pre-stub; zero `TEMP STUB`; owned paths `git status` clean.
- GREEN: focused `--test int_registry_lane --test int_connect --test int_methods`
  => 5/5 each (`/tmp/opencode/int00x_green.log`); full
  `-p opencode-rk-providers` => lib 51 + 62 targets ok, 0 failed
  (`/tmp/opencode/int00x_fullreg.log`).
- rustfmt drift kept: `int_registry_lane.rs:160`
  (`self.layers[index].entries.insert` one-line upstream fmt); NOT reformatted
  per freeze. Verifier/controller acceptance external; not claimed.
- RED re-establishment requires a fresh worktree with product file removed; left
  to controller (this worker held no ownership lock to restructure).
