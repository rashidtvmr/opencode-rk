# EXT-005 worklog

## Claim

Own bounded pure manifest validation per `tasks/EXT-005.md`: bytes in, typed verdict out. No registry, host, activation, state writes.

## Source evidence

- Candidate revision: `248f519` (`git log --oneline -1`).
- Pinned OpenCode commit: `95daf90670b7c039c436c85537da5fbfe2205b41` (`sources/upstream.lock.json`, PLAN.md:36).
- `sources/disc-003-reconciliation.json` finding `opencode.extensibility` (`implementationStatus: partial`): scoped plugin lifecycle exists; reload/watch + deferred external activation remain design work (task card lines 23-31).
- Product boundary drift found: `crates/tools/src/plugin_manifest.rs:1-55` owns different contract (`PluginManifest{name,version,permissions}`, `X.Y.Z` version, `MAX_PERMISSIONS=32`). EXT-005 contract is `Manifest{name,contract_version,capabilities}` + `validate_manifest_bytes` (task lines 70-86). `plugin_manifest.rs` untouched.
- Isolated lane files: `crates/tools/src/ext_manifest_lane.rs:1-142`, `crates/tools/tests/ext_manifest_lane.rs:1-202`. Test uses `#[path]` include so worker never touches shared `lib.rs` (test lines 8-9, module header lines 7-11). No `Cargo.toml` change (workspace deps only: `serde_json`, `thiserror`, `tempfile` dev).

## Target boundary

- Tests: `crates/tools/tests/ext_manifest_lane.rs` only (file already committed at `248f519`).
- Product: `crates/tools/src/ext_manifest_lane.rs` only (committed).
- No edit made by this lane: tree clean (`git status --short` empty). `lib.rs` wiring stays integrator-owned (`tasks/EXT-005.md:84-87` additive fragment rule).

## Tests

- Frozen SHA-256 (tests): `d53466c981768434cbd1957558854d4c9c3a4b0e66362df4d7d208778e2e92c0`.
- Frozen impl SHA-256: `e54e9647616f93c14be9b23ef62c54fd89b21aa4e1749d758d4b8d3a9bbb7994`.
- RED: not re-established by this lane. Prior commit `248f519` shipped tests+impl together; no RED receipt (frozen hash at RED, behavioral 0/5) exists in worklog. Gap recorded below. Cleanup would violate no-stub/frozen-test rules.
- GREEN (this lane, bounded): `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 120 cargo test -p opencode-rk-tools --test ext_manifest_lane` => 5 passed / 0 failed (`ext005_t01..t05`).
- No-stub check: `grep todo!/unimplemented!/stub/placeholder` over `ext_manifest_lane.rs` => clean. No `std::fs/net/process/thread`, no `tokio::`, no statics/env.
- `git diff --check` => clean. `python3 tools/validate_plan.py` tail shows only pre-existing stale `FEATURES.md` UI-014..018 statuses, unrelated to EXT-005.

## Decisions

- Name charset `[A-Za-z0-9][A-Za-z0-9._-]*`, 1..=128; caps same charset 1..=64, 0..=16, dup rejected; `contract_version==1` only; checks in order length->UTF8->JSON->fields; unknown fields ignored; missing `capabilities` => `[]`. All per task contract.
- Retained `Manifest` bounded (~1152 B max). Zero retained state. Debug derives on `Manifest` hold labels only; `ManifestError` carries no payloads.

## Remaining unknowns

- RED receipt missing: verifier needs compiling RED (0/5 on test IDs EXT-005-T01..T05) + frozen hash before accepting. Controller must decide: accept prior GREEN-only evidence, or authorize fresh RED cycle (would require new isolated test path since current file frozen-in-effect).
- `crates/ext` crate named in task (`crates/ext/src/manifest.rs`, `cargo test -p opencode-rk-ext`) does not exist; lane lives in `opencode-rk-tools` as `ext_manifest_lane`. Canonicalization/integration decision pending (integrator wires `pub mod` + renames or keeps lane path).
- Verifier/controller acceptance external, not claimed.

## DEDUP lease (this session)
- `plugin_manifest.rs` vs `ext_manifest_lane.rs` investigated: NOT duplicates (name/version/permissions vs EXT-005 JSON-bytes name/contract_version/capabilities). Left alone; no change. See `worklog/EXT-DEDUP.md`.

## Re-verify 2026-09-16 (DB/DISC/EXT lane)
- Lane impl: `crates/tools/src/ext_manifest_lane.rs` (bytes in, typed verdict; NOT wired into lib.rs, `#[path]` test only). Tests: `crates/tools/tests/ext_manifest_lane.rs` (5 fns ext005_t01..t05).
- GREEN: bounded `--test ext_manifest_lane` => 5 passed / 0 failed.
- RED probes (isolated /tmp/opencode/redprobe/p_manifest copies): (a) skip byte-size cap => ext005_t04 FAILED (InvalidJson vs TooLarge), 0/1 filtered; (b) neutralized version-guard in `validate_manifest` alone still GREEN (bytes path enforces version earlier — confirms bytes path is load-bearing, struct path secondary); (c) accept-any-version in bytes path still GREEN (t03 only asserts 999 rejected via bytes path... note: probe accepted version then final `validate_manifest` re-rejected; confirms defense-in-depth, single-guard probes insufficient); (d) skip final `validate_manifest` call still GREEN (bytes-path checks already cover t03/t04 cases).
- Verdict: byte-layer guards load-bearing and RED-sensitive (probe a). Field-guard redundancy means single-point probes need multi-guard neutralization for full RED; recorded, no test edits.
- Stub/IO grep clean. No edits made (worklog only).

## zF-EXT005 verify-only pass 2026-09-16 (rev 248f519)
- Verdict: bytes pipeline ALREADY PRESENT. Verify-only, no impl edits.
- Impl pre/post SHA256 (src): e54e9647616f93c14be9b23ef62c54fd89b21aa4e1749d758d4b8d3a9bbb7994 (identical, cmp IDENTICAL after RED probe restore).
- Test SHA256 (post, fmt-only import reorder in worktree): 92701b3f327e804647e5ba54bc032be44ac9d2dbb922c0787379c34597209ebd. `git diff` test file: import-wrap only, 2+/3-; src diff: empty.
- Contract check: `validate_manifest_bytes`, `MAX_MANIFEST_BYTES=16384`, `MAX_NAME_LEN=128`, `MAX_CAPABILITIES=16`, `MAX_CAP_LEN=64`, `SUPPORTED_CONTRACT_VERSION=1`, order length->UTF8->JSON->fields, unknown-fields-ignored, missing-caps->`[]` all present `src/ext_manifest_lane.rs:16-142`. `validate_manifest` struct entry present. No `todo!/unimplemented!` (src 0, test 0).
- GREEN: serial JOBS=1 THREADS=1 `cargo test -p opencode-rk-tools --test ext_manifest_lane -- --test-threads=1` => 5/5 (`ext005_t01..t05`), exit 0.
- RED probe: size-cap neutralized (`TooLarge` guard -> `let _ = MAX_MANIFEST_BYTES`) => `ext005_t04_byte_layer_failures` FAILED, 4/1; restored byte-identical (`cmp IDENTICAL`), re-ran GREEN 5/5.
- Pre-existing-parts note: test-file import reorder was already dirty in worktree before this lane (fmt churn), untouched here.
- Never touched: frozen tests content beyond worktree state, lib.rs, ralph.json, twins.
- Log: /tmp/opencode/zF-ext005.log.
- Gap-closed: y (EXT1-VERIFY4 bytes-pipeline gap closed by ext_manifest_lane; `plugin_manifest.rs` struct-shape lane is separate contract, untouched).

## bB-EXT005 2026-09-16 (rev 248f519) — gap-closure verdict
- Choice: NO port into canonical plugin_manifest.rs. Porting bytes pipeline (Manifest/ManifestError/validate_manifest_bytes/consts) into plugin_manifest.rs would collide: duplicate type names ManifestError+validate_manifest across modules (ambiguous imports), version is semver-string(X.Y.Z) vs u32 contract, MAX_PERMISSIONS=32 vs MAX_CAPABILITIES=16. Additive-merge infeasible without breaking existing plugin_manifest API/tests (manifest_t01..t05 import validate_manifest+ManifestError via crate path).
- Chosen minimal additive gap-close: wire `pub mod ext_manifest_lane;` into crates/tools/src/lib.rs (alpha slot) + fix pre-existing misordered tool_quota/tool_sandbox tail. canonical plugin_manifest.rs untouched (hash c8c6682c preserved). ext_manifest_lane.rs + frozen test untouched.
- Contract re-check (ext_manifest_lane.rs:16-142): validate_manifest_bytes, MAX_MANIFEST_BYTES=16384, MAX_NAME_LEN=128, MAX_CAPABILITIES=16, MAX_CAP_LEN=64, SUPPORTED_CONTRACT_VERSION=1, order length->UTF8->JSON->fields, unknown-ignored, missing-caps->[], struct validate_manifest. No todo!/unimplemented! (stub-rc=1 clean). IO grep per prior lane: no fs/net/process/thread/tokio/statics/env.
- GREEN serial JOBS=1 THREADS=1: `cargo test -p opencode-rk-tools --test ext_manifest_lane -- --test-threads=1` => 5/5 (ext005_t01..t05), exit 0. Log /tmp/opencode/bB-ext005.log (+bB-green.log).
- RED probe (behavior-stub): size-cap neutralized (TooLarge guard -> let _ = MAX) => ext005_t04 FAILED (tests/ext_manifest_lane.rs:133 TooLarge assert), 0p/1f filtered; restored byte-identical via cp back + cmp IDENTICAL; re-ran GREEN 5/5.
- cargo check -p opencode-rk-tools exit 0; 3 pre-existing warnings only (unused Duration hook_bus_v2, unused_mut/dead_code shell_tool/rtk_core). Log /tmp/opencode/bB-check.log.
- sha256 pre: lib ace227ad / plugin_manifest c8c6682c / ext_lane e54e9647 / test-ext 92701b3f / test-plugin af8baa1b. post: lib 605af3e9 / others identical. diff stat: lib.rs only +2/-1 (1 mod-line wire + fmt tail). Frozen tests: zero diff.
- gap-closed: y (bytes pipeline present + lib.rs-wired + RED-sensitive + GREEN; canonical struct-shape lane kept separate, no breaking). Twin-note: plugin_manifest vs ext_manifest_lane NOT duplicates (EXT-TWINS-DISPOSITION precedent); both keep.
