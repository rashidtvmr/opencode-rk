# WEB-015 worklog — projects, workspace context, memory disclosure (GREEN-only, NOT ACCEPTED)

Claim: daemon reads canonical `data/catalog.db` format-2 registry, exposes
bounded workspace metadata read-only; session membership + memory/context
authority explicitly unavailable (`session_scope_available=false`,
`memory_available=false`); absent catalog → explicit unavailable, no invented
projects. Project-scoped sessions/files/memory/T05 NOT satisfied.

## Source evidence

- `tasks/WEB-015.md:9-18` — boundary: registry metadata only; no
  workspace→session/file membership, no memory/context config; browser
  must not filter/attribute chats per workspace.
- `crates/server/src/lib.rs:196-235` (`list_workspaces`): catalog absent →
  `workspaces:[]` + unavailable reason; present → id/label/project_root/
  status/created_at_us only, plus `session_scope_available:false`,
  `memory_available:false`.
- `crates/sessions/src/lib.rs:362-390` (`with_workspace_catalog_path`,
  `list_workspaces`): `CatalogV2::open_existing` + `list_workspaces(limit)`,
  `None` when unconfigured.
- `crates/server/tests/web_workspace_api.rs:12-82` — T01 canonical-catalog
  read, T02 missing-catalog explicit unavailable.
- Upstream pins: opencode `95daf90670b7c039c436c85537da5fbfe2205b41`,
  9router `17c4cc76877bd1755030a8414f8d0083f48dcccf`
  (`sources/upstream.lock.json:9-27`). No upstream workspace reference
  claimed by card.
- `ralph.json`: WEB-015 `not-started`. Base rev `248f519`.

## Observed scenario (verify-only rerun, no product edit)

- `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 110 cargo test
  -p opencode-rk-server --test web_workspace_api`
  → 2 passed (`web_015_t01_workspace_registry_is_read_from_canonical_catalog`,
  `web_015_t02_missing_catalog_is_explicitly_unavailable`), 0 failed, 0.03s.
- Product code predates lane → GREEN-only, NOT valid RED. No RED
  fabricated.

## Target boundary

- Owned: this worklog only. No product/test file touched.
- Explicitly NOT owned: workspace routes, catalog v2, session/file
  membership, memory/context disclosure, browser selector.

## Tests

- None frozen by this lane. Prior evidence:
  `crates/server/tests/web_workspace_api.rs` (85 lines, 2 tests).
- T01 scoped-load/context-breakdown, T02 cross-workspace leak, T03 a11y,
  T04 bounds, T05 persistence: no frozen suite (no membership/memory
  authority exists to test against).

## Decisions

- `ponytail:` registry-only read; browser shows boundary explicitly,
  never filters chats by workspace until membership authority exists.
- Unavailable files/paths/cross-workspace access must fail explicitly
  (card T02) — no daemon path for this yet.

## Remaining unknowns

- Authoritative workspace→session/file membership + persisted
  memory/context config schema/service (integration proposal needed).
- Frozen T01..T05 RED/GREEN owned by future lane; verifier decides.
- Story stays NOT ACCEPTED per `tasks/WEB-015.md:15-18`.

## Additive lane (2026-09-16, rev 248f519) — missing-catalog reason probes
- Owned NEW file only: `crates/server/tests/web_workspace_reason.rs` (3 tests).
- Read-only: `crates/server/src/lib.rs:196-236` (`list_workspaces`), frozen `crates/server/tests/web_workspace_api.rs` (never edited).
- Probes: missing catalog carries non-empty reason containing "not configured" + scope/memory flags false; present catalog carries non-empty boundary reason + scope/memory false; unknown `?scope=` projects no membership (`sessions`/`memory` keys absent, stays unavailable/empty).
- GREEN (serial JOBS=1 THREADS=1 `--test-threads=1`): 3 passed, 0 failed.
- Sensitivity (mutation in /tmp copy only, restored byte-identical): flip `available==false`→`true` in owned test → FAIL `left: Bool(false) right: true`, log `/tmp/opencode/zE-ws-mut.log`. Owned file restored + GREEN rerun 3/3.
- Bounds: no impl/frozen/lib.rs/ralph.json edits. Same dirty-tree note as WEB-013 (unrelated `share_merge.rs:200` failure, untouched).

## Additive lane (2026-09-16, rev 248f519) — full T01-T05 suite
- Owned NEW file only: `crates/server/tests/web_workspace_full.rs` (5 tests). Read-only: `crates/server/src/lib.rs:196-237` (`list_workspaces`), frozen `crates/server/tests/web_workspace_api.rs` (never edited), sibling `web_workspace_reason.rs` (not edited).
- T01 catalog read (2 seeded workspaces, label/project_root/status passthrough + scope/memory false); T02 missing catalog explicit unavailable + "not configured" reason; T03 `?scope=no-such-scope` projects no membership (no sessions/memory keys, registry unfiltered); T04 workspace objects expose registry fields only (`created_at_us/id/label/project_root/status`); T05 repeated seeded reads deterministic (`first == second`).
- GREEN (serial JOBS=1 THREADS=1 `--test-threads=1`): 5 passed, 0 failed.
- Sensitivity (/tmp copy only, restored byte-identical sha256 `2a1cbfe0dbe1ee2fc59b1ecd97844313a26ea78df26f1250755756f2f53743ac`): flip T01 `available true→false` → FAIL `left: Bool(true) right: false`, log `/tmp/opencode/bD-ws-full-mut.log`. Owned file restored + GREEN rerun 5/5.
- Bounds: no impl/frozen/lib.rs/ralph.json edits. Dirty tree pre-existing (unrelated lanes, `web_workspace_api.rs` untouched by this lane).
