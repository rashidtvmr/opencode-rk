# WEB-017 worklog — editable writing/code artifacts (GREEN-only, NOT ACCEPTED)

Claim: bounded daemon artifact model (immutable versions, source row never
rewritten) + list/get/save HTTP APIs exist; run/apply explicitly disabled
(no safe execution/approval bridge); browser regression authored but
runner-unverified (no vitest/tsc). NOT ACCEPTED.

## Source evidence

- `tasks/WEB-017.md:30-42` — boundary: dedicated model for ordinary +
  format-2 branch session IDs; real assistant source message required;
  source row never rewritten; immutable versions; list/get/save APIs;
  32-step undo/redo; run/apply disabled.
- `crates/server/src/web_artifact.rs:13-21` — ON-DISK bounds differ from
  card text: `MAX_ARTIFACT_BYTES=256KiB` (card says 64 KiB/version),
  `MAX_VERSIONS=32` (card says 16), `MAX_PREVIEW_BYTES=64KiB`,
  `MAX_EXEC_OUTPUT_BYTES=16KiB`, `MAX_TITLE_LEN=256`. Card's "1 MiB
  retained, 32 artifacts/session" has no matching const in this file —
  DISCREPANCY recorded, not resolved (integrator-owned).
- `crates/server/src/lib.rs:83-116` — artifact routes (list/create/get/
  append-version); `lib.rs:187-192` capability: `editing_available:true`,
  `run_available:false`, `apply_available:false`.
- `crates/server/tests/web_artifact_api.rs:30-185` — T01 versions survive
  reload without mutating source; T02 source/content bounds fail closed.
- Upstream pins: opencode `95daf90670b7c039c436c85537da5fbfe2205b41`,
  9router `17c4cc76877bd1755030a8414f8d0083f48dcccf`
  (`sources/upstream.lock.json:9-27`). No upstream artifact reference
  claimed by card.
- `ralph.json`: WEB-017 `not-started`. Base rev `248f519`.

## Observed scenario (verify-only rerun, no product edit)

- `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 110 cargo test
  -p opencode-rk-server --test web_artifact_api`
  → 2 passed (`web_017_t01_artifact_versions_survive_reload_without_mutating_source_message`,
  `web_017_t02_artifact_source_and_content_bounds_fail_closed`),
  0 failed, 0.02s.
- Product code predates lane → GREEN-only, NOT valid RED. No RED
  fabricated.

## Target boundary

- Owned: this worklog only. No product/test file touched.
- Explicitly NOT owned: artifact model/routes, sessions artifact store,
  execution/approval bridge, browser editor.

## Tests

- None frozen by this lane. Prior evidence:
  `crates/server/tests/web_artifact_api.rs` (198 lines, 2 tests).
- T03 a11y/T04 bounds-cancel/T05 version-fidelity browser halves +
  run/apply safety: no frozen suite (no executor; no vitest/tsc).

## Decisions

- `ponytail:` run/apply stay disabled until safe native
  execution-owned tool/approval bridge exists; then per-action
  authorization enforced by caller.
- Card-vs-code bound mismatch (64KiB/16 vs 256KiB/32) left to
  integrator/verifier — lane must not edit frozen tests or card.

## Remaining unknowns

- Safe executor + approval bridge; per-session artifact cap +
  retained-bytes accounting location; browser editor/undo/a11y tests.
- Frozen T01..T05 RED/GREEN owned by future lane; verifier decides.
- Story stays NOT ACCEPTED per `tasks/WEB-017.md:39-42`.
