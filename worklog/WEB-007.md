# WEB-007 worklog — transcript geometry + per-message action bars

Claim: geometry/capability model implemented; frozen suite GREEN 5/5.
Source evidence:
- Upstream pins: opencode `95daf90670b7c039c436c85537da5fbfe2205b41`, 9router `17c4cc76877bd1755030a8414f8d0083f48dcccf` (`sources/upstream.lock.json`). Upstream tree not re-audited in this lane; behavior classified per task card (existing web transcript + message API) as new native model with deliberate parity contract in card.
- Impl: `crates/server/src/transcript_lane.rs` — `geometry_for` (:37), `row_actions` (:74), `popover_items` (:183), `is_branch_boundary` (:195), `normalize_persisted_role` (:202), `reading_order` (:215), `PopoverState` (:235), `trunc_preview` (:261), `animation_ms` (:277). Bounds: `MAX_PREVIEW_BYTES` 4KiB, `MAX_ACTIONS` 8.
- Web DOM: `web/src/App.tsx` (:166 Fork trigger, :171 "Branch in new chat" item) renders the contract; this lane owns only the server model, not the DOM.
Observed scenario: RED proven by mutating `full_width:true→false`; `web007_t01` failed `assertion failed: user.full_width`, 4/5 passed. Reverted; suite GREEN.
Target boundary: `crates/server/src/transcript_lane.rs` (owned, `#[path]`-included by test, NOT wired into `lib.rs` — integrator assembles).
Tests: `crates/server/tests/transcript_lane.rs` SHA-256 `f46f26de…323` (computed this rev). Frozen — not edited.
Decisions: no new deps; pure sync, no spawn/network/I-O; unavailable actions disabled with reason, never fake-ok.
Remaining unknowns: browser DOM reflow/a11y (200%/400%, focus visibility) needs runnable vitest/tsc — browser acceptance blocked per card pattern; verifier decides.
