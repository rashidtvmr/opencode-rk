# EXT-012 worklog

## Claim

Own the inert UI-declaration partition only: bounded record of
(scope, surface, label, detail) declarations, registration-order list,
pure `describe`, always-deferred `request_render` (gate open or closed).
No renderer, host, import, or client work.

## Source evidence

- Candidate base revision: `248f519d53ed29cbf7fef7ceb2b5010fb1a4224b`.
- Pinned OpenCode commit: `95daf90670b7c039c436c85537da5fbfe2205b41`.
- `tasks/EXT-012.md`: REQ-005, five test obligations, caps
  (`MAX_UI_DECLS=64`, label 64, detail 256), exclusions (EXT-005/001/006/008,
  UI-012 renderer, JS import, persistence).
- Product boundary: `crates/tools/src/plugin_ui_boundary.rs` (wired in
  `lib.rs`), canonical tests `crates/tools/tests/plugin_ui_boundary.rs`.
  Fallback `ext_ui_boundary_lane` pair byte-identical.
- Classification: partial/planned upstream + safer deviation (gate closed
  by default, declarations inert).

## Target boundary

- Product implementation: `crates/tools/src/plugin_ui_boundary.rs` only.
- Independent tests: `crates/tools/tests/plugin_ui_boundary.rs` only.
- Status/evidence: this worklog and `tasks/EXT-012.md`.

## Tests

- Canonical: `cargo test -p opencode-rk-tools --test plugin_ui_boundary`
  => 5 passed / 0 failed (`/tmp/opencode/ext_canonical.log`).
- Lane mirror: `--test ext_ui_boundary_lane` => 5/5 (`/tmp/opencode/ext012.log`,
  `/tmp/opencode/ext_lane_green.log`).
- Frozen SHA-256 (tests, never edited):
  `a343ebc2c7f9a90f07d21daf656395de5b24d947999ddeac162eaff15a8ca9a0`.
- No RED re-run: implementation predates lease.

## Decisions

- No product-code change: all five obligations already GREEN (happy path,
  revoke, validation atomicity, deferred-while-open, no-host safety with
  zero render invocations).
- Runtime-optional honored: feature-off path constructs nothing.

## Re-verification lease (2026-09-15)

- Base `248f519`; no product-code edit. Canonical 5/5 + lane mirror 5/5 in
  combined 40/40 run, exit 0 (`/tmp/opencode/ext009_012_green.log`); lib 49/49
  (`/tmp/opencode/tools_lib.log`).
- Hashes disk vs frozen HEAD: tests disk `db18b1e3...` / frozen `a343ebc2...`;
  lane tests disk `259fd956...` / HEAD `5cfa4f48...`; src `90883e97...` vs
  lane `de389584...` (one-line header delta only). Fmt-only drift, GREEN.
  Tests untouched; HEAD values authoritative.
- Dedupe refused (integrator-owned; `#[path]` isolation; no `lane2` rule
  found): see EXT-011 worklog. No shared-file edits.

## Remaining unknowns

- None for this slice. Presentation follow-up needs UI-012 renderer
  authority; explicitly out of scope.

## Re-verify 2026-09-16 (DB/DISC/EXT lane)
- Impl: `crates/tools/src/plugin_ui_boundary.rs` (191L), wired in lib.rs. Tests: `crates/tools/tests/plugin_ui_boundary.rs` (`#[path]`, 5 fns ext012_t01..t05).
- GREEN: bounded `--test plugin_ui_boundary` => 5 passed / 0 failed.
- RED probe (isolated copy): neutralizing dup-guard `return Err(UiError::Duplicate)` => ext012_t03 FAILED, 4/1. Sensitive.
- Stub/IO grep clean. No edits made (worklog only).
