# WEB-010 worklog — structured WYSIWYG chat composer

Claim: composer lowering/sanitize/draft/queue model implemented; frozen suite GREEN 5/5.
Source evidence:
- Upstream pins unchanged. Dependency: model/effort controls + session input state. Card boundary: slash/mentions/queue-steer chips disabled at HTTP boundary until native contracts exist; browser vitest/tsc acceptance blocked.
- Impl: `crates/server/src/chat_composer.rs` — `lower_composer_doc` (:274, deterministic, node-count→capability→emptiness→selector→byte order), `sanitize_doc` (:319), `accessible_label` (:349), `key_command` (:397, Tab always moves focus, Esc closes), `encode_draft`/`decode_draft` (:430-495, unknown kinds drop+count, cap checked pre-parse), `doc_from_legacy_text` (:498), `DraftQueue` (:149, cap 16, owner-steer in place, single live flag). Bounds: text 64KiB, nodes 256, draft 64KiB, queue 16. Errors carry kinds/sizes only, never input text.
Target boundary: `crates/server/src/chat_composer.rs` (owned, `#[path]`-included, not in `lib.rs`).
Tests: `crates/server/tests/chat_composer.rs` SHA-256 `0ef115dd…02275` (this rev). Frozen — not edited.
Decisions: no new deps; `Html`/`Embed` never lower; inactive chips error, never send as active.
Remaining unknowns: HTTP turn contract for slash/mentions/queue + browser test execution; verifier decides.
