# PHASE1-WAVE1-SOURCE-EVIDENCE

## Claim

- Task: `PHASE1-WAVE1-SOURCE-EVIDENCE`
- Task type: research
- Session: `ses_f2de005fbffe5z0kcvYj0FlGlm`
- Route: `9router-xk-gpt56-luna`
- Source revision: `5d666830bc9e5b716b2ea1d239d9d0ccbe6e067b`
- Branch: `plan/phase1-wave1-dispatch`
- Scope: source evidence only; no Cargo execution beyond `cargo metadata --no-deps`.
- Convergence: `python3 tools/convergence_gate.py` recorded `CONVERGENCE BLOCKED`, `total=90`; no reconciliation performed.

## Source-evidence matrix

| Candidate | Verdict | Exact owned file for future lane | Task type | Package/target | Exact command | Expected state | Public API evidence | Dependencies/conflicts | Frozen status/hash | Platform | Resource weight |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Tools shell broker RED | PROVEN for a focused public-API RED; not proof of the live server caller | `crates/tools/tests/phase1_shell_broker.rs` (new) | RED authoring | `opencode-rk-tools` / `phase1_shell_broker` | `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 timeout 180 cargo test -p opencode-rk-tools --test phase1_shell_broker -- --test-threads=1` | Compiling RED must fail only for the missing broker/side-effect assertion; current `ShellTool` broker path should provide the GREEN seam | `crates/tools/src/lib.rs:57-58` publicly exports `shell_bounds` and `shell_tool`; `shell_tool.rs:63-71` exposes `ShellConfig`; `:84-97` exposes `ShellTool` fields including `authz`; `:99-149` exposes `new`, builders, `broker`; `:163-175` exposes `execute`; `:189-210` authorizes exact `OperationIntent::Process` before spawn. `PermissionBroker`, `SecurityPolicy`, `PermissionSet`, `PermissionRule`, `RuleEffect` are public in `crates/security/src/lib.rs:80-225`. Existing `crates/tools/tests/shell_tool_process_tree.rs:1-135` is the integration-test pattern. `ToolExecutor::execute` also reaches `bash`/`shell` at `crates/tools/src/executor.rs:108-147,240-287`, but that method has no broker argument and is not the safe RED seam.` | Tools package has `opencode-rk-security` path dependency; no Cargo edit needed. Unix process fixtures only. Conflict: direct `ToolExecutor::execute` shell path is unbrokered and needs a server/integration owner, not this tools-only file. | Existing process-tree test hash `f7f2e3a49e4125943c17f366911dbaa4ff18b0e69424bcb1584c76ab39809a27`; no frozen hash for proposed file. | Unix for `/bin` process fixtures; `ShellTool` API itself is cross-platform. | Medium: one child tree, bounded output, one Tokio test thread. |
| OS isolation RED | BLOCKED | `crates/security/tests/phase1_os_isolation.rs` (new, do not dispatch until backend contract is accepted) | RED authoring | `opencode-rk-security` / `phase1_os_isolation` | `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 timeout 180 cargo test -p opencode-rk-security --test phase1_os_isolation -- --test-threads=1` | A meaningful RED must exercise a real target backend and assert inherited-capability closure plus absent denied side effects. Current code can only prove fail-closed refusal and policy confinement. | `crates/security/src/lib.rs:13-15,23` exports `os_backend`, `sandbox`, `tool_authorize`; `os_backend.rs:112-181` exposes `platform_support`, `require_supported`, `engage`; `:184-273` exposes `FsGrant`, `check`, `run_confined`; `:275-318` exposes `restricted_spawn`, `kill_on_violation`, `count_open_fds`; `sandbox.rs:41-123` exposes policy-only `SandboxPolicy`, `SandboxCheck`, `is_allowed`. Existing `crates/security/tests/sandbox_enforcement.rs:1-142` is an integration pattern but uses `#[path]` internals, so it does not validate the public crate boundary. | `crates/security/Cargo.toml:9-13` has no syscall/Landlock dependency and `lib.rs:2` forbids unsafe code. `os_backend.rs:1-37,168-181` explicitly says `engage` always returns `Blocked`; `platform_matrix.rs:151-161` always returns `UnsupportedSandbox`. `docs/SECURITY.md:36-56` forbids advertising policy/regex as OS isolation. A new public test can compile, but cannot truthfully RED/GREEN a real isolation backend without an approved backend owner/dependency and platform runner. | Existing `sandbox_enforcement.rs` hash `58bffede4ec2d403fd7e2674f18822e59850d2e92989c6f8dfb1fc7664f21c1e`; no frozen hash for proposed file. | Current host macOS; real Landlock only Linux. Windows/macOS backends are absent. | High if real backend/child isolation; low for current honest fail-closed probe, which is insufficient. |
| Receipt binding and archive owner | BLOCKED | `none - no checked-in archive producer`; receipt owner is existing `crates/cli/build.rs` | integration | `opencode-rk-cli` / `build-script-build`; release packaging target is absent | `OC2_BUILD_REVISION=5d666830bc9e5b716b2ea1d239d9d0ccbe6e067b CARGO_BUILD_JOBS=1 timeout 180 cargo build -p opencode-rk-cli --release --bin oc2 --features native` | Build script should receive the truthful checked-out revision before release compilation. A future packaging lane must then package the compiled `oc2` plus the target native closure; no existing archive command is present. | `crates/cli/build.rs:23-48` emits `cargo:rerun-if-env-changed=OC2_BUILD_REVISION`, validates exact 40 lowercase hex at `:50-60`, and emits `cargo:rustc-env=GIT_COMMIT` at `:45-47`; `crates/cli/tests/installed_default_entrypoint.rs:356-360` reads `OC2_E2E_REVISION` then compile-time `GIT_COMMIT`. `crates/opentui-bridge/native/build_opentui.sh:34-40,91-123` builds/verifies native artifacts only. `scripts/install-oc2.sh:2-6,194-202,210-294` validates and extracts a supplied archive; it contains no archive-create/package operation. `scripts/install-opencode2.sh:2-3,89` is likewise extraction only. | No release workflow/archive producer exists in tracked files; `.github/workflows/ci.yml` is generic CI per `worklog/APP-012.md:130-135`, with no release workflow. `scripts/install-oc2.sh` is installer/extractor, therefore wrong implementation owner. Packaging owner needs an explicit integration proposal/new path and shared release authority. | `crates/cli/build.rs` current hash not frozen here; installed-entrypoint frozen hash `fec2fdb94c74df2493eb8eb2f2098732d813096f0e01928f00a8cb8c621bf317`. No archive hash/receipt exists. | Release target must be selected per artifact matrix; current checked-in receipt path is target-neutral. | High: release build, native closure, archive, checksum, clean extraction. |
| Frozen installed-entrypoint verification | PROVEN as macOS verification; acceptance remains bounded by frozen parent gaps | `crates/cli/tests/installed_default_entrypoint.rs` (read-only verification target) | verification | `opencode-rk-cli` / `installed_default_entrypoint`, `--features native` | `env OC2_BUILD_REVISION=5d666830bc9e5b716b2ea1d239d9d0ccbe6e067b CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 timeout 180 cargo test -p opencode-rk-cli --features native --test installed_default_entrypoint -- --test-threads=1` | macOS cfg runs 5 tests, expected `5 passed, 0 failed` on a host with the checked-in native artifact. Receipt is compile-time `GIT_COMMIT`, or runtime `OC2_E2E_REVISION` override; explicit build input must be 40 lowercase hex. | Test is `#![cfg(target_os = "macos")]` at `:1`; disposable HOME/data, PTY, loopback and bounded capture at `:32-67,126-218`; receipt lookup at `:356-360`; five journeys at `:382-571`. Package/bin names are `crates/cli/Cargo.toml:9-18`; `native` feature is `:39-40`. Build receipt contract is `crates/cli/build.rs:23-60`. | Requires macOS PTY `/usr/bin/script`, aarch64 native bridge artifact, APP-001 startup wiring, APP-010 receipt wiring. Frozen test is immutable. It does not prove packaged archive installation, restart persistence, protected-path denial, approval/resume, Linux, or Windows. | Frozen test SHA `fec2fdb94c74df2493eb8eb2f2098732d813096f0e01928f00a8cb8c621bf317`; APP-010 integration claim records 5/5 GREEN with explicit receipt. | macOS only; current host is macOS arm64. | High: native build/link, PTY, daemon lifecycle; serial jobs/threads, 180s bound. |
| Frozen APP-012 journey verification | PROVEN for the frozen bounded read journey; not the full installed parent journey | `crates/server/tests/app012_tool_journey_red.rs` (read-only verification target) | verification | `opencode-rk-server` / `app012_tool_journey_red` | `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 timeout 180 cargo test -p opencode-rk-server --test app012_tool_journey_red -- --test-threads=1` | Current integrated read slice expected `1 passed, 0 failed`: loopback provider requests `read`, bounded output is `0123456789ab`, round two receives it, durable rows exist. Parent remains open for restart/rebuild, protected-path denial, approval/resume, and installed packaging. | `crates/server/tests/app012_tool_journey_red.rs:19-33` imports public `router_with_auth`, `RuntimeWiring`, `AppState`, `Storage`, `SessionService`, `ToolRegistry`; `:169-215` creates the deterministic provider/app; `:231-249` uses authenticated session API; `:263-296` uses real HTTP turn stream. `crates/tools/src/executor.rs:136-238` exposes brokered `execute_authorized` read path. | Depends on server runtime wiring, storage fixture, loopback provider; no host DB. Conflict: `APP-012` parent ledger row is `in-progress`; read child claims are off-plan/completed and do not close parent. | Frozen SHA `945236c4c6a565fefb3853afa6930c438ab38f250c074ef650b5cd252d3c8c50`; worklog records exact pushed-revision 1/1 GREEN. | Cross-platform source test; uses Tokio/server loopback and disposable SQLite. | Medium-high: server plus provider thread, bounded 128 KiB request/256 KiB response, serial jobs/threads. |
| Native artifact manifest verification | PROVEN for checked-in arm64 macOS manifest/artifact contract; release status remains blocked by broader TUI review | `crates/opentui-bridge/tests/native_artifact_manifest.rs` (read-only verification target) | verification | `opencode-rk-opentui-bridge` / `native_artifact_manifest`; no `native` feature | `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 timeout 180 cargo test -p opencode-rk-opentui-bridge --test native_artifact_manifest -- --test-threads=1` | Corrected frozen target expected `3 passed, 0 failed`; validates bounded metadata, fork commit `c01292fd0837bafd07ce458c74416b2b375a41ab`, target `aarch64-apple-darwin`, Mach-O arm64 dylib, sidecars. | `crates/opentui-bridge/tests/native_artifact_manifest.rs:10-14,18-23,37-98` validates manifest; `:100-154` validates artifact; `:156-204` validates sidecars. `crates/opentui-bridge/build.rs:11-65` is the native-feature link gate. `native/artifacts.json:3-14` declares path, SHA `798f30dd7f4fbe36d52c8834652ed7bcd7f20dfd2a1203d09cc24880eeb13a91`, size `6863648`. | Test is std-only and does not compile/link native feature. Existing builder matrix includes macOS/Linux/Windows-GNU, but this test checks only arm64 macOS artifact. Full Windows-GNU closure needs separate Windows CI/probe. | Current corrected test SHA `cb7d4cde7c3aed1916814c9aab747b68aa48015518c444e90bd8a58e66bd5c8b`; prior review hash `efb642364b6a4dc559fff019d21c14f778c170f34f96bf5e7183b4aeb536aa40` is obsolete evidence only. | Test can run on any host with checked-in files; artifact contract is macOS arm64. | Low-medium: bounded metadata plus 6.86 MiB file read, no network. |
| Keyring persistence | BLOCKED/QUEUED | `crates/providers/src/auth_store.rs` plus future provider integration test boundary; do not edit shared Cargo manifest in leaf lane | RED authoring then implementation | `opencode-rk-providers` / existing `prov_022_auth_store`; future persistence target not present | `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 timeout 180 cargo test -p opencode-rk-providers --test prov_022_auth_store -- --test-threads=1` | Existing target validates planning only, not persistence. Future RED must use the approved mock credential builder, then implementation must persist/retrieve through an audited keyring backend without real host keychain mutation. | `crates/providers/src/auth_store.rs:1-7` explicitly says no backend I/O; `:62-89` exposes `StoreBackend`; `:154-200` exposes allowlisted paths; `:357-430` exposes plans/status and explicit keyring availability. Existing `crates/providers/tests/prov_022_auth_store.rs` is the public planning boundary. | `worklog/APP-012.md:72-77` records controller authorization for audited `keyring` 3.6.3, but current `Cargo.toml` has no keyring dependency. Actual OS keychain proof requires disposable platform acceptance; no keychain mutation in local loop. Parent APP-005 secure setup remains unwired. | Existing `prov_022_auth_store` test is present; no frozen runtime-persistence hash. Off-plan PROV-022 completion cannot close this gap. | Platform-specific keyring acceptance: macOS Keychain, Windows Credential Manager, Linux Secret Service/mock. | High: dependency review, platform backend, secret lifetime/cleanup. |
| Approval/resume | BLOCKED/QUEUED | `crates/server/src/turn_service.rs` is the state owner; live caller boundary is `crates/server/src/lib.rs` | RED authoring after caller proposal | `opencode-rk-server` / existing `turn_service` lib tests; no frozen live approval target | `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 timeout 180 cargo test -p opencode-rk-server --lib turn_service -- --test-threads=1` | Existing state tests cover `AwaitingApproval`, approve/deny, and uncertain recovery. Live journey must pause on `RequireHuman`, accept a fresh scoped grant, resume once, and prove denial has no side effect. | `crates/server/src/turn_service.rs:303-357,386-465` exposes approval, settle, interrupt, retry and restart recovery APIs; `crates/server/src/lib.rs:1388-1438,1585-1643` is the live dispatch caller. APP-012 evidence says `RequireHuman` currently becomes a terminal tool error, with no approval-resume channel. | Must wire the real caller before a meaningful end-to-end RED/GREEN; avoid duplicate state-only leaf work. Approval is human authority, not model/prompt authority. | Existing unit evidence only; no frozen live approval hash. APP-012 parent remains open. | Cross-platform state; live PTY/HTTP journey platform-neutral if caller is wired. | High: provider round trip, approval state, no replay, durable audit. |
| Restart/resume | BLOCKED/QUEUED | `crates/server/src/loop_driver.rs` plus the APP-012 server/runtime restart caller | integration/verification | `opencode-rk-server` / `loop_driver_live`; storage baseline `opencode-rk-storage` / `restart_v2` | `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 timeout 180 cargo test -p opencode-rk-storage --test restart_v2 -- --test-threads=1` | Existing storage target proves close/reopen committed rows; it does not prove daemon process restart, same session/tool history, or no duplicate work. Next integrated test must kill/restart the real daemon and resume durable state. | `crates/server/src/loop_driver.rs:33-40,236,484-485` exposes checkpoint/resume; `crates/storage/tests/restart_v2.rs:76-107,109-159` proves WAL/reopen and rows. `docs/CONVERGENCE.md:20-24` requires exit, restart, resume, second client. | Keep queued behind APP-012 live wiring; no new state-only claim. Existing storage test is useful baseline, not parent acceptance. | `restart_v2` existing test; no frozen APP-012 restart hash. | Host-neutral SQLite baseline; full daemon journey should use current macOS PTY or a platform matrix. | Medium baseline; high full daemon restart/PTY. |
| Protected-path denial | BLOCKED/QUEUED | `crates/security/src/platform_matrix.rs` policy owner; future live test boundary `crates/server/tests/phase1_protected_path_denial.rs` does not exist | RED authoring | `opencode-rk-security` / existing lib policy tests; future server target absent | `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 timeout 180 cargo test -p opencode-rk-security --lib platform_matrix -- --test-threads=1` | Existing policy APIs should deny `.env`, `/etc`, tokens and secret paths even for `*`; future RED must prove real tool read/write performs no I/O and returns persisted denial through the server caller. | `crates/security/src/platform_matrix.rs:215-260` exposes `is_protected_path`, `authorize_file`; `:262-313` exposes `DenialReceipt` and `attempt_write_if_allowed`. Existing `crates/security/tests/sandbox_enforcement.rs:56-95` proves outside-root write leaves no file, but not mandatory protected-path or live tool dispatch. | APP-012 worklog explicitly requires a separate frozen RED for `.env`/outside-root and zero-I/O. Current `file_ops` read/list history and server caller must remain under review; do not weaken wildcard protections. | Existing policy unit/integration evidence; no frozen protected-path hash. | Host-neutral policy; live filesystem test uses disposable temp roots. | Medium: tiny fixtures, no network; full server journey high. |
| Windows-GNU closure | BLOCKED/QUEUED | `crates/opentui-bridge/build.rs` plus `crates/opentui-bridge/native/build_opentui.sh`; Windows CI owns execution | verification/integration | `opencode-rk-opentui-bridge` / target `x86_64-pc-windows-gnu`, native feature link gate | `timeout 180 bash crates/opentui-bridge/native/build_opentui.sh --verify-only crates/opentui-bridge/native/lib/x86_64-pc-windows-gnu/opentui.dll --import-lib crates/opentui-bridge/native/lib/x86_64-pc-windows-gnu/libopentui.dll.a --target x86_64-pc-windows-gnu` | PE DLL plus GNU import library must verify as a pair; `build.rs` must accept both and never relabel as MSVC. Full native-feature compile/install requires authorized Windows-GNU CI. | `crates/opentui-bridge/build.rs:47-58` requires Windows runtime DLL plus `opentui.lib` or `libopentui.dll.a`; `native/build_opentui.sh:19-40,547-623` defines matrix and pair verification. Checked-in files exist at `native/lib/x86_64-pc-windows-gnu/opentui.dll` and `libopentui.dll.a`. | Worklog APP-012 records Windows GNU as supported ABI, no MSVC target, and no Rust Windows target installed locally. `scripts/install-opencode2.ps1:1-70` installs only legacy `opencode2.exe`, not `oc2` native closure, so it is not this owner. `.github` workflow changes require protected-path review. | Artifact hashes: DLL `0be2efd8a77330ba23b0f5eeb094a548f5fee4b7ad7b4c6f83e0e786dadd6b15`; import lib `1dce093459b94a4d6bd0538c4fdd1681f50534930f3d59918b765bc87a982019`. No frozen Cargo Windows target hash. | Windows GNU CI only for actual compile/install/runtime; macOS can run bounded PE pair verifier only. | High: PE parser/import correspondence, cross-target build, CI artifact install. |

## Omitted lanes

- `tests/e2e/local_application.rs`: path absent at source revision; `worklog/APP-012.md:9` calls it the proposed parent owner, not an existing test. Do not dispatch as an existing target.
- Any archive-creation lane owned by `scripts/install-oc2.sh` or `scripts/install-opencode2.sh`: both consume/extract supplied archives; no packaging producer exists there.
- A real Landlock/macOS/Windows OS-isolation lane under current `opencode-rk-security`: public code intentionally fails closed, has no syscall backend, and must not be presented as isolation.
- A keyring lane that probes or mutates the host keychain, reads `.env`, or adds a dependency by editing shared Cargo manifests: no safe local authority or owned path.
- A full APP-012 acceptance lane based only on `app012_tool_journey_red.rs`: frozen target covers bounded read persistence in one app instance, not installed entrypoint, restart/resume, approval, protected paths, or second-client durability.
- A Windows-MSVC artifact lane: pinned upstream matrix and `build.rs` support Windows GNU, not MSVC; do not relabel GNU artifacts.
- New Cargo targets whose only purpose is to duplicate existing `shell_tool_process_tree.rs`, `sandbox_enforcement.rs`, or `native_artifact_manifest.rs` without a compiling RED failure. Existing public APIs already support focused verification; duplication would be speculative.

## Commands and source checks performed

- `cargo metadata --no-deps --format-version 1`: completed without compilation; verified package names and test target names. Relevant packages: `opencode-rk-tools`, `opencode-rk-security`, `opencode-rk-providers`, `opencode-rk-server`, `opencode-rk-cli`, `opencode-rk-opentui-bridge`.
- `python3 tools/convergence_gate.py`: `CONVERGENCE BLOCKED`, `total=90`; recorded only.
- `git ls-files`, bounded `grep`, `glob`, and source reads: verified all existing paths cited above; proposed `tests/e2e/local_application.rs` absent.
- No `cargo check`, `cargo test`, build, install, archive extraction, keychain operation, or platform backend execution performed per task constraint.

## Decisions and remaining unknowns

- `PROVEN` means the public boundary, existing package/target, and bounded command are source-proven. It does not mean parent acceptance.
- `BLOCKED/QUEUED` means a real caller, backend, platform runner, or frozen RED is missing; do not replace it with a state-only or policy-only test.
- Current branch has only the own ledger-row mutation before evidence-file creation; no product, test, Cargo, policy, plan, controller, or verifier files were changed.
- Archive producer/release receipt injection integration remains unowned. The only concrete receipt insertion point is `crates/cli/build.rs`.
