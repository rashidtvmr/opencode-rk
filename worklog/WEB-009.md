# WEB-009

Status: BLOCKED. Session: `ses_f384183edffefv0KL9K7uk6GC5`. Claim succeeded before edits.

## Evidence

- HEAD at inspection: `741e8c9` (working tree already contained unrelated dirty files).
- `crates/server/src/lib.rs:107-110` defines `AppState` with exactly `sessions` and `catalog`; frozen tests and `crates/cli/src/main.rs:706` construct it literally.
- `crates/server/src/lib.rs:877-989` creates the live stream from request-local `SessionService`, private `TURN_PERMITS`, `PermissionBroker`, `ToolRegistry`, and `AgentExecutor`; no `RuntimeWiring`/`EngineHandles` is available in the handler state.
- `crates/server/src/runtime_wiring.rs:301-357` provides daemon-owned `RuntimeWiring`, but has no caller in product code; only its own tests construct it.
- `crates/server/src/app_runtime.rs:609-660` provides `EngineHandles`, including shared permits, policy, tools, sessions, and events; no `AppState` field or server composition reaches it.
- `crates/cli/src/main.rs:678-706` opens sessions/catalog and passes the two-field literal directly to `router_with_auth`.

## Required boundary

The current focused tests are already GREEN: reasoning-summary deltas persist beside one assistant message; unsupported structured events fail closed; provider stream/failure behavior passes. Tool calls are real provider events only, not fabricated. No references/citations producer exists.

To close the live caller gap, daemon composition must inject a cloneable runtime handle into HTTP state, then the stream must borrow its shared permit/cancellation/policy/tool/persistence/event boundaries. Adding a required `AppState` field is incompatible with every frozen literal constructor. Making it optional preserves compatibility but leaves legacy routes on private runtime and cannot prove the daemon path. A second global registry would duplicate ownership and violate the runtime contract.

Observable contract remains: user event first; bounded reasoning/tool/final events; malformed/unsupported provider activity emits bounded error and never assistant content; only provider tool calls become tool activity; final settlement persists once; cancellation drops permit and does not replay ambiguous effects; no citations until native producer/persistence exists.

## Verification

Commands, serial, `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1`:

```text
env CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 cargo test -p opencode-rk-server --test session_turn_stream_api
2 passed
env CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 cargo test -p opencode-rk-server --test session_turn_activity_api
2 passed
python3 tools/convergence_gate.py
BLOCKED by pre-existing ledger violations (50 findings)
```

No RED authored: focused frozen tests already pass. No product file changed. Required integration proposal: wire `RuntimeWiring` creation/lease into daemon serve composition, expose it through a backward-compatible router state mechanism, then replace private stream boundaries in `lib.rs`; ownership must include `crates/cli/src/main.rs` and/or a shared state contract. This lane cannot safely implement that cross-file seam.
