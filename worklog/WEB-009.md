# WEB-009 worklog — structured reasoning/tool activity/references

Claim: turn-parts projection model implemented; frozen suite GREEN 5/5.
Source evidence:
- Upstream pins unchanged (`sources/upstream.lock.json`). Dependency: provider turn stream + V2 execution/tool persistence. Card's own boundary note: web turn adapter still has no native `tool_calls` producer nor durable citation contract — Tool cards/References remain unavailable, not fabricated. This lane implements the pure projection/persistence contract so producers can wire in; task stays NOT ACCEPTED until producers exist.
- Impl: `crates/server/src/turn_parts.rs` — `TurnEvent` (:64), `push` (:192, per-part bounds, unknown-tool/malformed errors, answer never touched on error), `finish` (:271), `parts` (:308, stable Reasoning/ToolActivity/Answer/References order), `accessibility` (:354, collapsed + `live_politeness:"off"`), `snapshot`/`from_snapshot` (:378-519, no hidden-CoT field; `chain_of_thought`/`hidden` keys rejected). Bounds: `MAX_EVENTS=512`, `MAX_PART_BYTES=64KiB`, `MAX_SUMMARY_BYTES=16KiB`, `MAX_REFERENCES=64`. Disconnect hook fires once (:175-183).
- Server stream path: `create_turn_stream` NDJSON in `crates/server/src/lib.rs` (:759-941); regression `session_turn_stream_api` GREEN 2/2 serial (`--test-threads=1`, process-global fixture — parallel run fails, pre-existing, not this lane).
Target boundary: `crates/server/src/turn_parts.rs` (owned, `#[path]`-included, not in `lib.rs`).
Tests: `crates/server/tests/turn_parts.rs` SHA-256 `b5874654…319d` (this rev). Frozen — not edited.
Decisions: std + serde_json only; unknown provider events must map to explicit error at call site (enum has no catch-all).
Remaining unknowns: native tool-call producer + durable citation contract (T01/T03/T05 end-to-end); verifier decides.
