# ACP-001 worklog

Claim: ses_f384fedeaffeiMPH6Dl2h3q5Ox holds ACP-001 in-progress (2026-09-22 re-verify). Ledger row tasks/completion/claims.json.

Status: IMPLEMENTED / NOT ACCEPTED (verifier decides).

## Claim
Stdio caller observes ACP v1 JSON-lines framing: one object per line for
initialize, session new/load/prompt, modes/models/variants; oversize or
malformed frames reject typed, session state untouched. No terminal, auth,
full history restore (upstream non-goals, preserved).

## Source evidence
- Pin `95daf90670b7c039c436c85537da5fbfe2205b41` (PLAN.md:36,
  sources/upstream.lock.json:9); observed-head drift recorded in task card,
  never equated; reconciliation outstanding.
- worklog/UPSTREAM-V2-web-acp-integrations.md:38-43: `acp/agent.ts`,
  `session.ts`, `types.ts`, `cli/cmd/acp.ts` = v1 JSON-lines server over
  stdin/stdout; README limits carried as non-goals.
- docs/research/UPSTREAM-V2-PARITY-SYNTHESIS.md section 9 (F39): no local ACP
  file; candidate v1 server scoped to documented commands plus README limits.
- Local absence pre-slice: `crates/server/src/acp_bridge.rs` absent (no acp
  module; HEAD `248f519`).
- Local partial (not re-owned): `app_client.rs`/`clients.rs` = endpoint
  config/registry only, no JSON-lines framing, no ACP method set.
- Classification: discovered scope under REQ-045; deliberate
  safer/resource-bounded deviation (explicit MAX_ACP_FRAME_BYTES 1 MiB cap;
  upstream supplies none; 64-session TooManySessions cap, no hidden eviction).

## Observed scenario
Pure codec + caller-owned session map. `decode_frame` validates cap before
parse, rejects non-UTF8, multi-object lines, unknown methods, out-of-bound
ids/text. `encode_frame`/`encode_frame_into` emit single `\n` line;
oversize appends nothing. `session_new/load/prompt` own the map; unknown
ids and bad text leave map byte-identical.

## Target boundary
`crates/server/src/acp_bridge.rs` owns AcpRequest, AcpResponse, AcpSession,
AcpSessions, AcpError, AcpErrorCode, MAX_ACP_FRAME_BYTES, decode_frame,
encode_frame, encode_frame_into, session_new/load/prompt, modes/models/
variants_body. `crates/server/tests/acp_bridge.rs` uses `#[path]` include;
lib.rs wiring left to integrator (lane never edits shared lib.rs).

## Tests
- Frozen: `crates/server/tests/acp_bridge.rs`
  SHA-256 `e5eb31def91fe3be8b8ae2e5de8f0d45b140f4fa61971ec6907f076eaf22394d`
- Impl: `crates/server/src/acp_bridge.rs`
  SHA-256 `2f38d37093efdf9bdfaa1a9720b2a34e40ade5bac5bcee8e6569b5a5d1f29cf9`
- RED probe (2026-09-16, temp stub in owned file, restored byte-identical):
  removed `line.push(b'\n')` => `cargo test -p opencode-rk-server --test
  acp_bridge` => 3 passed, 2 failed (`acp001_t01`, `acp001_t02` panic
  `assertion failed: line.ends_with(b"\n")`). Restored via backup;
  `diff -q` IDENTICAL, hash matches pre-probe.
- GREEN (restored impl):
  `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 120 cargo test
  -p opencode-rk-server --test acp_bridge -- --test-threads=2`
  => 5 passed, 0 failed.
- RE-VERIFY 2026-09-22 (session ses_f384fedeaffeiMPH6Dl2h3q5Ox):
  `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 cargo test -p opencode-rk-server --test acp_bridge -- --test-threads=1`
  => 5 passed, 0 failed (t01..t05 all ok). macOS has no `timeout`; ran without
  (rtk passthrough). Frozen tests untouched: `git diff --stat --
  crates/server/tests/acp_bridge.rs` empty. Impl hash unchanged
  `2f38d37093efdf9bdfaa1a9720b2a34e40ade5bac5bcee8e6569b5a5d1f29cf9`.
  Integration seam: `pub mod acp_bridge;` at crates/server/src/lib.rs:3
  (pre-existing on branch prod/native-tui-parity, not this lane's edit —
  wiring left to integrator per card). Zero impl writes this session.

## Decisions
`#[path]` include pattern (mirrors control_decode.rs precedent): no shared
lib.rs/Cargo.toml edit, integrator assembles. `encode_frame_into` proves
no-partial-emit. `serde` used (already workspace dep, no new dep).
Permission-broker pattern: module performs zero I/O/spawn, owns no FDs;
caller supplies bytes/map/root. Errors carry codes only.

## Remaining unknowns
File read/write + capability negotiation stay with ACP-002; SDK spawners
with SDK-002; real terminal/auth/history streaming explicit non-goals.
DISC-003 IN PROGRESS / NOT ACCEPTED. Frozen tests never edited.
