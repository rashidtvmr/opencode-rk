# PAR-005 worklog (verification lane, session ses_f41fada5fffek2ZhANqQ3vhz7U)

Claim: `crates/tools/src/app_services.rs` implements PAR-005 tool-service composition
types (registry `crates/tools/src/registry.rs:60-133`, executor
`crates/tools/src/executor.rs:79-130`, permission `crates/tools/src/permission.rs:1`).
Re-verified at HEAD — no product edits needed; files already GREEN.

Source evidence (HEAD 19c26184):
- `crates/tools/src/app_services.rs:27` `#![forbid(unsafe_code)]`.
- `crates/tools/src/app_services.rs:29-30` `use std::fmt; use std::sync::atomic::{AtomicBool, Ordering};` — std only, no process/thread/clock/Command (only `Command` mention is doc comment at :5).
- `crates/tools/src/lib.rs:5` `pub mod app_services;` — module wired statically.
- Repo static grep: `todo!|unimplemented!|panic!("stub|panic!("placeholder` → zero hits in both lane files (markers absent; "stub" string at app_services.rs:6 is doc mention of permission.rs:1, not a body stub).

Observed scenario: full-crate `cargo test -p opencode-rk-tools app_services`
fails to compile due to PRE-EXISTING broken target
`crates/tools/tests/mcp_config.rs:4` `mod mcp_config;` reading missing
`crates/tools/src/mcp_config.rs` (committed at 3e59074, outside lane). Scoped
lib run used: `cargo test -p opencode-rk-tools --lib app_services`.

Target boundary: pure composition types — `ToolCall` (id/tool/args/scope),
`ToolResult`, `DispatchError` (Denied/Timeout/Cancelled/Bounds), size/timeout
caps (MAX_ARGS_BYTES 4096 etc.), approval-digest helper (std FNV-1a fold,
32-byte shape matching storage `intent_hash`; ponytail: swap for blake3 when
dependency approved). No I/O/threads/clock/Command by construction.

Tests (in-file `#[cfg(test)]`, 11, GREEN at HEAD, zero test edits):
- approval_digest_stable_and_bound, benign_validates_with_default_timeout,
  cancel_gate_trips, deny_paths_report_without_side_effects,
  rejects_absolute_path_in_args, rejects_absolute_scope_and_windows_drive,
  rejects_empty_and_bad_labels, rejects_oversize_args,
  rejects_traversal_in_args, rejects_traversal_in_scope_without_mutation,
  timeout_caps_hold.
Exact cmd: `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 170 cargo test -p opencode-rk-tools --lib app_services`
Result: `11 passed; 0 failed` (95 filtered out).

Decisions: re-verify only, no edits. e2e `tests/e2e/tool_integration` owned by
integrator/parent.

Remaining unknowns: none in lane.
