# OPS-007 — small-machine admission guard

- claim: pure O(1) admission guard GREEN, no code change (src byte-identical to HEAD `248f519`; tests diff rustfmt-only).
- source evidence: commit 863a0019fc (card authoring HEAD); PLAN.md 5-6+9; config/resource-targets.json (4/256/1048576/65536, status initial-targets-not-measurements); BASE-003 owns mechanism, this card owns policy; `ops_runtime.rs` tick-counter is placeholder, not this contract.
- observed scenario: `crates/foundation/src/ops_budget.rs` (`OpsBudget::default`=4/256/1048576/65536 verified, `validate`, `:69` `admit`, `reserve_preview`, `BudgetError::InvalidLimit/OverCap`). Tests via `#[path]` include. Wired lib.rs:11.
- tests: RED prior lane (stub always `OverCap{0,0}`): 0/5. Not re-broken per no-stub policy.
- GREEN this session (bounded cmd, `--test-threads=2`): 5 passed / 0 failed.
- purity: no fs/env/net/clock refs; refusal mutates nothing; Display counters-only; no `todo!`/`unimplemented!`.
- frozen hashes: tests a56b7db4…8b76 (workdir-formatted); src 56d2829b…32bf (unchanged).
- remaining unknowns: none in slice. Whole-tree measurement stays release-bench activity per card. Verifier acceptance separate.
