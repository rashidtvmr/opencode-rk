# PAR-007 worklog (verification lane, session ses_f41fada5fffek2ZhANqQ3vhz7U)

Claim: `crates/tools/src/app_extensions.rs` implements PAR-007 extension types
standalone (std only, `#![forbid(unsafe_code)]`, no I/O). Re-verified at HEAD —
no product edits needed; files already GREEN. Prior evidence reconciled from
`worklog/PAR-007-ext.md`.

Source evidence (HEAD 19c26184):
- `crates/tools/src/app_extensions.rs:11` `#![forbid(unsafe_code)]`; no `use`
  lines at all (pure items) — std only, no I/O/threads/clock/globals.
- `crates/tools/src/lib.rs:4` `pub mod app_extensions;` — module wired statically.
- Related contracts (unchanged): `crates/tools/src/skill_defs.rs:23`
  `qualify_skills`; `crates/tools/src/plugin_lifecycle.rs:85` `PluginRegistry`
  (SUPPORTED_CONTRACT_VERSION=1); `crates/tools/src/plugin_ui_boundary.rs`
  inert `request_render`; `ext_compat.rs`, `ext_manifest_lane.rs`,
  `ext_commands.rs`, `skill_commands.rs`, `skill_gate.rs`, `ext_secure.rs`.
- Repo static grep: `todo!|unimplemented!|panic!("stub|panic!("placeholder` →
  zero hits in lane file.

Observed scenario: full-crate run blocked by same pre-existing broken target
`crates/tools/tests/mcp_config.rs:4` (committed 3e59074, outside lane). Scoped
lib run used: `cargo test -p opencode-rk-tools --lib app_extensions`.

Target boundary: pure in-memory registries (`ExtensionRegistry`, `UiCatalog`,
`CompatHost`, `Authority`), bounded (MAX_CONTRIBUTORS 128, MAX_CAPABILITIES 16,
MAX_UI 64, MAX_MARKER_LEN 256, MAX_COMPAT_BUDGET 8MiB).
`CompatHost::start(NativeOnly)` always refuses; `Authority::request` validates
all via grant gate, rejects whole batch unchanged.

Tests (in-file `#[cfg(test)]`, 8, GREEN at HEAD, zero test edits):
- custom_command_affects_turn_marker (T01), precedence_and_scope_project_beats_plugin (T01),
  native_capability_versioning (T02), unsupported_ui_not_advertised_native (T03),
  compat_never_starts_native_only + compat_account_requires_start_and_bounded_budget (T04),
  malicious_grant_rejected_authority_unchanged (T05),
  registry_bounds_and_duplicates (bounds/unknown).
Exact cmd: `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 170 cargo test -p opencode-rk-tools --lib app_extensions`
Result: `8 passed; 0 failed` (98 filtered out).

Decisions: `Scope` rank Builtin<Plugin<Project<User; re-verify only, no edits.
e2e `tests/e2e/extension_parity` owned by integrator/parent.

Remaining unknowns: none in lane.
