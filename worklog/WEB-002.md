# WEB-002 worklog

Status: IMPLEMENTED / NOT ACCEPTED (verifier decides).

## Claim
Domain failures surface as stable versioned error shape with correct status,
translated exactly at route boundary; domain service stays HTTP-free.

## Source evidence
- Pinned OpenCode `95daf90670b7c039c436c85537da5fbfe2205b41`.
- Control-plane handler blob `4c13afdb38a9f7452d6540fb1adf05129e9d649c`.
- Route policy `packages/opencode/src/server/routes/instance/httpapi/AGENTS.md`
  (blob `c44db1edbbfb43fc000b18fe8afbddbe0a2933a6`).
- Client contract test blob `64a2e958ce2d86e6f66e03d5b9353dbd1ffb358a`.
- Classification: discovered surface; no new taxonomy beyond pinned variants.

## Observed scenario
Module `crates/server/src/control_plane_errors.rs` (with `domain_imports_have_no_http()`
self-check) at HEAD `248f519`. Older sibling `crates/server/src/error_translate.rs`
same behavior, smaller test helper surface (extra T05 case differs).

## Target boundary
`DomainError { SessionNotFound, TargetExists, TargetUnreadable(String),
ConflictOrLocked, Internal(String) }` (no transport types);
`translate(&DomainError) -> HttpError { status, code, message }` frozen table
404/session_not_found, 409/target_exists, 422/target_unreadable,
409/conflict_or_locked, 500/internal; wire `{"error":{"code","message"}}`;
exhaustive match, no wildcard; `all_codes().len() == 5`.

## Tests
Frozen: `crates/server/tests/control_plane_errors.rs`
SHA-256 `050ad2725ac468cef3eac1a06d0d17f4d27c7e5d6aafe0adf8f80157978ee149`.
Legacy: `crates/server/tests/error_translate.rs`
SHA-256 `e825b2c095b1c16143d0a39c2b0db678b09393133433538e2e2e94d7b9872cdd`.
RED: compiling suite failing on missing translation module.
GREEN (2026-09-15, HEAD `248f519`):
`cargo test -p opencode-rk-server --locked --test control_plane_errors --test error_translate`
=> 5 + 6 passed, 0 failed.

## Decisions
No code change: behavior + redaction + exhaustiveness hold; stub scan clean.

## Remaining unknowns
None. SSE/raw placement and WEB-004 exposure stay with their owners.
