# EXT-008 worklog

## Claim

Own deferred-activation hook boundary per `tasks/EXT-008.md`: record bounded before/after decls in registration order, replay inertly, `try_execute` always `Deferred` gate open or closed. No handler runs, no I/O mutated.

## Source evidence

- Candidate revision: `248f519`.
- Pinned OpenCode commit: `95daf90670b7c039c436c85537da5fbfe2205b41`.
- `sources/disc-003-reconciliation.json` finding `opencode.extensibility` (partial): reload/watch + external activation deferred (task lines 23-29).
- Code-mode hook precedent with mismatch guard: `packages/opencode/src/tool/code-mode.ts:1-310`, `packages/codemode/src/codemode.ts:1-159`, `packages/codemode/src/tool-runtime.ts:1-806`; `sources/backlog-exhaustion.json` EXT-008 entry (`explicit-blocker`, `plugin-hook-contract-mismatch`, `material-new-pinned-evidence-required`). Lane invents no mutation contract: decls only.
- REQ-020 topology: hook bus SEC-010 (GREEN), SSRF guard SEC-011, bounded bus SEC-020 are separate owners; this lane is plugin-contribution boundary only (task lines 51-55).
- Isolated lane files: `crates/tools/src/plugin_hook_boundary.rs:1-187` (also wired `pub mod` at `crates/tools/src/lib.rs:38`), `crates/tools/tests/plugin_hook_boundary.rs:1-242` (`#[path]` include).
- Byte caps frozen per card (upstream supplies none): `MAX_HOOK_DECLS=128`, `MAX_FILTER_LEN=MAX_LABEL_LEN=64`.

## Target boundary

- Tests: `crates/tools/tests/plugin_hook_boundary.rs` only (committed).
- Product: `crates/tools/src/plugin_hook_boundary.rs` only (committed).
- No product/test edit by this lane; worklog-only change. `lib.rs` already wires module (prior lane); untouched here.

## Tests

- Frozen SHA-256 (tests): `57c4005a23af8a49fcc5c925eb0bb9a5bc75f3c0aeaf952c74ddf7188ec45cf8`.
- Frozen impl SHA-256: `fcb2e08a40cd4f56be563465f0f329d4713f843d4f685a6039efecde7ca27856`.
- RED: missing (same gap: shipped in `248f519` with impl; no 0/5 RED receipt).
- GREEN (this lane): `cargo test -p opencode-rk-tools --test plugin_hook_boundary` => 5 passed / 0 failed (`ext008_t01..t05`).
- Boundary proof in tests: `try_execute` known+unknown => `Deferred` closed and open (T04); sentinel in/out buffers byte-identical after every call (T04); absent-executor counter stays 0 incl. gate-ever-opened (T05); dup/overflow/invalid leave `list()` byte-identical (T03); revoke-unknown => 0 unchanged, re-declare after revoke => Ok (T02).
- No-stub / no-I/O: no `todo!/unimplemented!`, no fs/net/process/thread/tokio/statics. `git diff --check` clean.

## Decisions

- Filter charset `[A-Za-z0-9*._-]`, label `[A-Za-z0-9][A-Za-z0-9._-]*`; dup key = scope+phase+filter+label; ids monotonic from 1, never consumed on error; `revoke_scope` mirrors EXT-003 close-unknown (0, unchanged); `list` sorted by id; `replay` pure projection.
- `ActivationGate::closed()` default; `open()` starts nothing; recording identical open/closed.

## Remaining unknowns

- RED receipt missing; controller decides accept-GREEN vs fresh RED cycle.
- Crate mismatch: task wants `crates/ext/src/hook_boundary.rs` + `opencode-rk-ext`; lane lives in `opencode-rk-tools` (already `lib.rs`-wired, unlike EXT-005/006 lanes). Integrator canonicalization pending.
- Any execution/mutation follow-up needs new pinned hook-contract evidence (`reopenPolicy: material-new-pinned-evidence-required`); explicitly not attempted here.
- Verifier acceptance external, not claimed.

## DEDUP lease (this session)
- `ext_hooks.rs` vs `plugin_hook_boundary.rs` investigated: NOT duplicates (ExtHookRegistry pre/post/error vs inert HookBoundary Before/After + gate + Deferred). Left alone; no change. See `worklog/EXT-DEDUP.md`.

## Re-verify 2026-09-16 (DB/DISC/EXT lane)
- Impl: `crates/tools/src/plugin_hook_boundary.rs` (187L), wired `lib.rs:38`. Tests: `crates/tools/tests/plugin_hook_boundary.rs` (`#[path]`, 5 fns ext008_t01..t05).
- GREEN: bounded `--test plugin_hook_boundary` => 5 passed / 0 failed.
- RED probe (isolated copy): neutralizing dup-guard `return Err(HookError::Duplicate)` => ext008_t03 FAILED, 4/1. Sensitive.
- Stub/IO grep clean. No edits made (worklog only).
