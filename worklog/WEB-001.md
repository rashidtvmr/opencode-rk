# WEB-001 worklog

Status: IMPLEMENTED / NOT ACCEPTED (verifier decides).

## Claim
Route-layer decodes canonical MoveSession JSON once before domain service runs;
malformed input rejects with service uncalled.

## Source evidence
- Pinned OpenCode `95daf90670b7c039c436c85537da5fbfe2205b41`.
- `packages/opencode/src/server/routes/instance/httpapi/handlers/control-plane.ts`
  (blob `4c13afdb38a9f7452d6540fb1adf05129e9d649c`).
- `packages/opencode/test/server/httpapi-control-plane.test.ts`
  (blob `b837bf7556daf7a8fe436535d7605816fa8d2618`).
- `packages/core/src/control-plane/move-session.ts`
  (blob `e227f88df53924a49ea9d7fdb549c41a1b196c42`).
- `sources/disc-003-reconciliation.json` id `opencode.server-control-plane`
  (in-progress, partial — not release evidence).
- Classification: discovered surface; deviation only byte cap + reject-before-service.

## Observed scenario
Module `crates/server/src/control_plane_inputs.rs` present at HEAD (commit
`248f519`); byte-identical duplicate `crates/server/src/control_decode.rs`
also present (legacy name). Tests include module via `#[path]`, no `lib.rs` edit.

## Target boundary
`decode_move_session_input(bytes) -> Result<MoveSessionInput, InputDecodeError>`;
`MAX_CONTROL_INPUT_BYTES = 64*1024`; cap checked before parse; absent optional
=> `None`; present `null`/wrong type => `MalformedField`; extra keys ignored.

## Tests
Frozen: `crates/server/tests/control_plane_inputs.rs`
SHA-256 `5af77fd08ce9f04eeef106bd90fd201372918ff6f8c56549fccdf5bd1eefbc34`.
Legacy mirror: `crates/server/tests/control_decode.rs`
SHA-256 `63dfecb15146df0fdbcb0f83a8fe414f5f5d7d1cb7e4588d095d615c71c2bfd7`.
RED: compiling suite failing on missing input module (pre-implementation state).
GREEN (2026-09-15, HEAD `248f519`):
`cargo test -p opencode-rk-server --locked --test control_plane_inputs --test control_decode`
=> 5 + 5 passed, 0 failed.

## Decisions
No code change this pass: implementation already meets contract; no stub
(`todo!`/`unimplemented!` scan clean). Did not touch shared `lib.rs`/`Cargo.toml`.

## Remaining unknowns
None for decode. Domain Git/project/session mutations stay with domain owners.
