# REL-003 scratchpad — safety/resource release validator lane

## Claim
- Task REL-003 claimed by ses_f387af899ffeojhREiG4Rewoar, scratchpad worklog/REL-003.md.
- Owned paths: additive `tools/check_release_safety.py` (pre-existing, no edit needed),
  this scratchpad, ledger row. No controller/verifier/policy/lib.rs edits.

## Source evidence
- Card: `tasks/REL-003.md` (REQ-035, partition
  `safety-resource-correctness-release-validator`, entrypoint
  `python3 tools/check_release_safety.py --revision <rev> --gates <dir>
  --caps <caps.json> --quotas <quotas.json> --out <report.json>`,
  exit 0 pass / 2 assurance failure / 1 tool error).
- Implementation: `tools/check_release_safety.py` lines 1-343 (committed in
  248f519 "feat: add native harness feature batches"); stdlib only, single
  process, no network, no wall-clock in verdict, bounded (256 files / 8 MiB
  in / 64 KiB report), reads gates/caps/quotas read-only, writes only <out>,
  refuses to emit secret-canary bytes.
- Fixtures: `fixtures/release-safety/` (gates/, caps.json, quotas.json,
  fail-star-bypass, fail-side-effect-fs, fail-side-effect-proc, fail-oversize,
  fail-history-deletion, fail-blocked, fail-malformed, tdd-only).
- Repo HEAD at verification: 6d10d51.

## Observed scenario
Validator found fully implemented on arrival. Ran focused read-only checks
(single process, rtk-prefixed, disposable /tmp outputs only, user DB untouched):

- T01 safe bounded candidate passes: exit 0, passed true, all four checks
  pass, partition exact, failing_fixture null, stdout byte-mirrors report.
- T02 star-bypass: exit 2, mandatory_intact fail, failing_fixture
  `.../mandatory.json#SsrfBlock`, reason mandatory-bypass.
- T03 fs + proc side effects: exit 2, denied_no_side_effects fail, fixture
  names the op (`write:/tmp/...`, `exec:/bin/...`), reason denied-side-effect.
- T04 oversize: exit 2, bytes_bounded fail, reason unbounded-retention;
  history-deletion: exit 2, quotas_enforced fail, reason silent-history-deletion.
- T05 TDD-only gates: exit 2, all checks fail, reason unverified-release-state;
  determinism: two runs byte-identical (303 B); fixtures untouched
  (mtime+size snapshot equal); canary `REL003-CANARY-...` absent from
  report+stdout+stderr.
- Negatives: malformed gates exit 1 no report; gates-not-a-dir exit 1 no
  report; unknown flag exit 1; `--timeout 0.000001` exit 1 abort.
- `python3 tools/validate_plan.py` runs (pre-existing INT ownership-gap
  staleness notes only, unrelated to this lane).

## Target boundary
Distinct slice held: TDD-only input never passes (not REL-002); SEC-017
control semantics consumed not re-owned; DISC-008 inventory not re-owned;
no Rust product module per closureCriteria.

## Tests
No new test files authored: card verification is the validator CLI itself
against frozen fixture dirs; all T01..T05 obligations plus negatives
exercised directly. Zero test/fixture/validator edits.

## Decisions
No implementation write needed — repair lane with nothing to repair.
Landing scratchpad + ledger row only. Verifier decides acceptance;
no release-acceptance claim.

## Remaining unknowns
None for this lane. Frozen-test hash/RED-freeze ritual (TDD steps 3-4)
predates this lane (artifacts landed in 248f519); noted, not re-litigated.
