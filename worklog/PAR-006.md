# PAR-006 — End-to-end security on each supported OS (re-verify lane)

- Claim: PAR-006 in-progress, session ses_f41fada5effeotMYnIJLSZYX44, scratchpad worklog/PAR-006.md (via completion_claims.claim).
- Owned file: crates/security/src/app_policy.rs only (766 lines, 15 #[test] fns). Deps: opencode_rk_contracts + serde + thiserror. Mod wired at crates/security/src/lib.rs:3 (pre-existing, not touched).
- Source evidence: header docs crates/security/src/app_policy.rs:1-18 (baseline-first semantics, wildcard cannot bypass broker Deny, grant satisfies RequireHuman only, require_os_sandbox always UnsupportedSandbox). Tests at app_policy.rs:430+ (15 fns, PAR-006 T01-T05 obligations mapped).
- Observed: file unmodified at HEAD (git diff empty for owned file); grep for todo!/unimplemented!/stub/placeholder in owned file: zero hits.
- Target boundary: zero test edits. Run crate test target GREEN only.
- Tests: `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 180 cargo test -p opencode-rk-security app_policy` (see results below).
- Decisions: re-verify only; no implementation change needed if green. No other files touched.
- Remaining unknowns: none pending test run.

## Test results
- Cmd: `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 180 cargo test -p opencode-rk-security app_policy`
- Result: 15 passed, 0 failed (app_policy unit); integration targets 0 matched/filtered ok. Exit 0.
- Zero test edits. Owned file byte-identical to HEAD (no diff).
