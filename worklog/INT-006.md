# INT-006 worklog

## Claim

Verify the INT-006 remote-MCP transport/auth boundary candidate:
`crates/providers/src/mcp_transport.rs` + frozen tests
`crates/providers/tests/mcp_transport.rs` (T01..T05). No product edits made;
verification + mutation-probe evidence only.

## Source evidence

- Candidate base revision: `248f519`.
- Pinned OpenCode commit `95daf90670b7c039c436c85537da5fbfe2205b41`:
  `packages/core/src/integration.ts` + `packages/server/src/handlers/integration.ts`
  (per card; not re-audited beyond the card).
- `sources/disc-003-reconciliation.json` + `sources/integrations-ownership-gap.json`
  transport sections (per card).
- Contract read: `tasks/INT-006.md`, `docs/TDD.md`, `docs/SECURITY.md`.
- Product: `crates/providers/src/mcp_transport.rs`
  (sha256 `3eab7ccb56262d94d743d22b410ca2db76338051a0c1323f9194b1c94d6d36f5`).
- Tests: `crates/providers/tests/mcp_transport.rs`
  (sha256 `727ac4c8ddb5f2aa583b7b86827edc18a54ade68ec19b8e0ff47722039dbc991`).
- No `todo!`/`unimplemented!`/stub/placeholder in the module (covered by the
  shared INT-005 grep; identical exit-1 result).

## Target boundary

- Product: `crates/providers/src/mcp_transport.rs`, exported in
  `crates/providers/src/lib.rs` (predates lane; untouched).
- Contract values verified present: `HANDSHAKE_TIMEOUT_MS = 10_000`,
  `MAX_HANDSHAKE_BYTES = 64 KiB`, `MAX_URL_BYTES = 2048`, `MAX_ARGV = 32`,
  `MAX_ARG_BYTES = 1024`, `MAX_HANDLE_BYTES = 128`.
- API verified: `McpAttachment::validate`, `::connect(cfg, transport,
  known_handles, cancel)`, `::endpoint_label`, `::safe_describe`;
  `McpSession::close` + `Drop` reclaim; `LoopbackTransport` fake with
  `FAKE_MARKER = "loopback-fake-v1"`, virtual-clock `FakeStep` script.
- Exclusions honored: handles only (no tokens), no live sockets/processes,
  no reconnect loop, no registry, no persistence/events.

## Tests

- Frozen test sha256 unchanged: `727ac4c8...dbc991`.
- Mutation RED probe (temporary, restored byte-identical IDENTICAL):
  removed the unknown-handle `AuthRejected` check (kept handle unused) =>
  `cargo test -p opencode-rk-providers --test mcp_transport` => **4 passed /
  1 failed** (`int006_t04_failure_and_cancel_reclaim` FAILED). Evidence:
  `/tmp/opencode/int006_red2.log`. (First probe attempt hit a dead pattern and
  was discarded without touching behavior; effective probe is the second.)
- GREEN after restore: 5 passed / 0 failed. Evidence: `/tmp/opencode/int006.log`.
- Full crate regression shared with INT-005 lane: 55 targets ok, 312 passed,
  0 failed (`/tmp/opencode/provfull.log`).
- Adjacent lanes still GREEN: `refresh_gate` 5/5, `int_connect` 5/5,
  `integration_registry` 5/5.
- `git diff --check` clean; `cargo check -p opencode-rk-providers` clean
  (pre-existing warnings only).

## Decisions

- No product edits: candidate implements validate-before-connect, auth-handle
  resolution, virtual-clock bounded handshake (bytes + time + cancel +
  remote-close), per-failure handle reclaim, and `hdl-***`/query-stripped
  redaction.
- No scope expansion: MCP session semantics, tool listing, prompts, real
  sockets/processes stay caller-owned per card.

## Alias decision (2026-09-15 lane: INT-005/006/007)

- No aliases needed for INT-006: module `mcp_transport.rs` already exposes the
  exact card names (`McpTransport`/`McpAuth`/`McpAttachment`/`McpError`/
  `HANDSHAKE_TIMEOUT_MS`/etc.). No collision with a sibling module.
- Shared alias-lane evidence: RED stub-swap (skipped auth-handle check) =>
  4 passed / 1 failed (`int006_t04` FAILED), restored byte-identical
  (`diff -q` IDENTICAL), GREEN 5/5 after restore.
  Evidence: `/tmp/opencode/int006_red.log`, `/tmp/opencode/int006_green.log`.
- Frozen test hash unchanged: `727ac4c8...dbc991`; product hash unchanged:
  `3eab7ccb...d6d36f5`.

## Remaining unknowns

- None for this boundary. Verifier/controller acceptance remains external
  and is not claimed.
