# APP-001 — no-subcommand launches real application

- Claim: `tasks/completion/claims.json` APP-001 in-progress, session ses_orch_APP001, scratchpad worklog/APP-001.md.
- Deps: APP-002 completed, TUI-003 completed — pickable.
- Owned scope: `crates/cli/src/app_start.rs` ONLY. main.rs is APP-010/integrator — do NOT touch.

## Source evidence (HEAD 95bb156)

- `crates/cli/src/main.rs:187-192` — `None` arm calls `chat::run(&data)` (line-loop chat, not native TUI).
- `crates/cli/src/chat.rs:45-73` — `run()` probes daemon, spawns `serve` child, kills/waits owned child on exit; offline path prints manual `opencode-rk serve` instruction.
- `crates/cli/src/app_start.rs` (owned) — pure launch-mode routing (`decide_launch_mode`, `enters_raw_mode`), headless message/exit code, `TerminalRestoreGuard`, `PendingDescriptorGuard`; 8 tests green at HEAD.
- `crates/cli/src/daemon_client.rs` — credential-bound discovery/startup election (`try_become_owner`, `decide_lifecycle_authed`, `CredentialPolicy`); client-side authority for attach vs start.
- `crates/cli/src/native_app.rs` (TUI-003) — `NativeApp` state, views incl. Empty/Offline/Error/Actionable; in-app surface the default launch must open.
- `crates/cli/src/onboarding.rs` (APP-005) — in-app provider setup machine; missing creds must route here, not to trace/manual instructions.
- `crates/cli/src/shutdown.rs` (APP-009) — `RestoreGuard` terminal-restore decision; sibling pattern to owned guards.
- `crates/server/src/daemon.rs:151-172` (cited in owned file) — atomic descriptor publish temp+rename.

## Observed scenario / target boundary

Owned file already routes TTY/headless/error + RAII cleanup with no syscalls/FS/process control (caller wires probes/cleanup). Card remainder to close in owned file:

1. Fresh-HOME PTY → native view, owner start, no serve/session command.
2. Second terminal (reusable daemon) → attach same daemon, no second owner.
3. Missing creds → in-app setup view, never trace/manual server instructions.
4. Redirected stdio → headless/error plan, never raw, never owner/view.
5. Startup failure → terminal restored + temp descriptor/child cleaned; commit path cleans nothing.

## Tests (RED first, frozen before GREEN)

Existing 8 tests untouched. New APP-001 remainder tests (compile + fail on missing behavior):

- T1 fresh_home_pty_opens_native_as_owner_without_serve_command
- T2 second_terminal_attaches_same_daemon
- T3 missing_creds_opens_in_app_setup_not_trace
- T4 redirected_stdio_plan_never_raw_never_owner
- T5 startup_failure_restores_terminal_and_cleans_descriptor

RED impl: stubs returning wrong-but-compiling values (always-Owner / always-Main / probe-ignoring plan / empty setup message) so suite compiles and new tests fail on behavior.

- RED freeze hash: `6f41ab6f0394ab4e58f85f94d04a1a18e445ee70ccaca7eb64241cc4414338d4`
  (RED run: 8 passed / 5 failed — all 5 APP-001 remainder T1-T5 fail on behavior; 8 pre-existing pass)
- RED log: /tmp/opencode/app001-red.log (copied to /tmp/opencode/app001.log pre-GREEN, then overwritten by GREEN)

## Decisions

- Pure decision types only (`DaemonPresence`, `StartupRole`, `StartupView`, `DefaultLaunch` + 3 fns + 1 message); mirrors `daemon_client` election without adding deps so file stays standalone-compilable.
- `DaemonPresence` takes caller-validated state (validation stays in `daemon_client`); this file only routes.
- `setup_message` static text: no secrets, no control sequences, no `serve`/manual-server wording.
- Plan yields `role: None, view: None` off-TTY: redirected launch can never own/start or enter raw.

## Remaining unknowns

- Integrator wiring (`main.rs` None arm → `plan_default_launch` + `tui_entry`/`native_host` + `onboarding`) is APP-010 lane, out of scope.
- E2E PTY proof (fresh HOME, second terminal attach) is APP-012 golden journey, out of scope.

## Evidence

- GREEN cmd: `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 180 cargo test -p opencode-rk-cli --bin opencode-rk app_start` logged /tmp/opencode/app001.log
- GREEN result: 13 passed, 0 failed (8 pre-existing + 5 APP-001 remainder T1-T5). Zero test edits between RED freeze (`6f41ab6f…`) and GREEN — `cargo fmt` whitespace only, tests untouched.
- GREEN file hash: `38454ed39491dd6cbb31bbf64f29526036b1cc31242e1619c71ecc7684e230e2`
- No stubs/todo!/unimplemented!: `grep -c` = 0, no "RED stub" remains.
- Commit/push: _pending_
