# RC-03: singleton + stale-pid reclaim regression (child of AUD-001)

Claim: ses_orch_RC03, ledger `RC-03` in-progress. Owned scope: `crates/cli/src/service_commands.rs` ONLY. No main.rs edits.

## Source evidence
- AUD-001 repair child RC-03 (`sources/completion/audits/AUD-001.json`): gap = no committed test pins AlreadyRunning short-circuit (main.rs:540-549) nor stale-pid reclaim across processes. Explicit tests: (1) second serve same data-dir exit 0 prints origin undisturbed; (2) kill -9 reclaim rebinds fresh pid; (3) DELETE/restart client_count 0 socket removed.
- `crates/cli/src/main.rs:538-551`: `SingletonDaemon::bind` → `Err(AlreadyRunning)` reads descriptor, prints origin, `Ok(())` (exit 0); other errors propagate.
- `crates/server/src/daemon.rs:54-78` `PidLock::acquire`: flock try_lock; WouldBlock → AlreadyRunning; dead holder (kill -9) releases lock → reclaim succeeds. `pid_alive` (43-48) only gates descriptor read (133-149), not the lock.
- `crates/server/src/daemon.rs:237-241` Drop removes socket; `client_count` (229-231) atomic; `shutdown` (220-223) flag+Notify.
- `crates/cli/src/service_commands.rs`: pure-state file (no syscalls/IO); `ServiceController::start` AlreadyRunning (149-155), `ClientRegistry` detach-keeps-daemon (233-245), `ResourceLedger`/`assert_no_leak` (320-361). Zero cross-module callers (only `mod service_commands` in main.rs:44) — helper wiring to serve/web is integrator's job.
- `crates/cli/tests/web_singleton_runtime.rs:127-178`: existing integration test pins same-data-dir reuse + isolation (covers T01 at process level for web path); owned-file unit pins are the missing piece.

## Target boundary
Pure decision helper `decide_singleton_launch(lock_held_by_other, origin_usable)` + `SingletonLaunch::{Reuse,Rebind}` in owned file. Caller (serve/web) supplies observations; helper never touches IO — preserves file's pure-state invariant. Socket-unlink/client-counter ownership stays in server `SingletonDaemon`; T03 pins the pure analogue (detach-all → 0, stop → down, ledger back to baseline = all resources released).

## Tests (frozen RED → GREEN)
New in `service_commands.rs` mod tests:
- `rc03_second_launch_reuses_live_origin` (T01)
- `rc03_second_launch_without_descriptor_errors` (taxonomy extra)
- `rc03_stale_pid_reclaims_fresh` (T02)
- `rc03_rebind_after_stop_restarts_clean` (taxonomy extra)
- `rc03_restart_cycle_returns_to_baseline` (T03)

## Decisions
- Error for held-lock + unusable origin maps to `ServiceError::AlreadyRunning` (lock proves a holder; matches main.rs descriptor-unavailable error path).
- Stale pid content is not a helper input: flock ownership is the only signal (daemon.rs:66-72); tests pin Rebind for lock-free regardless of descriptor usability.
- `ponytail:` real socket unlink lives in server `SingletonDaemon::drop`; port-release in ledger is the pure analogue.

## Remaining unknowns
- None for lane. Integrator must call helper from serve/web AlreadyRunning path.

## Verification evidence
- RED (stub helper always ReuseLive, isolated rustc harness — the
  `opencode-rk-tools` lib has pre-existing `unsafe` compile errors blocking
  the in-crate runner for any edit validation; standalone harness is
  bit-identical copy of owned file):
  `rustc --edition 2021 --test /tmp/opencode/rc03/red.rs -o /tmp/opencode/rc03/red && /tmp/opencode/rc03/red rc03`
  → 2 passed, 3 failed (`rc03_stale_pid_reclaims_fresh`,
  `rc03_second_launch_without_descriptor_errors`,
  `rc03_rebind_after_stop_restarts_clean`). Log: /tmp/opencode/rc03-red.log.
- GREEN same harness on frozen file: 5 passed, 0 failed.
- In-crate unchanged-file run (validates compilation + no regressions):
  `cargo test -p opencode-rk-cli --bin opencode-rk service_commands`
  → 14 passed, 0 failed (9 existing + 5 new). Log: /tmp/opencode/rc03.log.
- Frozen test sha256: recorded at commit time below.
- Final frozen sha256 `54a99af1...2840bee` (whole owned file), all-14 harness
  green after formatting touch; zero test edits post-freeze.
