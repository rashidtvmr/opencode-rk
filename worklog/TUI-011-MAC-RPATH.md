# TUI-011-MAC-RPATH candidate

- Claim: proposal-only, user-authorized one-file implementation lease, session `ses_f3c4de578ffelQv59xDXmOs03B`; parent TUI-011 remains blocked. Base `f0b5a27ffa8cc6d843e4e84d64c9489506efb492`, which includes prerequisite compile fix `55a4292132958af6fc6d6c5b15e89c8d75b35e84` and frozen Mac loader test.
- Source evidence: `crates/cli/Cargo.toml:9-18,38-39` declares both binaries and native feature; `crates/opentui-bridge/build.rs:11-43` selects static or dynamic native library but emits no executable loader path; `scripts/install-oc2.sh:308-310` installs into sibling `bin/` and `lib/`. Mac native binary from 55a4292 had `@rpath/libopentui.dylib` with no `LC_RPATH`; dyld aborts on `--version`.
- Observable contract: a dynamic native Mac binary loads sibling `lib/libopentui.dylib` with no `DYLD_*`; a dynamic Linux binary uses `$ORIGIN/../lib` with no `LD_LIBRARY_PATH`; static and non-native builds do not gain a native loader path. The native artifact remains a bounded build prerequisite. No persistent state changes from this build script.
- RED: `tests/e2e/macos_loader.rs` frozen SHA-256 `c0a809a28800b542613bfb79b0d9566c07c955e45cb9afe441c0a697ae99ef36`; compiled with `rustc --test`, listed one test and failed exit 101 against the source-built 55a4292 binary (dyld SIGABRT, no LC_RPATH). The genuine fixture is at approved temp path `mac-opentui-preserved-4954312/packages/native/lib/aarch64-macos/libopentui.dylib`, SHA-256 `5e0265d4b053004fe78974562561e10e46567cca96697f100e3288653163330e`.
- Decision: add only `crates/cli/build.rs`; use Cargo's binary link-argument directive for both CLI binaries, and mirror bridge artifact selection to avoid an rpath for static builds. No test or installer changes.
- Candidate revision: `9cf998a5512f86e5f88c2f57280fc79d1f22c19a` (initial one-file code commit, before this evidence update). Fresh `git archive` in approved temp `native-rpath-proof-9cf998a/`, genuine dylib copied only into this disposable snapshot. Bounded `cargo build --locked -p opencode-rk-cli --bin oc2 --features native` with one Cargo job, offline dependency cache and a 300-second owned-process-group deadline: exit 0 in 66.5s; 146,747 stderr bytes were concurrently drained with only the last 16 KiB retained. Built binary SHA-256 `1255392b8114c17a730dbd8354a4d07756d1dac1697c32bff96ad0eb7047e7cd`, 45,447,176 bytes, contains `LC_RPATH @loader_path/../lib` and needs `@rpath/libopentui.dylib`.
- GREEN: unchanged frozen `tests/e2e/macos_loader.rs` test (SHA above) passed 1/1 when `OC2_NATIVE_BINARY` pointed at that source-built binary and `MAC_OPENTUI_FIXTURE` at the real dylib; `env_clear` in the test omits all DYLD_* variables. A previously patched disposable binary also passed, but was only feasibility evidence; this result uses source-built code.
- Non-native check: `cargo build --locked -p opencode-rk-cli --bin oc2 --no-default-features` exited 0 in 2.1s under a 120-second owned deadline; `otool -l` found zero LC_RPATH and `otool -L` found no libopentui dependency. Native binary bytes were preserved separately before this second build.
- Independent read-only verifier `ses_f21a38f31ffe4lctiPopLV946M` checked source archive `crates/cli/build.rs` against `git show 9cf998a` (both SHA-256 `c78add214d1f2b0218b53eaffd274769c4301432733666e1fbde4b0d32f01dc0`), fixture and native-binary SHA-256 values, Mach-O load commands, non-native absence of OpenTUI, frozen test hash, and independently compiled/ran the unchanged test: 1 discovered, 1/1 GREEN in 0.06s. This is scoped Mac loader evidence, not installer/release acceptance.
- Lenovo diagnostic: a 3 GiB/2000 MHz disposable Nomad allocation `edb8f227` fetched and checksum-verified the `9cf998a` source archive (SHA-256 `8b6ae41281748ace72e8dde75cc4ad76a17e6d139d90c2f5d522befb2f18e1de`), began a native Rust build, and exited 65. Nomad discarded its final logs before capture, so ELF RUNPATH and installed Linux layout are still **unverified**. The job is stopped and the owned Mac source server was terminated. Repeat later with bounded durable log capture on the installer candidate.
- Remaining: no Linux executable has been built from this revision; installer rejects the real ~72 MB Mac archive at its 1 MiB cap; native compile frozen test hash `3bf19f05...` has an independent pipe-deadlock contract review; repository validation still fails 51 backlog errors, convergence 83 findings. Parent TUI-011 remains blocked. This is not an integrated or accepted release.
