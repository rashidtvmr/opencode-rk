# RC-02 — hardened descriptor validation (repair child of AUD-001)

Claim: session ses_orch_RC02, ledger in-progress, owned scope crates/server/src/daemon.rs ONLY.

## Source evidence
- Gap: AUD-001 (sources/completion/audits/AUD-001.json:247-259, RC-02): server
  read_backend_descriptor (daemon.rs:133-149 @7b2ba00) has no size cap, no
  symlink check, no UID check. Hardened validator (validate_file_meta /
  parse_backend_descriptor, 8KiB cap) is mod-private to cli binary
  (main.rs:23 `mod daemon_client`, zero cross-module callers) so serve/web
  never invoke it.
- Current server gate (daemon.rs:142-147): schema==WIRE_SCHEMA_VERSION,
  pid_alive (/proc/<pid> exists, pid 0 dead), origin
  starts_with("http://127.0.0.1:"). Prefix check accepts
  `http://127.0.0.1:4096.evil.com`, trailing paths, port 0/99999/leading
  zeros. No kill/signal path anywhere in daemon.rs (grep kill|signal|libc: no
  hits) — occupied-port-never-kills holds structurally; test pins behavior.
- Callers: cli main.rs:513 web() short-circuit, main.rs:541 serve()
  AlreadyRunning fallback, main.rs:563 publish. Invalid descriptor must be
  fail-closed (Err) so serve() surfaces instead of rebinding over attack file.
- Reference (read-only): cli daemon_client.rs:40-44 MAX 8KiB, :114-131 meta
  order size->symlink->owner, :179-194 strict loopback parser. Reimplement
  natively in daemon.rs (cli binary not importable); no new deps (server
  Cargo.toml has no libc/nix; euid via /proc/self/status std-only, consistent
  with existing /proc pid_alive).
- No unsafe; lib.rs `#![forbid(unsafe_code)]` unaffected. daemon.rs has no
  forbid attr; new code safe-only.

## Observable contract (target)
- read_backend_descriptor: missing file -> Ok(None) (unchanged).
- symlink_metadata len > 8KiB -> Err(Descriptor TooLarge) BEFORE read.
- symlink -> Err(Descriptor symlink) before read (dangling symlink also Err,
  proving pre-read refusal: old code mapped to Ok(None) via NotFound).
- owner uid != euid -> Err(Descriptor wrong owner).
- post-read len recheck (TOCTOU), serde parse, then schema/pid/strict-origin
  mismatch -> Ok(None) (stale/forged content ignored; caller republishes =
  "rebound loopback").
- Strict origin: exactly `http://127.0.0.1:<port>`, digits, 1..=65535, no
  leading zeros, no suffix.

## Tests (in daemon.rs mod tests; frozen post-RED)
- T1 rc02_spoofed_origin_ignored: prefix-attack + trailing-path + bad-port
  origins with live pid -> Ok(None). RED: old starts_with returns Some.
- T2 rc02_symlink_refused: symlink->valid target -> Err. RED: old Some.
- T3 rc02_dangling_symlink_refused: dangling symlink -> Err (NotFound would be
  Ok(None)). RED: old Ok(None).
- T4 rc02_oversized_refused: >8KiB otherwise-valid JSON, live pid -> Err.
  RED: old parses and returns Some.
- T5 rc02_occupied_port_never_kills: sleep child + forged descriptor read ->
  result ignored/None AND child still alive, reaped. Regression (green on old
  too); pins no-kill invariant behaviorally.
- GREEN-time: pure check_owner_uid unit (foreign refused, self ok) +
  current_uid self-consistency (created file uid == current_uid, accepted).
  DEVIATION: fs-level foreign-UID refusal not fabricable as uid-1000 without
  root/sudo; logic pinned by pure unit test + positive fs wiring. No
  privilege-dependent or skipped tests committed.

## Decisions
- Refusals (size/symlink/owner) = Err(Descriptor), not Ok(None): fail-closed,
  serve() AlreadyRunning path bubbles instead of silently rebinding.
- Stale content (schema/pid/origin) = Ok(None): preserves existing
  ignore-and-republish semantics observed in audit runtimeEvidence.
- euid from /proc/self/status (Uid: r e s line, field 2); unreadable -> Err
  fail-closed.

## Remaining
- RED done: 4 fail as expected (spoof/symlink/dangling/oversize), occupied-port
  green-by-design (no-kill invariant). Frozen RED sha256:
  679d054a82d0a9dd23b0cf6943477296eb5c7cd9de3876429be794f (@10909ad).
- GREEN done: 13/13 daemon + 149/149 server lib green, zero test edits
  post-freeze. Frozen GREEN sha256:
  80b724627288d03ba1591c3bd9a25380b9f5e651d7cdf1867f4f6ae183928d16.
- Note: pre-existing dirty tree (30 files incl cli/tools/storage) blocks full
  `git diff` attribution but owned file diff isolated: daemon.rs +210/-4.
  `cargo test -p opencode-rk-server --lib` compiles despite dirty tools/ (tools
  errors seen earlier were from parallel-lane dirty files during check race;
  final GREEN run clean).
- Log: /tmp/opencode/rc02.log (RED) + GREEN tail appended; full GREEN command
  evidence below.
