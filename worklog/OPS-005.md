# OPS-005 — config overlay merge

- claim: pure overlay merge GREEN, no code change (workdir files byte-identical to HEAD `248f519`; untouched by formatter drift).
- source evidence: PLAN.md 5-6+9; docs/TDD.md 2-5; tasks/OPS-005.md contract; opencode 95daf90 packages/core/src/config.ts:122-217, config.test.ts:146-785. Discovered scope, deliberate deviation (pure merge; discovery/mutation/retention excluded).
- observed scenario: `crates/foundation/src/config_overlay.rs:98` `merge_overlay`; scope rank Global<Project<Local, order-independent; keys sorted; caps 4096/256B/64KiB (`MAX_ENTRIES=4096`, `MAX_KEY_BYTES=256` verified in src). Tests `crates/foundation/tests/config_overlay.rs` via `#[path]` include. `lib.rs:5` prewired.
- tests: RED prior lane (stub always `NoDocs`): 0/5. Not re-broken per no-stub policy.
- GREEN this session (`CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 120`, `--test-threads=2`): 5 passed / 0 failed.
- purity: no `std::fs`/`env`/`net`/`process`/`tokio`/`rusqlite`/`SystemTime` refs; error variants carry no key/value bytes; no `todo!`/`unimplemented!`.
- frozen hashes (sha256): tests 5ca563b6…edb09 (unchanged); src bb3579ae…aaf6b29e (unchanged).
- remaining unknowns: none in slice. Upstream differential fixtures not re-run (no network). Verifier acceptance separate.
