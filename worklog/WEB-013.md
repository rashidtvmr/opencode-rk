# WEB-013 worklog — search and deep-research chat mode (GREEN-only, NOT ACCEPTED)

Claim: daemon reports Search/Deep Research unavailable with explicit reason;
composer must surface disabled entries, never relabel grep/text turns.
No research executor exists. Story NOT ACCEPTED. No valid RED.

## Source evidence

- `tasks/WEB-013.md:29-38` — boundary: capability manifest reports
  unavailable; local grep never relabeled; no executor/plan-persistence/
  citation adapter; happy-path/replay tests blocked; NOT ACCEPTED.
- `crates/server/src/lib.rs:175-182` — `web_capabilities()`:
  `"search":{"available":false,"reason":"local grep is not a web-search adapter"}`,
  `"deep_research":{"available":false,"reason":"no native research execution
  and citation adapter is installed"}`.
- Upstream pins (boundary context, no research-adapter reference claimed):
  opencode `95daf90670b7c039c436c85537da5fbfe2205b41`,
  9router `17c4cc76877bd1755030a8414f8d0083f48dcccf`
  (`sources/upstream.lock.json:9-27`).
- `ralph.json`: WEB-013 `not-started`. Base rev `248f519`.

## Observed scenario (verify-only rerun, no product edit)

- `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 110 cargo test
  -p opencode-rk-server --test web_capabilities_api`
  → 1 passed (`web_012_t01_daemon_reports_real_tools_and_unavailable_web_adapters`),
  0 failed. Covers tools/approvals/search/deep_research/voice unavailability
  strings, not WEB-013 T01-T05 per-test.
- No `crates/server/tests/*research*` or `*search*` file exists; no
  research plan/progress/persistence code in `crates/server/src/`.
- Product code predates lane → first run GREEN. NOT valid RED.
  No RED history fabricated.

## Target boundary

- Owned: this worklog only. No product/test file touched.
- Explicitly NOT owned: `crates/server/src/lib.rs` capabilities block,
  any future research executor, composer browser code.

## Tests

- None frozen by this lane. Prior evidence:
  `crates/server/tests/web_capabilities_api.rs` (54 lines, 1 test)
  asserts unavailable adapters incl. search/deep_research.
- WEB-013-T01..T05: no frozen suite; happy-path/replay blocked
  per card (no executor).

## Decisions

- `ponytail:` record unavailable-boundary only; dedicated research
  executor + citation adapter deferred until native adapter exists.
- grep/text turns must carry no research mode/source fields.

## Remaining unknowns

- Real native search/research executor + plan/progress persistence +
  answer-citation adapter design (needs integration proposal).
- Frozen T01..T05 RED/GREEN owned by future lane; verifier decides.
- Story stays NOT ACCEPTED per `tasks/WEB-013.md:38`.

## Additive lane (2026-09-16, rev 248f519) — reason-string precision probes
- Owned NEW file only: `crates/server/tests/web_capabilities_reason.rs` (3 tests).
- Read-only: `crates/server/src/lib.rs:135-194` (`web_capabilities`), frozen `crates/server/tests/web_capabilities_api.rs` (never edited).
- Probes: unavailable entries carry non-empty `reason` (search/deep_research/voice + tools + plugins/approvals/attachments); reason names adapter boundary (search~grep, deep_research~citation, voice~transcription/audio); unknown `scope` never silently available (unknown key absent, `?scope=unknown_scope` stays unavailable).
- GREEN (serial JOBS=1 THREADS=1 `--test-threads=1`): 3 passed, 0 failed.
- Sensitivity (mutation in /tmp copy only, restored byte-identical): flip `available==false`→`true` in owned test → FAIL `left: Bool(false) right: true`, log `/tmp/opencode/zE-cap-mut.log`. Owned file restored + GREEN rerun 3/3.
- Bounds: no impl/frozen/lib.rs/ralph.json edits. Blocked note: dirty-tree compile failure observed in `crates/sessions/src/share_merge.rs:200` (parallel-lane `if not map...` syntax error, NOT mine, untouched); server suite still built because prior artifact/target state allowed `opencode-rk-server` test compile — recorded, not fixed (out of lease).

## Confirm section (2026-09-16, rev 248f519) — VERIFY-ONLY, no stubs, READ-ONLY lib.rs
- Serial JOBS=1 THREADS=1 `--test-threads=1`, `timeout 120`, `free -h` checked before each run (avail ~2.4-2.6 GiB). Full log `/tmp/opencode/aN-web1317.log`.
- This lane's suites GREEN: `web_capabilities_api` 1 passed (`web_012_t01_...`); `web_capabilities_reason` 3 passed; `session_history_api` 2 passed (web_014_t01/t02); `web_workspace_api` 2 passed; `web_workspace_reason` 3 passed; `chat_nav_lane` 5 passed (web014_t01..t05); `history_rewind` 5 passed (SESS-019 guard, not WEB-014); `voice_capture` 5 passed (web016_t01..t05); `web_artifact` 5 passed (artifact_t01..t05); `web_artifact_api` 2 passed; `web_008_lane` 5 passed (companion, not WEB-01x); `web_artifact_branch` 1 passed. All run exits 0, 0 failed.
- No product/test file touched; `lib.rs` READ-ONLY; frozen suites, `ralph.json`, mic untouched. Dirty-tree note: parallel-lane edits present at `crates/server/src/lib.rs` (+20 uncommitted), `crates/sessions/src/share_merge.rs` (+13/-2); worked around, never edited.
- Other IDs covered in their own files: WEB-014 → `worklog/WEB-014.md`; WEB-015 → `worklog/WEB-015.md`; WEB-016 → `worklog/WEB-016.md`; WEB-017 → `worklog/WEB-017.md`.

## Additive lane (2026-09-16, rev 248f519) — capability-contract T01-T05 full suite
- Owned NEW file only: `crates/server/tests/web_capabilities_full.rs` (5 tests). 126 lines.
- Read-only: `crates/server/src/lib.rs:135-194` (`web_capabilities`), frozen `crates/server/tests/web_capabilities_api.rs` (never edited, `git diff --name-only` clean), sibling `crates/server/tests/web_capabilities_reason.rs` (never edited).
- T01 pins available flags (search/deep_research/voice false; attachments draft_ingest true + web_turn false; plugins/approvals false; all tools false; artifacts true/true/false/false).
- T02 pins non-empty reasons (search/deep_research/voice/artifacts + plugins/approvals/attachments + every tool).
- T03 pins unknown-scope unavailable (key absent; `?scope=unknown_scope` keeps search/deep_research/voice + attachments/plugins/approvals unavailable).
- T04 pins surfaces (tool ids sorted [bash,edit,file,grep,read,write], enabled, reason~turn adapter; plugins~plugin host; approvals~approval; attachments~attachment).
- T05 pins determinism (two GETs byte-equal JSON; tool ids sorted order).
- GREEN (serial JOBS=1 THREADS=1 `--test-threads=1`, `timeout 120`, avail ~2.5 GiB): 5 passed, 0 failed.
- Sensitivity (/tmp copy only, restored byte-identical): flip `artifacts.available true`→`false` in owned test → FAIL `left: Bool(true) right: false` (T01), log `/tmp/opencode/bC-cap-full-mut.log`. Restored + GREEN rerun 5/5.
- sha256 `crates/server/tests/web_capabilities_full.rs`: 39a79a90e8cd002fc1b67a4ea7076594bf3915941925b4f948c5c82a055af938.
- Bounds: no impl/frozen/lib.rs/ralph.json edits by this lane (lib.rs dirty-tree +20 lines are parallel-lane `pub mod` wiring, contain zero `web_capabilit` diff lines, untouched).
