# AUTHWEB-BROWSER-RED

- Claim: `AUTHWEB-BROWSER-RED`, session `ses_f216878cfffeiiHQHFrHhDp5Bb`; claimed from the `not-started` row with `reclaimedBy` set to the stopped prior owner. Route was the assigned test-author route; no allowlist was provided.
- Baseline commit: `9cf998a5512f86e5f88c2f57280fc79d1f22c19a` (branch `red/AUTHWEB-BROWSER`).
- Source evidence: `web/src/lib/api.ts:171-188`, `request()` calls `fetch(path, ...)` without an Authorization header; `web/src/lib/api.ts:461-464`, `listSessions()` requests `/api/sessions`. `crates/server/src/daemon_auth.rs:3-7,150-175`, API routes require bearer auth and reject missing credentials with 401. This is current native server auth plus a new required browser-client behavior, exercised at the real API caller rather than inferred from CLI behavior.
- Classification: current client behavior lacks authorization; current server requires bearer; requirement under test is a browser client that consumes the fragment token, clears it from browser history, sends bearer authorization, and never places it in the query.
- Observable contract: real Node HTTP server binds only `127.0.0.1` on an ephemeral port. `listSessions()` with a fake 64-hex token in `#oc2-token=...` succeeds only when its real request carries `Authorization: Bearer ...`; the real server records query and rejects unauthenticated requests with 401. Client token lifetime is one page bootstrap and fragment clearing uses `history.replaceState`. The wrapper only resolves the real API client's relative URL to the local server; it does not synthesize responses.
- Resource/security bounds: one local HTTP server, one request at a time, ephemeral port, no external network, no real credential or database, and server closed in `finally`. Fake token is test-only. No shell invocation from test.
- Test: `web/src/lib/api.auth.test.mjs`; Node `v24.21.0`, command `node --experimental-strip-types --test web/src/lib/api.auth.test.mjs`. Compiled and executed one test; it failed as expected with the actual endpoint returning 401 from `request()` at `api.ts:184` while `listSessions()` called `/api/sessions`. The failure is specifically absent browser Authorization, not a loader/fixture error. The live endpoint separately returns 401 when no credential is sent (assertion follows the expected authenticated-client path and will execute after implementation).
- Baseline convergence: `python3 tools/convergence_gate.py` failed (exit 1; 84 findings, predominantly completed off-plan ledger tasks and notes admitting no acceptance). Per task direction, this is recorded as baseline and does not block this isolated RED-only candidate.
- Frozen test SHA-256: `f007aa291204204d366506c2c8b5149099317bb103da6fe915f8e4409e5a6a62` (`web/src/lib/api.auth.test.mjs`). No test edits after freeze.
- Status: RED-only candidate; not acceptance. No implementation or GREEN was attempted.
- Remaining: set ledger `blocked` (RED-only), commit and push test/scratchpad/ledger to `red/AUTHWEB-BROWSER` without merging.
