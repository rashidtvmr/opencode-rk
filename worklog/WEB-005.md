# WEB-005 scratchpad

Claim: WEB-005 via tools/completion_claims.py, session ses_f387af8abffeHpTVd4qtNoXaSb, scratchpad worklog/WEB-005.md. Status in-progress.

Source evidence:
- tasks/WEB-005.md:1-145 (contract, T01-T05, owns crates/server/src/event_stream.rs only).
- crates/server/src/event_stream.rs:1-372 (pre-existing impl: StreamConfig default 1MiB/64/15000, encode_frame, Hub, SlowConsumer).
- crates/server/tests/event_stream.rs:1-185 (frozen T01-T05 via #[path] include, no lib.rs dependency for tests).
- crates/server/src/lib.rs:37 `pub mod event_stream;` (already wired by integrator/ancestor; not touched).
- Upstream refs per card: protocol event.ts, client effect.test.ts, app server-protocol.ts — transport-only, no UI work.

Observed scenario:
- Lane task says inspect whether existing event_stream.rs/tests are complete; implement/repair only bounded SSE framing/subscriber behavior if tests prove contract.
- Both files present at HEAD. Impl looks complete covering all 5 obligations. No edits made yet.

Target boundary:
- Own: crates/server/src/event_stream.rs + worklog/WEB-005.md only.
- Never: tests, lib.rs, Cargo.toml, schemas, routes.

Tests:
- 2026-09-22 REV 2b28732: CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 cargo test -p opencode-rk-server --test event_stream -- --test-threads=1 => 5/5 GREEN (T01 framing, T02 oversize, T03 slow-consumer, T04 reclaim, T05 invalid-UTF8), zero test edits.
- Frozen test sha256 b77527a27769f5a7ba357804c77375968c1852458e5a8ba28d95fcb922ae61da; impl sha256 f95747030bcb83d4b4af91de5e17a77befe3f97e1009dd4ffe95f2720e9ea21d.
- Stub grep over impl+tests: no todo!/unimplemented!/placeholder/stub hits.

Decisions:
- If 5/5 green with zero edits: record gap note (RED receipt absent — impl+tests landed together in ancestor commits 248f519/1be93d3; no independent compiling-RED evidence in this lane), set honest status, commit/push owned artifacts only if changed. Since owned file unchanged, no commit needed beyond scratchpad+ledger? Lane orders: commit/push only owned artifacts. Claims.json change (claim) is owned ledger row — handle per orchestrator instruction (do not release; orchestrator integrates).
- If failures: repair only within event_stream.rs bounded behavior.

Remaining unknowns:
- None on contract/implementation. Integration gap recorded: RED receipt absent in this lane (impl+tests co-landed in ancestor commits 248f519/1be93d3); verifier decides acceptance.
- Landing: orchestrator owns claims.json release/integration; lane commits scratchpad only (owned impl file untouched). claims.json is assume-unchanged (H flag); ledger in-memory row holds WEB-005 in-progress.
