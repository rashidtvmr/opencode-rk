# WEB-005 worklog

Status: IMPLEMENTED / NOT ACCEPTED (verifier decides).

## Claim
`GET /events` serves bounded well-formed SSE: complete `data:` frames,
per-subscriber backpressure with slow-consumer disconnect, prompt reclaim.

## Source evidence
- Pinned OpenCode `95daf90670b7c039c436c85537da5fbfe2205b41`.
- Protocol SSE endpoint `packages/protocol/src/groups/event.ts`
  (blob `a6fc5692eae7cf631678b04eb58b9615bca163c4`).
- Client SSE decode `packages/client/test/effect.test.ts`
  (blob `7bf4d26f8fecb2aa054e907e43a4fc6b9a0fff68`).
- App health probe `packages/app/src/utils/server-protocol.ts`
  (blob `27b8dc208eac32ce6a6d26bee89c3d1094b58917`) — reference only.
- App migration checklist `packages/app/V1_API_MIGRATION.md`
  (blob `2850f107400cbb511ab97a5e3035ebed6a6fb9ce`) — no ownership assignment.
- Terminal WS URL `packages/app/src/utils/terminal-websocket-url.ts`
  (blob `a32b239cc932e882c1fb5b38273c2a8560f3f90c`) — excluded here.
- Classification: discovered surface; deviation: bounded buffer + slow-consumer
  disconnect instead of unbounded retention.

## Observed scenario
Module `crates/server/src/event_stream.rs` at HEAD `248f519`: `StreamConfig`
default `{ 1MiB, 64, 15000ms }`; `encode_frame` => `"data: <payload>\n\n"`;
oversize => `TooLarge` + drop-counted, kept alive; invalid UTF-8 =>
`InvalidUtf8`, no partial; slow buffer-full => `SlowConsumer`, others unaffected;
`unsubscribe` reclaims bytes/tasks; `flushed_after_close` false post-close.
Hub is a sync harness mirror of connection-task lifecycle; live HTTP streaming
wiring stays with transport owner.

## Target boundary
`encode_frame`, `sse_headers` (text/event-stream, no-cache), `keepalive_frame`
(`:ping`, no clock data), `Hub::{publish, publish_to, unsubscribe,
buffered_bytes, live_tasks, flushed_after_close}`; byte cap
`max_buffered_frames * max_frame_bytes`; payload semantics owned by INT-008.

## Tests
Frozen: `crates/server/tests/event_stream.rs`
SHA-256 `024eb5e21cf1c09eb69c376507ac54fbe7e69442e05371a503f58eef68978d39`.
RED: compiling suite failing on missing event-stream module.
GREEN (2026-09-15, HEAD `248f519`):
`cargo test -p opencode-rk-server --locked --test event_stream`
=> 5 passed, 0 failed.

## Decisions
No code change; stub scan clean; fixture-only payloads; no user DB writes.

## Remaining unknowns
Live `GET /events` transport wiring to Hub bounds (transport owner).
