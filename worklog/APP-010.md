# APP-010 — Installed binary and naming contract

Claim: ses_f42269ebaffejbo6GG4ng10o5V, scratchpad worklog/APP-010.md.
Prior row held by ses_prod_19a (in-progress) with no worklog file, no lane
branch, no APP-010 commits, no matching procs at rev 157a67a/e006f88.
Reclaimed with that evidence, re-claimed by this session.

## Source evidence
- Task card `tasks/completion/local.json:13`: paths
  `crates/cli/Cargo.toml`, `scripts/install-opencode2.sh`,
  `scripts/install-opencode2.ps1`. Those `install-opencode2.*` paths are
  stale duplicates: canonical identity is `oc2` since commit b58384c
  ("rename: packaged binary identity opencode2 -> oc2").
- `crates/cli/Cargo.toml:10-18`: bins `oc2` + `opencode-rk` compat alias,
  comment points at `scripts/install-oc2.*` (consistent).
- `crates/cli/src/install_commands.rs:18,20,165-167`: `BINARY_NAME="oc2"`,
  `LEGACY_BINARY_NAME="opencode-rk"`, `packaged_output_names_oc2` guard.
  Guard is live code but dead in production: no caller outside its own
  tests (see REVIEW_ITERATION_2.md R2-16 review).
- `scripts/install-oc2.sh:73-86`: checksum gate + legacy `opencode`
  refusal. Missing: post-install identity verification (review P1).
- Tracked scripts: both `install-oc2.*` (canonical) and
  `install-opencode2.*` (stale, `BIN="opencode2"` contradicts oc2
  identity). NOT touching the stale pair or the .ps1 in this lane
  (one-owned-file rule); recorded as follow-up.

## Owned file
- `scripts/install-oc2.sh` ONLY.

## Target boundary
- After `cp` + `chmod`, run installed `$INSTALL_DIR/$BIN --version`,
  require output contains `oc2` and not `opencode-rk`; else exit 74
  before the final version print. Mirrors `packaged_output_names_oc2`.
- Out of scope: .ps1 parity, stale-duplicate removal, release-archive
  pipeline (FIX-PACKAGING owns `scripts/validate-release.sh` NEW).

## Tests
- `sh -n scripts/install-oc2.sh` (syntax).
- Functional, disposable dirs under /tmp/opencode (real script runs):
  - T1 good stub (`oc2 0.1.0` output) installs, exit 0.
  - T2 bad stub (`opencode-rk 0.1.0` output) rejected, exit 74, binary
    still copied but flagged (document exact semantics below).
  - T3 legacy `opencode` adjacency still refuses, exit 73.
  - T4 install dir with spaces works.
- `cargo check -p opencode-rk-cli --bins` 0 errors (no CLI change; gate).

## Decisions
- Exit 74 (sysexits IOERR-adjacent, matches script's existing 64/65/66/
  69/73 typed-exit style) on identity mismatch.
- Verification runs the installed copy, not the stage copy, so it also
  covers a bad copy.

## Remaining unknowns
- None for this lane. Follow-ups: .ps1 parity, stale
  `install-opencode2.*` disposition, release-pipeline wiring.

## GREEN evidence (2026-09-20, rev 157a67a+/e006f88)
- RED: pre-fix script accepted legacy-named stub (`opencode-rk 0.1.0`
  --version output) with exit 0 (`grep -c packaged_output scripts/
  install-oc2.sh` = 0; demo log of bad-stub install exit 0).
- POST-FIX functional runs, disposable dirs /tmp/opencode/app010:
  - T1 good stub (`oc2 0.1.0 linux-x64`): exit 0, prints installed path
    + version (/tmp/opencode/app010 rerun; log inline above).
  - T2 bad stub (`opencode-rk 0.1.0`): exit 74, `FAIL: ... legacy
    name; removing .../p2/oc2`, install dir left empty
    (/tmp/opencode/app010/t2.log).
  - T3 legacy `opencode` adjacency: exit 73 refusal preserved
    (/tmp/opencode/app010/t3.log).
  - T4 spaces+non-ASCII install dir: exit 0, version prints twice
    (script + direct run) (/tmp/opencode/app010/t4.log).
  - T5 checksum mismatch: exit 65 preserved
    (/tmp/opencode/app010/t5.log).
- `sh -n scripts/install-oc2.sh`: OK.
- `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 timeout 110 cargo check -p
  opencode-rk-cli --bins`: Finished dev, 0 errors (446 pre-existing
  warnings x2 bins).
- Zero test edits (no test files touched); no CLI source change.
