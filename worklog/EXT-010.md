# EXT-010 worklog

## Claim

Own the configured-external-discovery deferred partition only: bounded
in-memory log of package/file discovery intents recorded as
`ExternalDiscoveryDeferred`, zero traversal/resolution/import/activation.

## Source evidence

- Candidate base revision: `248f519d53ed29cbf7fef7ceb2b5010fb1a4224b`.
- Pinned OpenCode commit: `95daf90670b7c039c436c85537da5fbfe2205b41`.
- `tasks/EXT-010.md`: Requirements `none`, five test obligations, cap
  `EXT10_MAX_DEFERRED=128`, excluded partitions (no npm, host,
  skill/command discovery, policy gating, persistence).
- Product boundary: `crates/tools/src/plugin_discover.rs` (wired in
  `lib.rs`), canonical tests `crates/tools/tests/plugin_discover.rs`.
  Fallback `ext_discovery_lane` pair differs by comments only; behavior
  identical.
- Classification: partial/planned upstream + resource-bounded deviation.

## Target boundary

- Product implementation: `crates/tools/src/plugin_discover.rs` only.
- Independent tests: `crates/tools/tests/plugin_discover.rs` only.
- Status/evidence: this worklog and `tasks/EXT-010.md`.

## Tests

- Canonical: `cargo test -p opencode-rk-tools --test plugin_discover`
  => 5 passed / 0 failed (`/tmp/opencode/ext_canonical.log`).
- Lane mirror: `--test ext_discovery_lane` => 5/5 (`/tmp/opencode/ext010.log`,
  `/tmp/opencode/ext_lane_green.log`).
- Frozen SHA-256 (tests, never edited):
  `ea6c2c689a988c9356d7f71d88165f6cfab258425ca71288a71aaa3fa5e166c1`.
- No RED re-run: implementation predates lease (HEAD `248f519`).

## Decisions

- No product-code change: all five obligations already GREEN
  (record/list, scope revoke, validation/caps/drain, no-loading proof,
  zero-cost-when-off).
- Runtime-optional honored: disabled path constructs nothing; tests assert
  no loader artifacts, no subprocess, no files.

## Re-verification lease (2026-09-15)

- Base `248f519`; no product-code edit. Canonical 5/5 + lane mirror 5/5 in
  combined 40/40 run, exit 0 (`/tmp/opencode/ext009_012_green.log`); lib 49/49
  (`/tmp/opencode/tools_lib.log`).
- Hashes disk vs frozen HEAD: tests disk `06c872b3...` / frozen `ea6c2c68...`;
  lane tests disk `6772dc5c...` / HEAD `5dfd101f...`; src `d7d7c5c0...`
  (HEAD-clean) vs lane `30e24833...` (header/comments delta, behavior
  identical). Fmt-only drift, GREEN. Tests untouched; HEAD values
  authoritative.
- Dedupe refused (integrator-owned; `#[path]` isolation; no `lane2` rule
  found): see EXT-011 worklog. No shared-file edits.

## Remaining unknowns

- None for this slice. Real JS-runtime import/install needs new pinned
  authority; explicitly out of scope.

## Re-verify 2026-09-16 (DB/DISC/EXT lane)
- Impl: `crates/tools/src/plugin_discover.rs` (160L), wired (plugin_discover in lib.rs). Tests: `crates/tools/tests/plugin_discover.rs` (`#[path]`, 5 fns ext010_t01..t05).
- GREEN: bounded `--test plugin_discover` => 5 passed / 0 failed.
- RED probe (isolated /tmp/opencode/redprobe/probe010 copy): early-return seq without pushing record => ext010_t01/t02/t03/t05 FAILED, 1 passed / 4 failed. Record-push load-bearing.
- Stub/IO grep clean. No edits made (worklog only).
