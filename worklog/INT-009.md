# INT-009 worklog

## Claim

Own exactly one narrow pure slice: caller-supplied project-location context
resolution (`resolve_request` / `resolve_session` + identity precedence).
No filesystem/git/DB I/O, no LocationServiceMap, no migration/persistence,
no EventV2 emission. Session routes ignore request context.

## Source evidence

- Candidate base revision: `248f519` (workdir HEAD at implement time).
- Pinned OpenCode commit: `95daf90670b7c039c436c85537da5fbfe2205b41`
  (`sources/upstream.lock.json`).
- Partition: `sources/integrations-ownership-gap.json`
  `project-location-context-resolution-boundary`:
  - source `packages/core/src/project.ts:1-136` blob
    `49e054d7cc9ae2c3229669e949089f3174242ecd`;
  - callers `packages/server/src/location.ts:11-60` blob
    `8ae5aa6e6d08b26ed063ad7841b7195b3096ff7a`,
    `packages/server/src/middleware/session-location.ts:15-67` blob
    `86fa80f75c1ed4b6113a568718b4221c25717041`;
  - spec `specs/v2/api.html:424-516` blob
    `147d24f58bb30c479acca1e4a16f393eea4fead2`.
- Constraints: same file `projectLocationConstraints`: precedence
  `[normalized-non-file-git-remote, cached-common-directory-id,
  root-commit, global]`, `fileRemoteContributesIdentity: false`,
  `resolveWritesProjectCache: false`,
  `migrationAndPersistenceOwnedByLegacyService: true`,
  `sessionAcceptsRequestContext: false`.
- Classification: partial upstream + deliberate safer deviation (pure
  caller-supplied resolution; zero I/O in this slice).
- `docs/TDD.md` 2-5 + `docs/SECURITY.md` read before product work.

## Ownership decision

- `opencode.integrations` residual stays shared; this lane owns ONLY the
  pure resolution rule above. Location runtime stays with
  `opencode.configuration-runtime` owners; INT-008 event compat untouched.

## Target boundary before RED

- Test owner: `crates/providers/tests/location_ctx.rs` (5 tests T01..T05),
  wired via `#[path]` so shared `lib.rs` untouched.
- Product candidate: `crates/providers/src/location_ctx.rs` +
  additive `pub mod location_ctx;` in `crates/providers/src/lib.rs:37`.
- No Cargo dependency change.

## Contract and resource decisions before RED

- Bounds frozen: `MAX_DIR_LEN = 256`, `MAX_WS_LEN = 64` (upstream supplies
  none; frozen RED owns values).
- Request path falls back, never errors on malformed hints; session path
  strict (`InvalidPin`); bad default => `InvalidDefault`.
- `resolve_*` sync on `&`, no threads, no clock, no I/O; output one small
  struct per call.

## Tests

- Frozen test SHA-256:
  `d24d58e1cf86a046cc6a6e1b5a023e75602990bf72dc2482d72ae56e70530ec0`.
- Frozen test git object: `9dbfa237d82ffbe4b214ead385ef1889098ec9bd`.
- Frozen command:
  `cargo test -p opencode-rk-providers --test location_ctx`.
- RED history: impl + tests landed together in `42b471c`, so compile-RED
  (`E0432` on gutted impl) is authoring feedback only. Valid behavioral RED
  reconstructed this session by stubbing both `resolve_*` to typed errors:
  `cargo test -p opencode-rk-providers --test location_ctx` =>
  **0 passed / 5 failed** (impl restored byte-identical after).
- Frozen impl SHA-256:
  `403a40e4cca99262d04b4da7ef9eca808a1152a069f1fcd590847918240c869a`.
- Focused GREEN (this session, `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2`,
  `--test-threads=2`): 5 passed / 0 failed.
- Workspace check: `cargo check -p opencode-rk-providers
  -p opencode-rk-foundation` clean (only pre-existing unrelated unused-
  import warnings in `security/hook_bus_v2.rs`, `providers/auth.rs`).
- No `#[ignore]`, no stub (`todo!/unimplemented!`), no test edit after RED.
- Purity grep: no `std::{env,process,fs,net}`, `Command`, `tokio`,
  `sqlite/rusqlite` in impl.

## Implementation

- `DirHint`/`WsHint` charset+length validation; request fallback;
  session-pin strict; identity `Remote > CommonDir > RootCommit > Global`
  with `file:` fallthrough and empty-means-absent.
- Error displays carry no hint bytes (T05 asserts absence).

## Remaining unknowns

- Real discovery (fs/git), project-cache commit semantics,
  migration/persistence, LocationServiceMap, EventV2: excluded, need
  separate task-specific pinned evidence per gap closure criteria.
- Verifier/controller acceptance external; not claimed.
