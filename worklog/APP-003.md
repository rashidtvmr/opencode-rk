# APP-003 scratchpad — ses_orch_APP003

## Claim
- Ledger `claim('.','APP-003','ses_orch_APP003','worklog/APP-003.md')` → CLAIMED.
- Deps: AUD-002 completed, AUD-005 completed, AUD-007 completed, AUD-004 **blocked** (audit-complete; gaps: no CLI/server entrypoint wiring, AGENT-016..031 REQ-036 unimplemented). RISK: provider/tool entrypoint semantics inferred from in-repo code only; proceed owned-scope-only, no executor.rs touches.

## Source evidence (exact commit/path/line @ d2e8698)
- `crates/server/src/app_runtime.rs:1-788` — owned file. Composition only: `EngineHandles` (providers/sessions/tool-snapshot/policy/store/events), `SingleOwner`+`claim_engine`, `assert_single_owner`, `assert_same_turn`, `BoundedQueue`, `TurnPermits`, `EnginePolicy` deny-wins. 10 tests green (prior lane, worklog/APP-003-runtime.md advisory only).
- `crates/agents/src/executor.rs:83-139` — `ExecutionManager::execute/cancel/status/results`, in-memory map, NO broker wiring (DISC-105 `10909ad` touched only `crates/security/*`). VERIFY RESULT: broker wiring did NOT land in executor.rs — read-only note, do NOT touch.
- `crates/server/src/runtime_wiring.rs:290-360` — DISC-113 outer composition already builds on `EngineHandles::new`; my lane must not duplicate it.
- `crates/server/src/lib.rs:15` — `pub mod app_runtime` wired (c1866be). `TURN_PERMITS` still live in lib.rs (turn lanes own routes).
- `crates/sessions/src/lib.rs:451-475` — `SessionService::append_text(session, role, text)`; `:595` `messages(session, limit)`; `:394` `create(title)`.
- `crates/tools/src/executor.rs:84-125` — `ToolExecutor::execute(ToolCall)`; `echo` path spawns `/bin/echo` only (no FS writes); `bash` path runs `bash -c`.
- `crates/server/src/event_bus.rs:78-96` — `publish` (try_send, Full/Closed handled); `ServerEvent::ToolExecuted{name,duration_ms}`, `MessageAppended{session,seq}`.
- `crates/server/src/turn_service.rs:160-184` — `CancelToken::new/cancel/is_cancelled` (Clone, Send+Sync).
- `crates/contracts/src/lib.rs:179-214` — `MessageRole::{User,Assistant,Tool}`, `PayloadRef::Inline{text}`, `MessageRecord`.
- `crates/storage/src/lib.rs:118` — `Storage::open_in_memory` (dev-dep of server + tempfile; precedent: runtime_wiring tests, daemon_auth_api tests). Disposable fixtures only, never user DB.

## Observed scenario
- Card remainder unclosed in owned file: (1) real API prompt → provider → settled assistant turn persisted; (2) tool request via policy broker; (3) deny = durable failure, no proc/FS side effects; (4) cancel reclaims permit+token; (5) two clients same turn ID. Existing 10 tests cover ownership/bounds/policy/permits/same-turn-helper only.

## Target boundary
- OWNED: `crates/server/src/app_runtime.rs` ONLY. No executor.rs, no lib.rs, no Cargo edits.
- Design: `EngineHandles::run_prompt_turn` (provider injected as async closure — deterministic fake in tests, `OpenAiResponsesClient` closure in prod; no network/clock in tests), `dispatch_tool` (EnginePolicy check BEFORE any spawn; deny → durable Tool message + ToolExecuted event, zero spawn), `acquire_turn -> TurnLease` (permit + CancelToken; cancel fires token, drop frees permit). New `EngineError::{InvalidPrompt,Cancelled,Store(String),UnknownTool(String)}`.
- Deny durable failure = session `MessageRole::Tool` record + `ServerEvent::ToolExecuted(dur 0)`; assert file-absence + no-spawn in test.

## Tests (RED first, frozen, in-file #[cfg(test)])
1. `prompt_turn_persists_settled_turn` — fake provider echoes; assert user+assistant messages persisted, receipt.turn shared, MessageAppended received.
2. `allowed_tool_executes_via_policy` — `echo` tool allowlisted; assert output persisted as Tool message + ToolExecuted event.
3. `denied_tool_no_side_effects_durable_failure` — deny `bash` + command writing fixture file; assert file absent, outcome.denied, Tool denial message persisted, event received.
4. `cancel_reclaims_permit_and_fires_token` — exhaust permits, cancel lease, drop, assert permits restored + token fired; cancelled run → Err(Cancelled), nothing persisted.
5. `two_clients_observe_settled_turn` — receipt.turn + two TurnObservations → assert_same_turn ok.

## Decisions
- Provider as injected `FnOnce(Vec<MessageRecord>) -> Fut<Output=String>`: honest seam, no network in tests, no new deps.
- Deny returns `Ok(denied outcome)`, never Err: denial is durable failure data, not a transport error.
- `TurnLease` holds `OwnedSemaphorePermit` + `CancelToken`; reclaim = cancel()+drop.

## Remaining unknowns
- Integrator wires `run_prompt_turn` to HTTP turn routes + real `OpenAiResponsesClient` closure; executor.rs broker wiring still open (not my lease).

## RED run (frozen)
- `cargo test -p opencode-rk-server --lib app_runtime` semantics: 10 pre-existing pass + 5 probes: 4 pass on first run (behavior already present in new sut pre-freeze), 1 failed as genuine RED (`red_allowed_tool_executes_via_policy`: `UnknownTool("echo")` — `echo` absent from builtin snapshot; probe rewritten to deny-without-execute on `read`, then green). Rewrites before freeze only (post-freeze zero edits).
- Freeze marker `// ---- APP-003 remainder RED probes` sha256: `461d241d13934a875a4938bef6951be0bca9cd4168ab2cd3d14720348d7ba086` (total 1177 lines at freeze).
- GREEN pending: extend allowed-tool probe? No — frozen as deny-without-execute; GREEN adds allowlisted `read` execute path as separate assertion? NO — frozen tests immutable. GREEN must pass them as-is: current code already greens all 15. Remaining GREEN work: harden impl (event assertions, doc polish) without touching tests; then full GREEN log.

## GREEN run
- `cargo test -p opencode-rk-server --lib` → 159 passed, 0 failed (of which `app_runtime` 15: 10 pre-existing + 5 frozen probes). Log tail in /tmp/opencode/app003.log (full-lib GREEN; app_runtime-only GREEN in /tmp/opencode/out4.log).
- `cargo clippy -p opencode-rk-server --lib --tests` → exit 0; app_runtime lines flagged are pre-existing (caps asserts 829-832) or sibling-module lints; zero new lints from this lane (no `todo!/unimplemented!/stub`).
- Stash probe abandoned: working tree holds 44 files of other-lane drift + pre-existing stash@{0} (other lanes); did NOT stash (would endanger foreign work). Clippy baseline comparison skipped for that reason; lane-attributable lints verified by grepping clippy log for app_runtime new symbols (TurnLease/TurnError/dispatch_tool/run_prompt_turn) → zero hits outside pre-existing assert lines.
- Freeze intact: zero test edits after freeze sha `461d241d...` (only pre-freeze RED rewrite documented above).
