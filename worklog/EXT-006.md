# EXT-006 worklog

## Claim

Own broker-gated scoped execution per `tasks/EXT-006.md`: capability check -> op check -> args bound -> cancel -> broker assert -> one bounded in-memory write. Deny leaves zero side effects.

## Source evidence

- Candidate revision: `248f519`.
- Pinned OpenCode commit: `95daf90670b7c039c436c85537da5fbfe2205b41` (`sources/upstream.lock.json`).
- `sources/disc-003-reconciliation.json` finding `opencode.extensibility` (partial): lifecycle + permission runtime exist; external activation deferred (task lines 23-31).
- Caller precedent (trusted review only): `packages/opencode/src/tool/code-mode.ts:1-310` + `packages/codemode/src/tool-runtime.ts:1-806` filter MCP tools through permissions + before/after hooks. Lane takes permission-filter shape only; hook mutation excluded (frozen EXT-008).
- Isolated lane files: `crates/tools/src/ext_scoped_exec_lane.rs:1-277`, `crates/tools/tests/ext_scoped_exec_lane.rs:1-184` (`#[path]` include, no shared `lib.rs` edit).
- Duplication found: `crates/tools/src/plugin_scoped_exec.rs` byte-identical to `ext_scoped_exec_lane.rs` (`diff` => no output); wired in `crates/tools/src/lib.rs:42`. `crates/tools/tests/plugin_scoped_exec.rs` differs only in `#[path]`/mod name. Both suites pass 5/5. Canonicalization pending: keep one, remove other via integrator.

## Target boundary

- Tests: `crates/tools/tests/ext_scoped_exec_lane.rs` only (committed).
- Product: `crates/tools/src/ext_scoped_exec_lane.rs` only (committed).
- No edit made by this lane: tree clean at start; worklog-only change after. `lib.rs`/`Cargo.toml`/schemas untouched.

## Tests

- Frozen SHA-256 (tests): `b6f50d9119d7f5562f5bd4a43e62bb229059158fc1c46023c63557749dd1fc78`.
- Frozen impl SHA-256: `37f08cdada38df46157fab67b45bdf78fa80e54c3609e30de6d669f498cfee1b` (identical for `plugin_scoped_exec.rs` duplicate).
- RED: missing (same gap as EXT-005: shipped together in `248f519`, no 0/5 RED receipt on file).
- GREEN (this lane): `cargo test -p opencode-rk-tools --test ext_scoped_exec_lane` => 5 passed / 0 failed (`ext006_t01..t05`). Mirror `plugin_scoped_exec` also 5/5.
- No-stub: no `todo!/unimplemented!/stub`; no fs/net/process/thread/tokio/statics in lane module. `PermissionBroker` has no default-allow impl; Debug impls log `args_len`/`bytes_out_len` only, never bytes.
- Order verified by tests: unknown cap => zero broker calls + sink identical (T02); deny => 1 broker call + empty sink + fingerprint unchanged (T03); oversize/cancel/revoked => zero broker calls (T04); overflow atomic, pre-existing entries unchanged (T04).

## Decisions

- `bind` revalidates EXT-005 rules (name charset, version==1, <=16 caps, dup rejected) and snapshots caps; revocation caller-driven via `revoke()`.
- Sink: `MAX_SINK_ENTRIES=256`, `MAX_SINK_BYTES=65536`, key `{cap}:{op}`; overwrite path checks byte cap before write; `MAX_ARGS_BYTES=MAX_RESULT_BYTES=4096`.
- Cancel `AtomicBool` checked before broker call and again before sink write.

## Remaining unknowns

- RED receipt missing; same controller decision needed as EXT-005.
- Crate/path mismatch: task wants `crates/ext/src/scoped_exec.rs` + `cargo test -p opencode-rk-ext`; lane lives in `opencode-rk-tools`. Plus intra-crate duplicate (`ext_scoped_exec_lane` vs `plugin_scoped_exec`) needs integrator pick.
- Verifier acceptance external, not claimed.

## DEDUP lease (this session)
- `ext_scoped_exec_lane.rs` converted to re-export shim over canonical `plugin_scoped_exec.rs` (were byte-identical `37f08cda`). Frozen `#[path]` tests untouched; both 5/5 green. See `worklog/EXT-DEDUP.md`.

## Re-verify 2026-09-16 (DB/DISC/EXT lane)
- Canonical impl: `crates/tools/src/plugin_scoped_exec.rs` (286L), wired `lib.rs:42`. Twin `ext_scoped_exec_lane.rs` (same header/content). Tests: `plugin_scoped_exec.rs` + `ext_scoped_exec_lane.rs` (`#[path]`, 5 fns each).
- GREEN: bounded `--test plugin_scoped_exec` + `--test ext_scoped_exec_lane` => 5/5 each.
- RED probe (isolated /tmp/opencode/redprobe/probe001 copy): `if !broker.assert(req)` => `if false && ...` => ext006_t03 FAILED (Ok vs Err(Denied)), 4 passed / 1 failed. Broker-deny guard load-bearing.
- Stub/IO grep clean. No edits made (worklog only).
