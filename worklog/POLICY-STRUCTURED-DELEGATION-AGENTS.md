# POLICY-STRUCTURED-DELEGATION-AGENTS

- Claim: `in-progress`; session `ses_f2e01057cffey1GLUs75okSXLd`.
- Owned file: `AGENTS.md`.
- Source evidence: `AGENTS.md:1-207`; existing ownership, TDD, security, memory, convergence, claim, and landing safeguards remain authoritative.
- Target: add a MUST-level, self-contained structured prompt contract and reusable template without weakening existing rules.
- Observed conflict: none. The existing policy requires worker context and safeguards but does not require a complete prompt schema.
- Tests: docs-only; planned `git diff --check` and final item-by-item review.
- Decisions: require one declared task type; prohibit mixing implementation with independent verification; permit explicit N/A only where a RED/GREEN phase is genuinely inapplicable.
- Remaining unknowns: remote branch tracking may require explicit `git push origin HEAD:docs/structured-subagent-prompts`.
