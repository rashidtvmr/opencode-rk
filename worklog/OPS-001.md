# OPS-001 worklog

## Claim

Pure admission ledger + disabled-cost attestation + explicit spill, GREEN. No edits this session; only verification.

## Source evidence

- `tasks/OPS-001.md` contract; PLAN.md 9; `config/resource-targets.json` (256/1048576/65536/262144); BASE-003/BASE-006 precedents consumed, never reopened.
- Impl `crates/foundation/src/resource_ledger.rs` (`admit`, RAII `Reservation`, `attest_disabled`/`attest_single_enabled`, `spill_tool_preview`).
- Tests `crates/foundation/tests/resource_ledger.rs` via `#[path]` include.

## Observed scenario

- Target boundary: owned pair only. `lib.rs` already `pub mod resource_ledger` (foundation writer owns; no wiring need).
- Workdir diff vs HEAD `248f519`: rustfmt-only (struct-variant layout, chain splits); semantics identical.

## Tests

- Src SHA-256: `b72c90179ba9400a547a9d05bee67b4ca8ad57726a194e07f1680db38fae5ccf`
- Tests SHA-256: `40d3e10795a6d7f2168f584cb6e0dd766ae2a301077a5290ffd463a94b103eb0`
- RED: prior lane reconstruction (stubbed admit): T03+T04 fail. Not re-broken per no-stub policy.
- GREEN this session (`CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 120`, `--test-threads=2`): 5 passed / 0 failed.
- Purity: no `tokio`/`sqlite`/`SystemTime`/`thread::spawn` in src (`std::fs` only in spill to caller dir). No `todo!`/`unimplemented!`.
- `cargo check -p opencode-rk-foundation`: clean (1 pre-existing dead_code lib warning).

## Decisions

- No code change. No test edit. No wiring request.

## Remaining unknowns

- Whole-tree RSS on fixture machine: outside slice. Verifier acceptance separate.
