# TUI-011-INSTALLER-REAL-ASSET — one-file implementation candidate

## Claim and boundary

- Session `ses_f3c4de578ffelQv59xDXmOs03B`; implementation branch `lane/TUI-011-INSTALLER-REAL-ASSET` at frozen test commit `cc3ebaa3c2f14c2070565cafc0f970f7cd887191`; owned product path **only** `scripts/install-oc2.sh`. Separate test author owns `tests/e2e/native_archive_install.rs`; frozen SHA-256 `0da0e11a1fa0e562e6057f1fdb2568c7a3ea7dee20680ceb68e3f34c516e7932` must not change.
- Parent TUI-011 remains open; proposed child is off-plan and cannot be marked accepted. `python3 tools/validate_repository.py` currently fails 51 pre-existing backlog errors; convergence has 83 findings on original checkout. Integration ownership must reconcile these and the 109-commit gap to origin/main before merge.

## Evidence at exact source revision

- `9cf998a:crates/cli/build.rs:26-34`: Mac native binary needs `@loader_path/../lib`; Linux `$ORIGIN/../lib`. A source-built Mac executable SHA-256 `1255392b8114c17a730dbd8354a4d07756d1dac1697c32bff96ad0eb7047e7cd` and real dylib SHA-256 `5e0265d4b053004fe78974562561e10e46567cca96697f100e3288653163330e` already passed installed-layout loader 1/1.
- `cc3ebaa:scripts/install-oc2.sh:16,224-267`: 1 MiB expanded limit rejects the checksum-verified real two-member Mac archive of 71,814,584 expanded bytes (archive SHA-256 `4d7a0ded5f3e0610b7cf8eb8b168404225f11827d81be7c9d2ce614379989131`). Specifically `tar -xOf | head -c 1048577` closes early; tar writes a broken-pipe error to `probe.err`, and line 230 exits 65 before the explicit size comparison.
- `cc3ebaa:scripts/install-oc2.sh:269-310`: staged identity invokes `$stage/oc2 --version` and `--help` before the native dylib is placed under the sibling `../lib` directory; the loader cannot resolve it in that stage layout even when the size bound is fixed.
- Test author separately compiled and ran frozen new real-archive test RED, then this implementer independently repeated `rustc --edition 2021 --test tests/e2e/native_archive_install.rs` (exit 0), `--list` (1), and the unchanged test with explicit archive/checksum/script under isolated HOME/TMPDIR: exit 101 with one failure on installer exit 65 in 0.18s. Existing frozen Python installer suites passed 8/8 at the same source base.

## Observable contract and resource bounds

- Accept checksum-valid native archives for the supported platform up to a finite expanded payload large enough for the real assets; keep the archive-member limit, per-member stream checks, type/traversal rejection, and fail before destination writes on invalid/missing/oversized members.
- Stage the verified binary and its dylib into disposable sibling `bin/` and `lib/` directories before either identity check, then install both atomically with existing rollback semantics. Maintain exit 65 for invalid archive, 74 for bad identity/destination, preserve unrelated user data and existing installer security tests. No `DYLD_*`/`LD_LIBRARY_PATH` required to run installed binary.
- Explicit chosen expanded maximum: 128 MiB aggregate (134,217,728 bytes), at most four archive members and two regular files; probing and extraction stream with bounded buffers. Installation uses owned `mktemp` staging and destination temps, cleanup on exit/trap. No user OpenCode DB or credentials in tests.

## Remaining

- Product edit: **only** `scripts/install-oc2.sh` changes the expanded ceiling from 1 MiB to 128 MiB and relocates the two checked regular files into `$stage/bin/oc2` and `$stage/lib/libopentui.dylib` (or `.so`) before staged `--version`/`--help`. It leaves checksum, archive count/type/traversal, transaction rollback and uninstall guards intact. This is a proposal-only implementation candidate.
- GREEN on unchanged frozen test SHA above: `rtk rustc --edition 2021 --test tests/e2e/native_archive_install.rs -o <approved-temp>/tui011-frozen-red-test` compiled; `--list` discovered 1; `rtk env -i PATH=/usr/bin:/bin:/usr/sbin:/sbin HOME=<approved-temp> TMPDIR=<approved-temp> OC2_REAL_ARCHIVE=<real-archive> OC2_REAL_CHECKSUM=4d7a0ded5f3e0610b7cf8eb8b168404225f11827d81be7c9d2ce614379989131 OC2_INSTALLER_SCRIPT=<candidate-script> <test>` exited 0, **1/1** GREEN in 0.51s; no DYLD_* environment.
- Existing immutable bootstrap suites `tests/bootstrap/test_tui011_installer_native_closure.py` and `test_tui011_installer_security.py`: `rtk env TMPDIR=<approved-temp> python3 -m unittest ...` exited 0, **8/8** GREEN in 0.485s. `rtk sh -n scripts/install-oc2.sh`, `rtk git diff --check` exited 0; frozen test SHA unchanged.
- Additional disposable negative resource probe: create a two-member, checksum-valid gzip tar with 65 MiB of zeros in each regular member (130 MiB expanded, 595,064 bytes compressed) using a 64 KiB generator in an owned temp root. The candidate installer exited 65 with `expanded payload exceeds 134217728 bytes` and left neither binary nor library/destination; test completed within 45s. This is an ad-hoc diagnostic, not a frozen test.
- Limits: four members and 128 MiB aggregate expanded payload. macOS checksum-valid real archive is 16,939,157 compressed / 71,814,584 expanded bytes; installer test + binary `--version` both pass. Test process and data stay inside disposable roots; no user DB accessed. One quick std-only Rust compile, 0.51s behavioral GREEN, 0.485s regressions; no Cargo build for this script change.
- Remaining: independent verifier should repeat exact frozen hash, positive and negative checks. Linux installed proof requires fresh exact-revision Nomad run with bounded persisted logs. `validate_repository.py` still fails 51 baseline errors and `origin/main` is 109 commits behind the candidate base. This candidate cannot merge/claim parent acceptance until trusted reconciliation and installed TUI/Web/approval/provider journeys pass. A successful script does not prove signed provenance or full release.
- Independent verifier `ses_f217abd2bfferQRgAbC0R3kY92` on exact `2f169298` repeated the frozen real Mac test **1/1** and existing installer suites **8/8**, verified 130 MiB oversize exit 65/no install, and pushed a blocked receipt at `origin/verify/TUI-011-INSTALLER-REAL-ASSET` commit `6962e8f21ff3c6d474169e660af223eeea895215`. Frozen test hash and script identity remained unchanged.
- **New real Linux RED**: Lenovo Nomad allocation `1d6bf109` built source archive SHA-256 `70914841bb63de66f93c7470855f6f68068a86120cb829a002483b4229271c41` from candidate `2f169298` (3 GiB job, CARGO_BUILD_JOBS=1). Cargo build exited 0 in 1m58s, `readelf -d` printed `RUNPATH [$ORIGIN/../lib]`, and a disposable `bin/../lib` execution printed `oc2 0.1.0-alpha.1` without LD_LIBRARY_PATH. Built Linux `oc2` was **118,773,880 bytes**, native `libopentui.so` **26,583,552 bytes**; combined **145,357,432 bytes**, above this candidate's 134,217,728-byte bound. Checksum-valid real Linux archive SHA-256 `8dc056db07e4b4e1778b590831b5e984edc6ea1ea4fec1efea3a29ffcf1e3d40`, 33,205,859 compressed bytes. Installer printed `expanded payload exceeds 134217728 bytes`, exited 65, and the fail-closed Nomad job stopped. Durable bounded receipt: `/private/var/folders/b0/dj81nc_j2yq2bkmg0yd2sgyc0000gn/T/opencode/linux-native-2f16929/nomad-1d6bf109-receipt.txt`, SHA-256 `2a02745041d8b3b35335c55a2079837beac715ec34dbc6c7b850d16ceadee810` (106,952 bytes). Mac source server PID 38335 was stopped after checksum-verified download; Nomad job explicitly stopped after exit. Linux installed version/entrypoint remains RED. A fresh independent compiling Linux-size RED and real native archive retention are required before altering the cap again; 128 MiB is inadequate for this platform.
- **Exact-source Mac rebuild after script integration**: fresh `git archive 2f169298` snapshot in approved temp `native-installer-proof-2f16929/`, real fixture dylib SHA-256 `5e0265d4b053004fe78974562561e10e46567cca96697f100e3288653163330e` copied only into disposable build path. Bounded offline `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 cargo build --locked -p opencode-rk-cli --bin oc2 --features native` exited 0 in **64.7s**; 146,795 stderr bytes concurrently drained to a 16 KiB tail. Built Mac binary SHA-256 `a1257ace6ccde1fbcdde205d7b46e498d0d8fc15c3fa91318bafc86c4e228d7b` (45,457,192 bytes), `LC_RPATH @loader_path/../lib` and dependency `@rpath/libopentui.dylib`. New real two-member archive SHA-256 `985344b1056f48f4c3c4989ad2f66f2f8367c5bf7fc55c33cb06b3578ecfb064`, 16,939,018 compressed / 71,824,600 expanded bytes. Unchanged frozen installer test SHA `0da0e11a...` compiled/discovered 1 and passed **1/1 in 0.52s** with explicit new archive/checksum/exact-commit script and env-clear child. Unchanged frozen loader test SHA `c0a809a28800b542613bfb79b0d9566c07c955e45cb9afe441c0a697ae99ef36` compiled and passed **1/1 in 0.06s** with exact-revision binary/fixture, no DYLD_* in its installed-layout run. This proves Mac loader+installer only; not the no-subcommand TUI, web, provider, persistence, or release.
