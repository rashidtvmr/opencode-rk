# WEB-012 worklog — plugin/app/tool chooser with approvals

Claim: pure chooser/approval/selection model implemented; frozen suite GREEN 5/5.
Source evidence:
- Upstream pins unchanged. Dependency: native ToolRegistry + approvals policy. Server exposes real registry read-only via `GET /api/capabilities` (`crates/server/src/lib.rs` :115-175, deterministic id order, `available_for_web_turn:false` with reason — no faked execution). Card boundary: selection/approval write-path stays disabled until web turn owns tool execution.
- Impl: `crates/server/src/web_tool_chooser.rs` — `Registry::search` (:118, deterministic, capped), `Selection::select` (:146, idempotent; disabled/denied/unknown rejected, nothing substituted), `turn_contract` (:182, registry order, enabled-only), `ApprovalQueue::{request,resolve,cancel,cancel_all}` (:223-298, no-approval tools + denied/disabled never mint; focus `return_to` restored), `snapshot`/`rehydrate` (:333-410, ids+decisions+seqs only — no labels/permissions/secrets; atomic fail; oversize pre-parse reject). Bounds: caps 64, search 32, selection 8, pending 16, snapshot 4KiB, audit 64 (oldest drops).
Target boundary: `crates/server/src/web_tool_chooser.rs` (owned, `#[path]`-included, not in `lib.rs`).
Tests: `crates/server/tests/web_tool_chooser.rs` SHA-256 `f69d4a34…bbc60` (this rev). Frozen — not edited.
Decisions: std-only; denied tools cannot escape via approvals; rehydrated disabled/denied ids never reactivate.
Remaining unknowns: web turn tool-execution + approval-owned flow (T01/T03/T05 end-to-end); verifier decides.
