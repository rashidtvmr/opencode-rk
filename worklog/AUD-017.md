# AUD-017 scratchpad — Controller acceptance, leases, TDD and worker ownership

Claim: AUD-017 claimed by ses_f420f8732ffe5Rv2Nxle3iSLgV, scratchpad worklog/AUD-017.md.
Prior report inspectedCommit 1128738b (stale). Refresh at HEAD 1614754a76648011761893b013ae08b4f5fad33d.
Owned file: sources/completion/audits/AUD-017.json ONLY. No plan/verifier/test edits.

## Source evidence (HEAD 1614754, verified by read)

- tools/ralph_loop.py:95-96 ownership_lock() returns f"feature:{task_id}" — coarse one-lock-per-task-id, no shared-path table.
- tools/ralph_loop.py:327-344 claim() same-id only — lock derived from task id, prefix collision never computed.
- tools/ralph_loop.py:347-351 ready_queue() batch + claim; :677 batch loop; :718 ThreadPoolExecutor lanes; serial integrate :740-756 (record_result+heartbeat/release per lane, sequential).
- tools/ralph_loop.py:421-448 finalize_worktree() ff-only merge; branch kept for integrator on divergence.
- tools/ralph_loop.py:601-608 record_result() accepted-before-integrated BUG STILL PRESENT: :602 state.status="accepted" set BEFORE :604 finalize_worktree; :607-608 ff-fail keeps accepted with last_error "accepted but not integrated".
- tools/ralph_loop.py:650-652 NEW since 1128738b: OOM guard `limit = min(limit, 2)` serializes lanes regardless of config/CLI (config maxConcurrentLanes also cut 6->2, :65).
- tools/plan_model.py:44-54 NEW families SYNC/RUN/ACP/WSX/SDK/HEAD ranks (DISC-003 manifest mirror); :96-112 ready() all-deps-accepted, blocked deps hold dependents out; :201-213 rank-gate synthesis (was :193-205 at 1128738b, shifted +8).
- tools/lane_gate.py:31-44 UNCHANGED: hardcoded storage v2 lanes only (gc/admission/execution/approvals/snapshot/import/quota/retention + 4 test lanes).
- tools/completion_scheduler.py UNCHANGED (260 lines): :105-106 overlaps(); :136-140 validate_candidate one-file grant; :143-156 validate_proof independent verifier + frozen hash + obligations; :158-260 run_rolling refill-on-completion; caps :169-175 capacity<=20/max_unverified<=20/max_attempts<=3; single verify/integrate pipeline :211-216; post-integrate rerun :197-198; cancel/join :253-260.
- ralph.json: AUTO-001..007 ALL status=accepted, each 5 obligations T01-T05 (prior report said T01-T03 — corrected).
- sources/upstream.lock.json: opencode pin 95daf90670b7c039c436c85537da5fbfe2205b41, vendored false.

## Observed scenario

- `timeout 120 python3 -m unittest discover -s tests/bootstrap -p "test_auto*.py"` → Ran 16 tests, FAILED (failures=6): plan now 258 stories vs frozen 219 expected; SESS-001 in M0 ready set; validator flags stale ownership-gap notes. Pre-existing frozen-test drift, NOT caused by this audit.
- `timeout 120 python3 -m unittest tests.bootstrap.test_auto003_leases tests.bootstrap.test_validate_plan` → Ran 18 tests, FAILED (failures=2): backlog-exhaustion-in-validation + every-lower-rank-is-dependency acyclicity.
- `python3 tools/completion_plan.py --check` → SPEC OK: additions=109, legacy=258, scenarios=545; NOT product acceptance.

## Target boundary

Refresh report only: inspectedCommit->1614754, re-traced lines, corrected obligations, recorded test counts, kept gaps as gaps, repair children open. No acceptance.

## Decisions

- Verdict stays audit-complete, no acceptance claimed.
- Repair children carried + 1 new: frozen auto-controller/bootstrap tests drifted vs 258-story plan (record_result/ready/validator expectations stale).
- OOM cap-2 noted as observation (serializes rolling claim; matches AGENTS.md 8GiB budget) — not a repair child, documented in trace.

## Remaining unknowns

- Real harness adapter/roles binding (COORD-001..008) still open — TrustedAdapter is Protocol only, launches no agents. Out of this lane.
