# OPS-004 worklog

## Claim

Offline install version/channel parse + explicit-input path derivation, GREEN. No edits this session; only verification.

## Source evidence

- `tasks/OPS-004.md` contract; opencode `95daf90` `installation/version.ts:1-8` (blob `25d9cd99`), `global.ts:10-85` (blob `a192a4b4`, derivation shape only), `npm-config.ts:12-40` (blob `896bb848`, excluded boundary).
- Impl `crates/foundation/src/install_meta.rs:63` `parse_version`; caps raw<=128/label<=64/base<=1024/derived<=2048; `Unsafe`/`UnknownChannel`/`BadBaseDir` typed.
- Tests `crates/foundation/tests/install_meta.rs` via `#[path]` include.

## Observed scenario

- Target boundary: owned pair only. `lib.rs` already `pub mod install_meta`.
- Workdir diff vs HEAD `248f519`: rustfmt-only array wrap; semantics identical.
- Excluded (creation/env/locks/npm-config/network) absent from src.

## Tests

- Src SHA-256: `1548fa81dc663f238a657a2164f4608ef6339b90f19c41b639847afda35a680b`
- Tests SHA-256: `d613884087cfa8efd15db99ee2cb66f3ed75d38e8291742a8604a9a102b2b802` (unchanged from prior lane; tests file untouched in workdir diff)
- RED: prior batch. Not re-broken per no-stub policy.
- GREEN this session (bounded cmd, `--test-threads=2`): 5 passed / 0 failed.
- Purity: zero `std::fs`/`std::env`/`Command`/tokio refs; no `todo!`/`unimplemented!`.
- `cargo check -p opencode-rk-foundation`: clean.

## Decisions

- No code change. No test edit.

## Remaining unknowns

- Unreviewed installation remainder needs future owned slice. Verifier acceptance separate.
