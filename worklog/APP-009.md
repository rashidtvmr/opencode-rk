# APP-009 (shutdown.rs lane)

Session: ses_orch_APP009. Owned file: `crates/cli/src/shutdown.rs` ONLY.
RC-03 owns service_commands.rs — NOT touched.

## Claim
`cc.claim(...)` OK 2026-09-18: APP-009 in-progress, session ses_orch_APP009,
scratchpad worklog/APP-009.md. Deps APP-002 + TUI-002 both completed in ledger.
Base HEAD `d2e8698` confirmed (`git rev-parse HEAD`); `git pull --rebase`
refused (44 modified + 52 untracked files from other lanes in tree — untouched).

## Source evidence
- Card `tasks/completion/local.json` APP-009: service controls/shutdown/
  terminal restoration; 5 tests (close-one-of-two, SIGINT/SIGTERM+panic
  restore, suspend/resume, stop drain/cancel, repeated start/stop no-leak).
- `crates/cli/src/shutdown.rs:1-282` (tracked, in HEAD): ShutdownReason
  (Sigint/Sigterm/Panic/Explicit, `shutdown.rs:24-33`), RestoreReceipt
  (`:60-67`), RestoreGuard<F: FnOnce()> + Drop (`:96-154`), SuspendMarker
  (`:159-190`), 6 in-file tests (`:192-282`).
- RC-03 VERIFIED landed: `decide_singleton_launch` at
  `crates/cli/src/service_commands.rs:387-399` + 5 rc03 tests (`:567-640`).
  Close-one-of-two (`:482-499`), drain/cancel (`:422-465`), no-leak ledger
  (`:537-565`) all in service_commands.rs — RC-03 owned, not duplicated here.
- `crates/cli/src/terminal_host.rs:185-248` TUI-002 RestoreFlag/RawModeGuard;
  guard-unification doc (`:39-48`) names the abort/SIGKILL ceiling shared
  with shutdown::RestoreGuard. `mod shutdown` wired in `main.rs:45`.
- docs/TDD.md (RED must compile+fail; freeze hash; verifier decides),
  docs/SECURITY.md (no secrets/syscalls in loop; disposable fixtures).
  This module: `#![forbid(unsafe_code)]`, std-only, no syscalls/handlers/
  FDs — no security surface.

## Observed scenario
Prior lane worklog/APP-009-shutdown.md holds the mechanism RED (empty Drop:
4 pass/2 fail) → GREEN (6/6). This lane closes card remainder mapped to
shutdown.rs: SIGINT/SIGTERM drop-restore was NOT pinned (drop_restores uses
Explicit only; Sigint/Sigterm appear only in suppressed paths), and no
repeated arm/drop cycle test existed. stop-drains / no-leak-FDs / close-one-
of-two live in RC-03's file (cited above) — out of scope by lane boundary.

## Target boundary
Owned file only. Added 3 regression tests using existing API (no prod-code
change needed — implementation already generic over reason):
- sigint_drop_restores / sigterm_drop_restores (card test 2, signal paths)
- repeated_arm_drop_cycles_restore_exactly_once (card test 5, guard half:
  N cycles → N restores, still armed semantics, no double-restore)
Card tests 1/4/5-resource-half: RC-03 service_commands.rs (see evidence).
"Unbounded logs": neither module retains logs (bounded queues/counters
only) — N/A, no log buffer exists to bound.

## Tests
Mandated: CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 180 cargo test
-p opencode-rk-cli --bin opencode-rk shutdown → /tmp/opencode/app009.log
(see Completion section for results + frozen hash).

## Decisions
- No prod-code change: Drop/suspend/marker already correct; churning API
  (e.g. Guard::resume) for RED theater rejected — lazy ladder rung 1.
- 3 added tests green-first-run, frozen at add-time; mechanism RED evidence
  inherited from prior lane (cited). Zero edits post-freeze.
- No stubs/todo!/mock.

## Completion
- `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 180 cargo test
  -p opencode-rk-cli --bin opencode-rk shutdown` → 9 passed, 0 failed
  (287 filtered out), log /tmp/opencode/app009.log.
- Frozen sha256 `08eb082d...` (full: see test run). Zero test edits post-add.
- Ledger `completed` with evidence note above.

## Remaining unknowns
- None in slice. Signal-handler → reason wiring + real FD/listener release
  belong to caller/integrator (documented module ceiling).
