# TUI-011 one-file builder lane (cross-platform matrix)

Status: blocked on MSVC/remaining artifact-copy lanes/frozen conflicts (builder matrix complete; see Log).

## Claim

- Task: `TUI-011`
- Session: `ses_f3033b018ffefBqAN95ooRBHD2`
- Base HEAD: `4db19b3`, branch `lane/TUI-011-native-artifacts`
- Owned file: `crates/opentui-bridge/native/build_opentui.sh` (sha `6910c341f3c0c2bc8ba4c9b54ade9047da2b9ba7037f2949759cfeab6064645e` at start)
- Scratchpad: `worklog/TUI-011.md`
- Prior TUI-011 state: reclaimed to not-started by `ses_f3c4de578ffelQv59xDXmOs03B`; prior scratchpad notes (manifest/NOTICES/SBOM lanes) retained above, untouched.

## Source evidence

- `crates/opentui-bridge/build.rs:32-64` gate: macos wants `libopentui.a|libopentui.dylib`; linux `libopentui.a|libopentui.so`; windows-gnu `libopentui.dll.a + opentui.dll`; windows-msvc `opentui.lib + opentui.dll`.
- Pinned fork `packages/native/build.zig:21-30` SUPPORTED_TARGETS (ground truth from external checkout @c01292f): `x86_64-linux-gnu.2.17`, `aarch64-linux-gnu.2.17`, `x86_64-linux-musl`, `aarch64-linux-musl`, `x86_64-macos.13.0`, `aarch64-macos.13.0`, `x86_64-windows-gnu`, `aarch64-windows-gnu`. NO msvc target, NO musl rust triples in scope.
- `build.zig:513` interface `zig build -Dlibrary-target=<t> -Doptimize=ReleaseSafe [-Dmacos-sdk=...]`; outputs to `packages/native/lib/<output_name>` where output_name is `x86_64-linux|aarch64-linux|x86_64-macos|aarch64-macos|x86_64-windows|...` (build.zig:buildTarget, `../lib/{output_name}`).
- `build.zig:10-12, zon` Zig must be exactly 0.16.0.
- Host tools present: `file nm objdump otool lipo python3` yes; `readelf llvm-* mingw*` no. GNU `nm -D --defined-only` + `objdump -f/-p` work on ELF here (probed on vendored `native/lib/x86_64-unknown-linux-gnu/libopentui.so`: x86-64, NEEDED libm.so.6+libc.so.6, all 5 ABI symbols present).
- Existing arm64 artifact: `native/lib/aarch64-apple-darwin/libopentui.dylib` sha `798f30dd...eb13a91`, 6863648 B; `--verify-only` exit 0.

## Target boundary

- Extend ONLY `build_opentui.sh`: `--target <rust-triple>` mapping rust->zig, per-format verify (Mach-O/ELF/PE), atomic windows pair copy, fail-closed unknown/MSVC/foreign-host.
- No edits to artifacts.json/NOTICES/sbom.json/tests/workflows. No repo output copies; matrix outputs go to approved external `/private/.../T/opencode/tui011-build/matrix/<triple>`.
- Default (no --target) keeps arm64 behavior/output bytes identical.

## Matrix plan (one at a time, timeout 600 each)

1. x86_64-apple-darwin (zig x86_64-macos.13.0, +SDK)
2. x86_64-unknown-linux-gnu (zig x86_64-linux-gnu.2.17)
3. aarch64-unknown-linux-gnu (zig aarch64-linux-gnu.2.17)
4. x86_64-pc-windows-gnu (zig x86_64-windows-gnu, DLL+import-lib pair)
5. x86_64-pc-windows-msvc: expected NOT producible (no pinned interface) -> fail-closed proof, mark blocked.

## Decisions

- Host gate stays Darwin/arm64 (only pinned Zig archive hash known: aarch64-macos). Other hosts fail closed, no fabricated hashes.
- MSVC fails closed exit 1 with reason; unknown triples exit 2 usage.
- ELF verify: file(1) + objdump arch + nm -D ABI + NEEDED allowlist (bare *.so*, no paths) + RPATH/RUNPATH relative-only.
- PE verify: file PE32+ DLL x86-64 + import-lib ar-archive check + exports via nm, fallback objdump -x, else die.

## Log

- bash -n SYNTAX_OK; --help exit 0 (matrix usage).
- Invalid --target bogus exit 2; --target aarch64-linux exit 2.
- MSVC refusal exit 1: `x86_64-pc-windows-msvc is not producible: the pinned build.zig SUPPORTED_TARGETS lists only x86_64/aarch64-windows-gnu, no MSVC ABI entry; refusing to invent one`. MSVC stays fail-closed: no pinned source target, no compatible .lib generated; GNU output never relabeled.
- Default arm64 --verify-only exit 0 sha 798f30dd7f4fbe36d52c8834652ed7bcd7f20dfd2a1203d09cc24880eeb13a91 (bytes preserved).
- x86_64-apple-darwin build+verify exit 0 sha a7756f27becf5e0b9f25e553ad0141b76de2274c57fee6edb156c9b0ca5c9ef5 (Mach-O x86_64, lipo x86_64, @rpath, no RPATH, 5/5 ABI).
- x86_64-unknown-linux-gnu build+verify exit 0 sha 9f074adf1e3c67bb027433d44da05b9e285a37ae58cf0b2ed76504304450c79e (ELF x86_64, 6 NEEDED allowlisted, no RPATH, 5/5 ABI).
- aarch64-unknown-linux-gnu build+verify exit 0 sha e85a45710e9e181b3eb7cca877a1d9022f2210bfa1e06b7c159e734506da3b89 (ELF aarch64, 5 NEEDED, no RPATH, 5/5 ABI).
- Vendored native/lib/x86_64-unknown-linux-gnu/libopentui.so --verify-only exit 0 sha e84ced36b9f0d77069067833f1dd0edcc6324abdd5e4be662e1dccd2994b5d9a (differs from fresh matrix build; no repo copy made).
- Windows GNU probe: `zig dlltool --help` = llvm-dlltool, `-D/-d/-l/-m` supported, TARGETS include i386:x86-64, exit 0. `zig build -Dlibrary-target=x86_64-windows-gnu` installs only opentui.dll (6797824 B probe) + opentui.pdb into lib/x86_64-windows; import lib lives only as zig-out/lib/opentui.lib (101524 B ar archive). DLL export table: 431 names incl. all 5 required.
- Determinism check: .def from ALL 431 sorted exports + `zig dlltool -D opentui.dll -d exports.def -l libopentui.dll.a -m i386:x86-64` exit 0 produces 101524 B ar archive byte-identical (cmp) to zig-out/lib/opentui.lib.
- x86_64-pc-windows-gnu full script build exit 0: DLL sha 0be2efd8a77330ba23b0f5eeb094a548f5fee4b7ad7b4c6f83e0e786dadd6b15 (6800384 B, PE32+ GUI x86-64); import lib sha 1dce093459b94a4d6bd0538c4fdd1681f50534930f3d59918b765bc87a982019 (101524 B ar, 431/431 export correspondence, 5/5 required); pair --verify-only exit 0; atomic pair publish confirmed by hash equality pre/post copy.
- Owned script now: pe_exports/pe_export_def/verify_pe_dll_only + pinned zig dlltool generation of build.rs-accepted libopentui.dll.a from complete export table; strings added to required tools; no host-unpinned tool used.
- No repo artifact copies; matrix outputs under tui011-build/matrix/<triple>/out only.
- Remaining: vendor/copy lanes for matrix artifacts, macOS signing/notarization, frozen native_artifact_manifest conflicts (SHA-digit predicate :95, 64KiB dylib bound :122) - all outside this lane's authority. TUI-011 blocked accordingly.

## Vendored x86_64 macOS artifact

- Path: `crates/opentui-bridge/native/lib/x86_64-apple-darwin/libopentui.dylib`.
- SHA-256: `a7756f27becf5e0b9f25e553ad0141b76de2274c57fee6edb156c9b0ca5c9ef5`.
- Verified Mach-O x86_64, `@rpath/libopentui.dylib`, zero `LC_RPATH`, and all five required ABI exports.
- `build_opentui.sh --verify-only --target x86_64-apple-darwin` exited 0.
- Runtime execution was not claimed on the arm64 host; the Rust x86_64 target is not installed.
