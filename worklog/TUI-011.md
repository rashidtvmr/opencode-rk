# TUI-011 one-file builder lane (cross-platform matrix)

Status: blocked on MSVC/remaining artifact-copy lanes/frozen conflicts (builder matrix complete; see Log).

## Claim

- Task: `TUI-011`
- Session: `ses_f3033b018ffefBqAN95ooRBHD2`
- Base HEAD: `4db19b3`, branch `lane/TUI-011-native-artifacts`
- Owned file: `crates/opentui-bridge/native/build_opentui.sh` (sha `6910c341f3c0c2bc8ba4c9b54ade9047da2b9ba7037f2949759cfeab6064645e` at start)
- Scratchpad: `worklog/TUI-011.md`
- Prior TUI-011 state: reclaimed to not-started by `ses_f3c4de578ffelQv59xDXmOs03B`; prior scratchpad notes (manifest/NOTICES/SBOM lanes) retained above, untouched.

## Source evidence

- `crates/opentui-bridge/build.rs:32-64` gate: macos wants `libopentui.a|libopentui.dylib`; linux `libopentui.a|libopentui.so`; windows-gnu `libopentui.dll.a + opentui.dll`; windows-msvc `opentui.lib + opentui.dll`.
- Pinned fork `packages/native/build.zig:21-30` SUPPORTED_TARGETS (ground truth from external checkout @c01292f): `x86_64-linux-gnu.2.17`, `aarch64-linux-gnu.2.17`, `x86_64-linux-musl`, `aarch64-linux-musl`, `x86_64-macos.13.0`, `aarch64-macos.13.0`, `x86_64-windows-gnu`, `aarch64-windows-gnu`. NO msvc target, NO musl rust triples in scope.
- `build.zig:513` interface `zig build -Dlibrary-target=<t> -Doptimize=ReleaseSafe [-Dmacos-sdk=...]`; outputs to `packages/native/lib/<output_name>` where output_name is `x86_64-linux|aarch64-linux|x86_64-macos|aarch64-macos|x86_64-windows|...` (build.zig:buildTarget, `../lib/{output_name}`).
- `build.zig:10-12, zon` Zig must be exactly 0.16.0.
- Host tools present: `file nm objdump otool lipo python3` yes; `readelf llvm-* mingw*` no. GNU `nm -D --defined-only` + `objdump -f/-p` work on ELF here (probed on vendored `native/lib/x86_64-unknown-linux-gnu/libopentui.so`: x86-64, NEEDED libm.so.6+libc.so.6, all 5 ABI symbols present).
- Existing arm64 artifact: `native/lib/aarch64-apple-darwin/libopentui.dylib` sha `798f30dd...eb13a91`, 6863648 B; `--verify-only` exit 0.

## Target boundary

- Extend ONLY `build_opentui.sh`: `--target <rust-triple>` mapping rust->zig, per-format verify (Mach-O/ELF/PE), atomic windows pair copy, fail-closed unknown/MSVC/foreign-host.
- No edits to artifacts.json/NOTICES/sbom.json/tests/workflows. No repo output copies; matrix outputs go to approved external `/private/.../T/opencode/tui011-build/matrix/<triple>`.
- Default (no --target) keeps arm64 behavior/output bytes identical.

## Matrix plan (one at a time, timeout 600 each)

1. x86_64-apple-darwin (zig x86_64-macos.13.0, +SDK)
2. x86_64-unknown-linux-gnu (zig x86_64-linux-gnu.2.17)
3. aarch64-unknown-linux-gnu (zig aarch64-linux-gnu.2.17)
4. x86_64-pc-windows-gnu (zig x86_64-windows-gnu, DLL+import-lib pair)
5. x86_64-pc-windows-msvc: expected NOT producible (no pinned interface) -> fail-closed proof, mark blocked.

## Decisions

- Host gate stays Darwin/arm64 (only pinned Zig archive hash known: aarch64-macos). Other hosts fail closed, no fabricated hashes.
- MSVC fails closed exit 1 with reason; unknown triples exit 2 usage.
- ELF verify: file(1) + objdump arch + nm -D ABI + NEEDED allowlist (bare *.so*, no paths) + RPATH/RUNPATH relative-only.
- PE verify: file PE32+ DLL x86-64 + import-lib ar-archive check + exports via nm, fallback objdump -x, else die.

## Log

- bash -n SYNTAX_OK; --help exit 0 (matrix usage).
- Invalid --target bogus exit 2; --target aarch64-linux exit 2.
- MSVC refusal exit 1: `x86_64-pc-windows-msvc is not producible: the pinned build.zig SUPPORTED_TARGETS lists only x86_64/aarch64-windows-gnu, no MSVC ABI entry; refusing to invent one`. MSVC stays fail-closed: no pinned source target, no compatible .lib generated; GNU output never relabeled.
- Default arm64 --verify-only exit 0 sha 798f30dd7f4fbe36d52c8834652ed7bcd7f20dfd2a1203d09cc24880eeb13a91 (bytes preserved).
- x86_64-apple-darwin build+verify exit 0 sha a7756f27becf5e0b9f25e553ad0141b76de2274c57fee6edb156c9b0ca5c9ef5 (Mach-O x86_64, lipo x86_64, @rpath, no RPATH, 5/5 ABI).
- x86_64-unknown-linux-gnu build+verify exit 0 sha 9f074adf1e3c67bb027433d44da05b9e285a37ae58cf0b2ed76504304450c79e (ELF x86_64, 6 NEEDED allowlisted, no RPATH, 5/5 ABI).
- aarch64-unknown-linux-gnu build+verify exit 0 sha e85a45710e9e181b3eb7cca877a1d9022f2210bfa1e06b7c159e734506da3b89 (ELF aarch64, 5 NEEDED, no RPATH, 5/5 ABI).
- Vendored native/lib/x86_64-unknown-linux-gnu/libopentui.so --verify-only exit 0 sha e84ced36b9f0d77069067833f1dd0edcc6324abdd5e4be662e1dccd2994b5d9a (differs from fresh matrix build; no repo copy made).
- Windows GNU probe: `zig dlltool --help` = llvm-dlltool, `-D/-d/-l/-m` supported, TARGETS include i386:x86-64, exit 0. `zig build -Dlibrary-target=x86_64-windows-gnu` installs only opentui.dll (6797824 B probe) + opentui.pdb into lib/x86_64-windows; import lib lives only as zig-out/lib/opentui.lib (101524 B ar archive). DLL export table: 431 names incl. all 5 required.
- Determinism check: .def from ALL 431 sorted exports + `zig dlltool -D opentui.dll -d exports.def -l libopentui.dll.a -m i386:x86-64` exit 0 produces 101524 B ar archive byte-identical (cmp) to zig-out/lib/opentui.lib.
- x86_64-pc-windows-gnu full script build exit 0: DLL sha 0be2efd8a77330ba23b0f5eeb094a548f5fee4b7ad7b4c6f83e0e786dadd6b15 (6800384 B, PE32+ GUI x86-64); import lib sha 1dce093459b94a4d6bd0538c4fdd1681f50534930f3d59918b765bc87a982019 (101524 B ar, 431/431 export correspondence, 5/5 required); pair --verify-only exit 0; atomic pair publish confirmed by hash equality pre/post copy.
- Owned script now: pe_exports/pe_export_def/verify_pe_dll_only + pinned zig dlltool generation of build.rs-accepted libopentui.dll.a from complete export table; strings added to required tools; no host-unpinned tool used.
- No repo artifact copies; matrix outputs under tui011-build/matrix/<triple>/out only.
- Remaining: vendor/copy lanes for matrix artifacts, macOS signing/notarization, frozen native_artifact_manifest conflicts (SHA-digit predicate :95, 64KiB dylib bound :122) - all outside this lane's authority. TUI-011 blocked accordingly.

## Vendored x86_64 macOS artifact

- Path: `crates/opentui-bridge/native/lib/x86_64-apple-darwin/libopentui.dylib`.
- SHA-256: `a7756f27becf5e0b9f25e553ad0141b76de2274c57fee6edb156c9b0ca5c9ef5`.
- Verified Mach-O x86_64, `@rpath/libopentui.dylib`, zero `LC_RPATH`, and all five required ABI exports.
- `build_opentui.sh --verify-only --target x86_64-apple-darwin` exited 0.
- Runtime execution was not claimed on the arm64 host; the Rust x86_64 target is not installed.

## aarch64 Linux artifact copy @ 8fcf8ad

- Session: `ses_f3016e712ffeVUpWMbxICwi1kZ`; branch: `lane/TUI-011-artifact-matrix`; HEAD before artifact: `8fcf8ad438dc10757c4232c03f808b17a0550113`.
- Source: `/private/var/folders/b0/dj81nc_j2yq2bkmg0yd2sgyc0000gn/T/opencode/tui011-build/matrix/aarch64-unknown-linux-gnu/out/libopentui.so`.
- Destination (sole owned artifact): `crates/opentui-bridge/native/lib/aarch64-unknown-linux-gnu/libopentui.so`.
- Atomic copy used a same-directory temporary file, `fsync`, then `os.replace`; no other artifact path changed. Source, staged, and published SHA-256 all equal `e85a45710e9e181b3eb7cca877a1d9022f2210bfa1e06b7c159e734506da3b89`; size `26598960` bytes; mode `0755`.
- `file -b`: `ELF 64-bit LSB shared object, ARM aarch64, version 1 (SYSV), dynamically linked`; `objdump -f`: `elf64-littleaarch64`, `architecture: aarch64`.
- `objdump -p`: `NEEDED` exactly `libm.so.6`, `libc.so.6`, `libpthread.so.0`, `libdl.so.2`, `librt.so.1`; no `RPATH` or `RUNPATH`.
- `nm -D --defined-only`: all five required exports present: `createRenderer`, `destroyRenderer`, `getCurrentBuffer`, `bufferDrawText`, `bufferWriteResolvedChars`.
- Builder verify-only: `bash crates/opentui-bridge/native/build_opentui.sh --verify-only crates/opentui-bridge/native/lib/aarch64-unknown-linux-gnu/libopentui.so --target aarch64-unknown-linux-gnu` exited `0`; builder confirmed ELF architecture, ABI, NEEDED allowlist, no RPATH/RUNPATH, and SHA.
- `rustup target list --installed` returned only `aarch64-apple-darwin`; `aarch64-unknown-linux-gnu` is not installed. No Rust bridge native check was run; no target was installed.
- Builder syntax/help and missing-artifact fail-closed checks remain green. Prior matrix/x86 evidence above retained unchanged.
- TUI-011 remains BLOCKED, never completed: x86_64 Linux, Windows GNU/MSVC, macOS signing/notarization, and frozen artifact/sidecar issues remain outside this one-file lane.

## Vendored x86_64 Linux artifact @ 552bc16

- Session: `ses_f3011e183ffenp0hQODntJeO5F`; branch: `lane/TUI-011-artifact-matrix`; starting HEAD: `552bc16309fbe7f56d3d84cf6513eab56a47b494`.
- Source: `/private/var/folders/b0/dj81nc_j2yq2bkmg0yd2sgyc0000gn/T/opencode/tui011-build/matrix/x86_64-unknown-linux-gnu/out/libopentui.so`.
- Destination (sole owned artifact): `crates/opentui-bridge/native/lib/x86_64-unknown-linux-gnu/libopentui.so`.
- Precondition required old SHA-256 `e84ced36b9f0d77069067833f1dd0edcc6324abdd5e4be662e1dccd2994b5d9a`, source SHA-256 `9f074adf1e3c67bb027433d44da05b9e285a37ae58cf0b2ed76504304450c79e`. A same-directory temporary file was streamed, SHA-checked, `fsync`ed, mode-set `0755`, then installed with `os.replace`; destination directory was `fsync`ed. Published size is `26627832` bytes; published SHA is the required new value.
- `file -b`: `ELF 64-bit LSB shared object, x86-64, version 1 (SYSV), dynamically linked`; `objdump -f`: `elf64-x86-64`, architecture `x86_64`.
- `objdump -p`: `NEEDED` exactly `libm.so.6`, `libc.so.6`, `ld-linux-x86-64.so.2`, `libpthread.so.0`, `libdl.so.2`, `librt.so.1`; no `RPATH` or `RUNPATH` entries.
- `nm -D --defined-only`: all five required exports present: `createRenderer`, `destroyRenderer`, `getCurrentBuffer`, `bufferDrawText`, `bufferWriteResolvedChars`.
- Builder target verification: `bash crates/opentui-bridge/native/build_opentui.sh --verify-only crates/opentui-bridge/native/lib/x86_64-unknown-linux-gnu/libopentui.so --target x86_64-unknown-linux-gnu` exited `0`; validated ELF architecture, ABI, NEEDED allowlist, absent RPATH/RUNPATH, and SHA.
- `rustup target list --installed` returned only `aarch64-apple-darwin`. `x86_64-unknown-linux-gnu` is not installed; bounded native bridge target check was not run, truthfully `NOT_RUN`. No target was installed.
- `python3 tools/convergence_gate.py` was run before further leaf selection and remains repository-wide BLOCKED with 80 pre-existing ledger findings; none identify this artifact.
- TUI-011 remains BLOCKED, never completed: Windows GNU/MSVC distribution, macOS signing/notarization, and frozen manifest/sidecar work remain outside this one-file artifact lane.

## Vendored x86_64 Windows GNU DLL @ f6bf9d7 (half 1)

- Session: `ses_f300d33cdffeaD8ZWEVsgzB5i5`; branch: `lane/TUI-011-artifact-matrix`; base HEAD: `f6bf9d7`.
- Sole product artifact: `crates/opentui-bridge/native/lib/x86_64-pc-windows-gnu/opentui.dll`.
- Source: `/private/var/folders/b0/dj81nc_j2yq2bkmg0yd2sgyc0000gn/T/opencode/tui011-build/matrix/x86_64-pc-windows-gnu/out/opentui.dll`.
- Required source identity: size `6800384`; SHA-256 `0be2efd8a77330ba23b0f5eeb094a548f5fee4b7ad7b4c6f83e0e786dadd6b15`. Copy streamed to a same-directory temp, verified staged hash/size, `fsync`ed, `os.replace`d, then directory `fsync`ed. Source, staged, published bytes compare equal; published mode `0644`.
- `file -b`: `PE32+ executable (DLL) (GUI) x86-64, for MS Windows`. `objdump -f`: `coff-x86-64`, architecture `x86_64`; `objdump -p`: PE32+ (`0x20b`), DLL characteristics `0x2022`.
- Bounded stdlib PE parser: complete export table `431` unique names, all `431` ordinals in range, no forwarded exports, required ABI `createRenderer destroyRenderer getCurrentBuffer bufferDrawText bufferWriteResolvedChars` present; import directory has `14` unique bare `.dll` names with no `/`, `\\`, `..`, whitespace, or other path surprises. `cmp` source/destination returned `0`.
- DLL-only builder command correctly failed closed because the import library is not vendored yet: `bash crates/opentui-bridge/native/build_opentui.sh --verify-only crates/opentui-bridge/native/lib/x86_64-pc-windows-gnu/opentui.dll --target x86_64-pc-windows-gnu` -> exit `1`, `--import-lib` required.
- External-source full-pair verification (not a repository pair claim) exited `0`: `bash crates/opentui-bridge/native/build_opentui.sh --verify-only crates/opentui-bridge/native/lib/x86_64-pc-windows-gnu/opentui.dll --import-lib /private/var/folders/b0/dj81nc_j2yq2bkmg0yd2sgyc0000gn/T/opencode/tui011-build/matrix/x86_64-pc-windows-gnu/out/libopentui.dll.a --target x86_64-pc-windows-gnu`; reported `431/431` export/import correspondence, DLL SHA matches, external import-lib SHA `1dce093459b94a4d6bd0538c4fdd1681f50534930f3d59918b765bc87a982019`.
- MSVC fail-closed probe exited `1`: pinned `SUPPORTED_TARGETS` has no MSVC ABI target; no MSVC artifact was invented.
- No import library, Windows runtime, signing identity, macOS signing/notarization, or frozen manifest/sidecar edits in this lane. Parent remains BLOCKED on the next import-lib lane, MSVC policy, signing/notarization, and frozen contract work. Never mark TUI-011 completed.


## Vendored x86_64 Windows GNU import library (half 2)

- Session: `ses_f30068899ffe1I3Y6JUB49ciWd`; branch: `lane/TUI-011-artifact-matrix`; base HEAD: `0b9c6fa5adb8f2f35f8501f4755b7c5b8029de5d`.
- Sole product artifact: `crates/opentui-bridge/native/lib/x86_64-pc-windows-gnu/libopentui.dll.a`.
- Source: `/private/var/folders/b0/dj81nc_j2yq2bkmg0yd2sgyc0000gn/T/opencode/tui011-build/matrix/x86_64-pc-windows-gnu/out/libopentui.dll.a`.
- Required source identity: size `101524`; SHA-256 `1dce093459b94a4d6bd0538c4fdd1681f50534930f3d59918b765bc87a982019`. Source was streamed to a same-directory temporary file, hash/size checked, `fsync`ed, mode `0644`, then atomically installed with `os.replace`; destination directory was `fsync`ed. Published size and SHA match the required identity; no other artifact path changed.
- Import-library structure: `file -b` reports `current ar archive`; `!<arch>` magic verified; bounded stdlib parser found `436` archive members (`2` archive index members, `434` data members), all data members named `opentui.dll/`, no trailing bytes. `ar -tv` and `nm -g --defined-only` corroborate the GNU import archive.
- Required ABI check: `createRenderer`, `destroyRenderer`, `getCurrentBuffer`, `bufferDrawText`, `bufferWriteResolvedChars` present in archive string table and `nm` output (`5/5`); archive references `opentui.dll`.
- Full builder verification: `bash crates/opentui-bridge/native/build_opentui.sh --verify-only crates/opentui-bridge/native/lib/x86_64-pc-windows-gnu/opentui.dll --import-lib crates/opentui-bridge/native/lib/x86_64-pc-windows-gnu/libopentui.dll.a --target x86_64-pc-windows-gnu` exited `0`; bounded PE parser reported `431` exports and `431/431` export/import correspondence. DLL SHA `0be2efd8a77330ba23b0f5eeb094a548f5fee4b7ad7b4c6f83e0e786dadd6b15`; import-lib SHA `1dce093459b94a4d6bd0538c4fdd1681f50534930f3d59918b765bc87a982019`.
- Rust target check: `rustup target list --installed` returned only `aarch64-apple-darwin`; `x86_64-pc-windows-gnu` is not installed. No target was installed; no Windows Rust bridge compile was claimed.
- `python3 tools/convergence_gate.py` remains repository-wide BLOCKED with `80` pre-existing findings; no finding identifies this artifact.
- Parent TUI-011 remains BLOCKED, never completed: MSVC remains unsupported by the pinned Zig interface; Windows signing identity and macOS signing/notarization are unavailable; frozen artifact-manifest/sidecar conflicts remain. This half-2 lane only vendors the GNU import library. No test/builder/source/sidecar/policy edits.

## Manifest artifact matrix @ ea97ae9

- Task/session: `TUI-011` / `ses_f3000be43ffezAvxGSb7zGVVTT`; branch `lane/TUI-011-artifact-matrix`; starting HEAD `ea97ae96e4c0e595c6a34a81072c8e859d711654`.
- Sole production file: `crates/opentui-bridge/native/artifacts.json`. Existing 19 top-level arm64 fields remain first and in order; append-only matrix fields follow. Builder pin updated to exact commit `312d31586dbab33e8cab4607fd3227469b5f0b58`.
- Source pins remain fork commit `c01292fd0837bafd07ce458c74416b2b375a41ab`, tree `261e8ea4b0ac68bb589760c3c871e202ef71ed6b`, Zig `0.16.0`, exact archive URL/SHA-256/size, and each artifact's Zig target. Optimize mode is `ReleaseSafe` for every entry.
- Matrix enumerates all six vendored files only: macOS arm64/x86_64 dylibs; Linux aarch64/x86_64 shared objects; Windows GNU DLL/import archive. Each entry records exact target, repo-relative path, SHA-256, size, type. Windows entries share pair ID `x86_64-pc-windows-gnu-opentui`, have reciprocal `paired_path`, and roles `runtime-dll`/`import-library`.
- Windows GNU verification metadata records `passed-as-pair`, `artifact_count: 2`, and complete `431/431` export/import correspondence. Independent metadata names `shasum -a 256` plus `stat -f %z`, result `6/6`. Runtime execution is explicitly `not-run`.
- Unsupported `x86_64-pc-windows-msvc` is a reason-only target entry: pinned `build.zig` has no MSVC ABI target; builder refuses instead of relabeling GNU bytes. No MSVC artifact path/hash/size is listed.
- JSON parse/bound/integrity check: `python3 -m json.tool ...`; independent Python SHA-256/stat; all 6 paths, hashes, sizes and pair references matched. Manifest is `5204` bytes, below `64 KiB`.
- Builder `312d315` remains byte-identical to HEAD. Pinned `build_opentui.sh --verify-only` exited 0 for all four single-artifact targets and the Windows GNU pair. Builder independently checked exact architecture/format, five required ABI exports, Mach-O install names/rpaths, ELF NEEDED/rpaths, PE archive structure, and 431/431 Windows export/import correspondence.
- Independent file identities: arm64 mac `798f30dd...` / `6863648`; x86_64 mac `a7756f27...` / `7190318`; aarch64 Linux `e85a4571...` / `26598960`; x86_64 Linux `9f074adf...` / `26627832`; Windows DLL `0be2efd8...` / `6800384`; import archive `1dce0934...` / `101524`.
- Frozen test `crates/opentui-bridge/tests/native_artifact_manifest.rs` stayed byte-identical at SHA-256 `efb642364b6a4dc559fff019d21c14f778c170f34f96bf5e7183b4aeb536aa40`. Exact expected result: 1 passed, 2 failed. The only failures are immutable defects: line 95 wrongly requires every SHA character to satisfy both `is_ascii_hexdigit()` and `is_ascii_lowercase()` (digits are not lowercase letters); line 122 applies the manifest's 64 KiB cap to the multi-MB Mach-O artifact. Sidecar test remains green. No regression or test edit.
- Exact frozen command: `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 cargo test -p opencode-rk-opentui-bridge --test native_artifact_manifest -- --test-threads=1`; expected 1/3 pass, exit 101. Test binary replay showed `running 3 tests`, sidecar OK, two defect failures above.
- No artifact, test, builder, sidecar, source, controller, verifier, or policy edit. Parent stays BLOCKED on frozen parser defects plus signing/notarization and runtime/platform proof. No parent completion or acceptance claimed.
- Canonical `python3 tools/validate_repository.py` reached policy/verifier self-test checks successfully, then failed repository-wide backlog exhaustion with 51 pre-existing errors outside this lane. No validation/policy file changed.

