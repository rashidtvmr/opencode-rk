# TUI-011 native artifact manifest lane

Status: blocked. Manifest contract implemented; frozen test remains blocked by missing sidecars and an immutable-test size conflict.

## Claim

- Task: `TUI-011`
- Session: `ses_f305ebdc0ffeCnRhTWuyJnTGWl`
- Base: `6eba7ba7f8a29b82f7a81f87a6fdf9c93b852cc4`
- Branch: `lane/TUI-011-native-artifacts`
- Owned production file: `crates/opentui-bridge/native/artifacts.json`
- Scratchpad: `worklog/TUI-011.md`
- Frozen test: `crates/opentui-bridge/tests/native_artifact_manifest.rs`
- Frozen test SHA-256: `efb642364b6a4dc559fff019d21c14f778c170f34f96bf5e7183b4aeb536aa40`

## Source evidence

- `tasks/completion/tui.json:14` TUI-011 contract requires reproducible native distribution, ABI checks, SBOM/license/checksum metadata.
- `docs/architecture/COMPLETION_NATIVE_TUI.md:14-20,33-37` pins the OpenTUI fork and requires build flags, SBOM, licenses, deterministic libraries, and loader metadata.
- `crates/opentui-bridge/native/build_opentui.sh:4-15,28-50` records exact fork/tree/Zig/archive/target/ReleaseSafe/ABI/artifact bounds.
- `crates/opentui-bridge/native/build_opentui.sh:137-220,297-377` verifies source pins, Zig archive/version, Mach-O arm64, ABI exports, install name/rpath safety, and SHA-256.
- `crates/opentui-bridge/tests/native_artifact_manifest.rs:10-16,36-97,99-147,149-197` frozen contract checks manifest bounds/pins/path/SHA, Mach-O arm64, and sidecars.

## Manifest contract

`crates/opentui-bridge/native/artifacts.json` is bounded valid JSON. It records:

- Fork repository, commit `c01292fd0837bafd07ce458c74416b2b375a41ab`, tree `261e8ea4b0ac68bb589760c3c871e202ef71ed6b`.
- Zig `0.16.0`, archive URL, archive SHA-256 `b23d70deaa879b5c2d486ed3316f7eaa53e84acf6fc9cc747de152450d401489`, archive size `52238004`, Zig target `aarch64-macos.13.0`.
- Rust target `aarch64-apple-darwin`, relative artifact path `lib/aarch64-apple-darwin/libopentui.dylib`, SHA-256 `798f30dd7f4fbe36d52c8834652ed7bcd7f20dfd2a1203d09cc24880eeb13a91`, size `6863648`, `ReleaseSafe`.
- Install name `@rpath/libopentui.dylib`, empty LC_RPATH list, required exported ABI list.
- Builder commit `6eba7ba7f8a29b82f7a81f87a6fdf9c93b852cc4` and path `crates/opentui-bridge/native/build_opentui.sh`.

## Read-only verification

Commands:

```text
rtk shasum -a 256 crates/opentui-bridge/native/lib/aarch64-apple-darwin/libopentui.dylib
rtk stat -f '%z %N' crates/opentui-bridge/native/lib/aarch64-apple-darwin/libopentui.dylib
rtk file crates/opentui-bridge/native/lib/aarch64-apple-darwin/libopentui.dylib
rtk lipo -archs crates/opentui-bridge/native/lib/aarch64-apple-darwin/libopentui.dylib
rtk otool -D crates/opentui-bridge/native/lib/aarch64-apple-darwin/libopentui.dylib
rtk otool -l crates/opentui-bridge/native/lib/aarch64-apple-darwin/libopentui.dylib | rtk grep -E 'LC_RPATH|path '
rtk nm -gU crates/opentui-bridge/native/lib/aarch64-apple-darwin/libopentui.dylib | rtk grep -E 'createRenderer|destroyRenderer|getCurrentBuffer|bufferDrawText|bufferWriteResolvedChars'
rtk bash crates/opentui-bridge/native/build_opentui.sh --verify-only crates/opentui-bridge/native/lib/aarch64-apple-darwin/libopentui.dylib
```

Observed: SHA matches manifest; size `6863648`; Mach-O 64-bit arm64; `lipo` `arm64`; install name `@rpath/libopentui.dylib`; no LC_RPATH; all five required exports present; builder `--verify-only` exits 0 and returns the same SHA.

## Frozen test result

Command:

```text
rtk env CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 cargo test -p opencode-rk-opentui-bridge --test native_artifact_manifest -- --test-threads=1
```

Independently reproduced result: `0 passed; 3 failed`.

- `test_manifest_exists_bounded_and_records_contract` fails at frozen `native_artifact_manifest.rs:95`. The immutable predicate requires every SHA character to satisfy both `is_ascii_hexdigit()` and `is_ascii_lowercase()`. Digits satisfy the first predicate and fail the second, so the truthful SHA-256 containing digits cannot pass. No manifest or test change made.
- `test_artifact_exists_and_is_macho_arm64` fails at frozen `native_artifact_manifest.rs:122`. The test applies `MAX_BYTES = 64 * 1024` to artifact metadata; the verified dylib is `6,863,648` bytes. The builder contract permits up to `134,217,728` bytes. No stripping, compression, or replacement made.
- `test_license_and_sbom_sidecars_exist_and_bounded` fails first at frozen `native_artifact_manifest.rs:155`: `native/NOTICES` is missing. `native/sbom.json` is also missing and required by the same sidecar assertion. No sidecar or test edits made.

## Builder and native verification

```text
rtk bash crates/opentui-bridge/native/build_opentui.sh --verify-only crates/opentui-bridge/native/lib/aarch64-apple-darwin/libopentui.dylib
```

Exit `0`; builder reported Mach-O arm64, exact `arm64` architecture, all five ABI exports, no `LC_RPATH`, install name `@rpath/libopentui.dylib`, and SHA-256 `798f30dd7f4fbe36d52c8834652ed7bcd7f20dfd2a1203d09cc24880eeb13a91`.

```text
rtk env CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 DYLD_LIBRARY_PATH="$PWD/crates/opentui-bridge/native/lib/aarch64-apple-darwin" DYLD_FALLBACK_LIBRARY_PATH="$PWD/crates/opentui-bridge/native/lib/aarch64-apple-darwin" cargo test -p opencode-rk-opentui-bridge --features native --lib -- --test-threads=1
```

Exit `0`; `73 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`.

## Manifest verification

`python3` stdlib read reports valid JSON, manifest size `1034` bytes, artifact size `6,863,648` bytes, declared SHA equals actual SHA `798f30dd7f4fbe36d52c8834652ed7bcd7f20dfd2a1203d09cc24880eeb13a91`, and declared/actual size match.

## Blocker

- Frozen `native_artifact_manifest.rs` has the disputed SHA predicate and artifact-size bound described above; frozen test remains unmodified.
- Missing `crates/opentui-bridge/native/NOTICES` and `crates/opentui-bridge/native/sbom.json` remain outside this one-file lane.
- TUI-011 is blocked pending controller/frozen-test review and sidecar ownership. Do not claim acceptance.

## Other checks

- `python3 tools/convergence_gate.py`: blocked by pre-existing repository-wide ledger findings.
- `python3 tools/validate_repository.py`: protection checks pass; backlog exhaustion fails pre-existing (51 errors).
