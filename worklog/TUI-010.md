# TUI-010 — Native terminal parity (headless acceptance)

Claim: session ses_f41f2c272ffek0lL97XJwropQw, status in-progress.
Card: TUI-010 "Native terminal parity and platform acceptance", paths tests/e2e/native_tui, status not-started.
HEAD: 1614754.

## Card-vs-harness gap (honest)

- Card path `tests/e2e/native_tui` does NOT exist at HEAD (checked `ls`, no such dir).
- No real PTY in this CI env: no pty device guarantee, no Windows console, no macOS runner.
- Real resize/mouse/clipboard/IME/tab-switch scenarios cannot execute here.
- Closest real harness: `crates/cli/tests/*.rs` binary-driven over stdio pipes:
  `tui_entrypoint.rs:55,65,80,104,134,145` (t01-t06), `tui_live_state.rs:129,167`
  (l01-l02), `tui_follow.rs:142` (f01), `native_launch.rs`, `native_daemon_flow.rs`,
  `default_tui.rs`.
- Owned file: `crates/cli/tests/native_tui_parity.rs` (13 tests, zero skips,
  std-only `#![forbid(unsafe_code)]`, no new dev-deps). Frozen sha256 81d12f9a…
  (post warning-cleanup; re-checksum at commit time).

## Baseline / pre-existing failures (evidence, not my lane)

- `cargo test -p opencode-rk-cli --test tui_entrypoint` (log /tmp/opencode/tui010-baseline.log):
  5 passed, 1 failed — t06 `interactive_pipe_submit_help_quit` FAILS at HEAD because
  `tui_entry.rs:548` refuses interactive TUI on piped stdin
  ("refusing interactive TUI on piped stdin"). t06 predates that guard; needs a
  frozen-test/contract decision by integrator, NOT edited here.
- `tui_live_state l01` FAILS at HEAD (`create session` expect, l01 needs `web` daemon
  + real POST /api/sessions; 0 passed 1 failed, /tmp/opencode/tui010-l01.log).
- Dirty tree blocked full `cargo test`: another lane's `crates/security/src/os_backend.rs`
  (untracked, not mine) breaks `opencode-rk-security` lib build (`kill_on_drop`
  E0599, /tmp/opencode/tui010-green2.log). My target builds because cargo reuses the
  prebuilt cli test profile; full-workspace green is an integrator concern.

## Tests (all headless, pipes only, zero skips)

- p01 startup once-frame contract (banner/status/composer/footer + Ctrl+P/T hints)
- p02 compose keymap flag + env
- p03 tools status actions named (model switcher / context detail / help)
- p04 session-stream scriptable path (`--follow --follow-for 1`, bounded, exit 0)
- p05 quit: piped `tui` fails closed with typed "piped stdin" + scriptable-path hint
- p06 resize: COLUMNS=40 vs 200 frames byte-identical headless (verified manually too)
- p07 unicode memory name (héllo-日本語.md) renders + fallback hints stay
- p08 exit-restore: bounded exit + follow-up run works (shell usable)
- p09 resource bound: memory pane caps at 64 files (70 given → 64 listed)
- p10 invalid keymap fails closed naming value
- p11 dead origin `--once` fails closed bounded, no fabricated `(live)` data.
  NOTE: descriptor file written by test is ignored by current `resolve_origin_bearer`
  (lookup misses in this env); observed stderr is the typed unauthenticated-descriptor
  refusal — still fail-closed, still names the failure. A live-daemon variant needs a
  running `serve` daemon (pre-existing l01 failure shows that path is broken at HEAD).
- p12 `--native --once`: renders or typed terminal/headless refusal (observed exit 2
  with "stdin and stdout are redirected…", never a silent wrong path)
- p13 no-leak: 3 repeated frames exit cleanly

## Results

- GREEN: `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=1 timeout 170 cargo test -p opencode-rk-cli
  --test native_tui_parity -- --test-threads=1` → 13 passed, 0 failed
  (logs /tmp/opencode/tui010-green1.log 13/13 with 1 unused_mut warning in new file;
  /tmp/opencode/tui010-green3.log 13/13 zero new-file warnings).
- No existing frozen tests touched. No plan/verifier edits. No other lanes touched.

## Remaining unknowns (for integrator/verifier)

- Real-PTY parity (resize events, mouse, clipboard, IME, tab switching, raw-mode
  restore on SIGKILL-style death) still needs a PTY runner; document as deviation.
- t06 (tui_entrypoint) vs piped-stdin guard contradiction needs a contract ruling.
- l01-style live-daemon binding currently fails at HEAD; p11 pins only the
  fail-closed half.
