# OPS-009 scratchpad — in-memory ordered cassette replay

Claim: `cc.claim(pathlib.Path('.'), 'OPS-009', 'ses_f387af8a9ffe7GarMQ3qCTI8wr', 'worklog/OPS-009.md')` → in-progress. No collision.

Source evidence (commit `621ec4f63d52718be8a97a426fe8c6bebb69ed4c` at lane start):
- `crates/foundation/src/ops_replay.rs:1-138` — full replay checker (pre-existing, landed in `1be93d3`/`248f519`).
- `crates/foundation/tests/ops_replay.rs:1-136` — frozen OPS-009-T01..T05 (path-include, lib.rs untouched by lane).
- `crates/foundation/src/lib.rs:18` — `pub mod ops_replay;` already wired by integrator.
- Task card `tasks/OPS-009.md:61-95` — contract/failure/resource bounds.

Observed scenario: owned file already implements the full contract; zero repair needed, byte-identical kept:
- `Frame{request,response}`, `Cassette{frames}` + `new/push_frame/len/is_empty` (+ additive `frames()` accessor, benign dead_code warning in lib build only).
- `ReplayError::{EmptyRequest,TooManyFrames{max,actual},Mismatch{index,expected,actual},Exhausted{index},UnusedFrames{remaining}}`; custom `Debug` prints index+lengths only; `Display` (thiserror) prints index/counts only — no body bytes.
- `MAX_FRAMES=1024`, `MAX_FRAME_BYTES=65536`; over-cap push refused pre-push (cassette unchanged); 1025th push → `TooManyFrames{max:1024,actual:1024}`.
- `replay(&Cassette,&[&str],bool)->Result<usize,ReplayError>`: ordered match, first-mismatch return, past-end `Exhausted{index:len}`, strict tail `UnusedFrames{remaining}`, prefix OK non-strict; `replay` takes `&` refs only — mutates nothing.
- No `std::fs/env/net`, no tokio, no print/log, no `todo!/unimplemented!/env!` in impl or tests (grep exit 1 both).
- Deterministic: pure fn, no clock/env/IO.

Target boundary: owned `crates/foundation/src/ops_replay.rs` + this scratchpad only. Frozen tests, lib.rs, Cargo.toml untouched.

Tests (frozen, zero edits): `CARGO_BUILD_JOBS=1 RUST_TEST_THREADS=1 cargo test -p opencode-rk-foundation --test ops_replay` → 5/5 ok (T01 happy, T02 order/determinism/prefix, T03 caps, T04 failures/immutability, T05 redaction/isolation). Test sha256 `b81069a4…`, impl sha256 `c775514f…`. `cargo check -p opencode-rk-foundation` clean (1 benign dead_code warning on additive `frames()`).

Decisions: no code change (already GREEN, editing risks frozen-test drift); no workspace check (lane scope = focused target, one build job per orders).

Remaining unknowns: none for this slice. Out of scope per card: persistence, live record transport, CI/env policy, env-secret scanning, WebSocket streaming.
