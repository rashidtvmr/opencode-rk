# TUI-003 — real native application shell (`crates/cli/src/native_app.rs`)

Session: ses_orch_TUI003. Owned scope: `crates/cli/src/native_app.rs` ONLY.
Deps: TUI-002 (completed) + APP-003 (completed) — pickable, claim OK.

## Claim
- `cc.claim(...'TUI-003','ses_orch_TUI003','worklog/TUI-003.md')` → in-progress (this session).

## Source evidence
- Card: `tasks/completion/tui.json:6` — TUI-003, deps TUI-002+APP-003, paths
  `native_app.rs` + `native_layout.rs` (layout half = sibling lane, NOT touched).
- TDD contract: `docs/TDD.md:32-56` (compiling RED, freeze hash, GREEN, no test edits).
- Security: `docs/SECURITY.md` (no secret/file/DB access; pure-state file, no IO).
- HEAD `832ab7a` == `origin/main` (0 ahead/behind); dff7cce is ancestor.
  `git pull --rebase` impossible (97 dirty files repo-wide, other lanes');
  fetch shows no new remote work, so no rebase needed.
- HEAD `native_app.rs` = 527 lines, 6 tests. Working tree had +337/-15 uncommitted
  (Empty/Error views, badges, focus redirect, debounce) — origin unknown
  (no scratchpad existed), treated as UNVERIFIED: backed up to
  `/tmp/opencode/tui003-uncommitted-backup.rs` (sha256 be866616...), then
  reverted owned file to HEAD to run honest RED→GREEN myself.

## Observed scenario (HEAD baseline gaps vs card)
HEAD already has: Focus/View/Freshness/Rect/ShellLayout/FrameScheduler/NativeApp
with set_view stale-forcing?, mark_live_data, mark_disconnected, cycle_focus,
resize (no redirect), 6 tests. Gaps to close (remainder):
1. PTY input→focus→daemon-data frames: focus redirect on compact (resolve_focus),
   Tab/Shift-Tab skip of zero-area sidebar, input gates (accepts_input,
   can_edit_composer).
2. Empty/Loading/Offline/Error distinguishable+actionable: mark_empty/mark_loading/
   mark_error + retry/dismiss + pending_view debounce (flush_pending_view).
3. Tiny no-overlap: Rect zero-area guard in overlaps, resolve on resize.
4. Stale/offline never fake-live: must_badge_stale/data_badge/is_focus_visible,
   needs_render/pending_frames host hooks.
5. Coalesced idle-bounded loop: already present; tests pin it + MAX_HISTORY bound.

## Target boundary
- ONLY `crates/cli/src/native_app.rs` (+ this scratchpad + ledger row).
- No IO/clock/threads/FFI; `forbid(unsafe_code)` kept. Pure state, caller feeds inputs.
- Sibling files (`native_shell.rs`, `native_layout.rs`, `native_host.rs`,
  `terminal_host.rs`) read-only for evidence.

## Tests
- RED (new tests vs HEAD API, all compile, fail on missing behavior) — see below.
- GREEN cmd: `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 180 cargo test -p opencode-rk-cli --bin opencode-rk native_app` → log `/tmp/opencode/tui003.log`.

## Decisions
- Revert-then-reimplement rather than adopting uncommitted diff unverified.
- RED tests use only HEAD-existing API where behavior differs (compile+fail rule).

## Remaining unknowns
- None for owned file. Integrator unifies `ShellLayout::compute` dup with
  `native_layout.rs` (out of scope).

## RED evidence (frozen)
- Frozen file: `/tmp/opencode/tui003-red-frozen.rs`, sha256
  `0430e086fc6827229938513ca7df08ffded98b88e3e02c2f49a7b714824953a1`
  (11 tests: 6 HEAD + 5 new TUI-003 RED).
- RED cmd: `rustc --edition 2021 --test crates/cli/src/native_app.rs -o /tmp/opencode/tui003-red-bin && /tmp/opencode/tui003-red-bin`
  → 8 passed / 3 failed (`zero_area_rects_never_overlap`,
  `resize_redirects_stranded_sidebar_focus`, `compact_tab_cycle_skips_hidden_sidebar`).
  2 extra RED tests compile+pass on HEAD invariants (input gating states distinct,
  tiny transcript layout) — kept as regression guards.
- Earlier `cargo test ... native_app` attempt (before revert) is invalid evidence:
  sibling `native_shell.rs` working-tree state references not-yet-existing
  `native_app` APIs (11 E0599). Standalone rustc harness is the honest RED/GREEN
  runner until integrator lands the sibling lanes.

## GREEN evidence
- Standalone: `rustc --edition 2021 --test crates/cli/src/native_app.rs -o
  /tmp/opencode/tui003-green-bin && /tmp/opencode/tui003-green-bin`
  → 11 passed / 0 failed. Test module byte-identical to frozen RED
  (`diff` of `#[cfg(test)]` section → identical); only impl changed.
- In-crate (mandated cmd): `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 180
  cargo test -p opencode-rk-cli --bin opencode-rk native_app`
  → log `/tmp/opencode/tui003.log`, exit 0, `22 passed; 0 failed`
  (11 owned `native_app` + 10 `native_approvals` substring-matched +
  1 `native_layout::consts_match_native_app` pin). Owned 11/11 green, zero test edits.
- No stubs: `grep todo|unimplemented|stub|FIXME` → no matches.
  `forbid(unsafe_code)` intact, line 1. No IO/clock/threads added.
- Dead-code warnings only (non-test profile; sibling lanes not yet landed to
  call every new API — e.g. `cycle_focus_prev`, `flush_pending_view`).
  No errors, no warnings in test profile for owned tests.

## What GREEN implements (card remainder closed in owned file)
- T1 PTY input→focus→frames: `set_focus` resolves via layout, `cycle_focus` /
  `cycle_focus_prev` skip zero-area sidebar, focus moves dirty the scheduler;
  `needs_render` / `pending_frames` host hooks expose coalesced frames.
- T2 distinguishable+actionable: `mark_empty` / `mark_loading` / `mark_error`
  (+`pending_view` debounce + `flush_pending_view`), `retry`/`dismiss`,
  `accepts_input` / `can_edit_composer` gates, `pending_view()` accessor.
- T3 tiny no-overlap/focus: `Rect::is_empty` + zero-area `overlaps` guard,
  `rect_of` / `focus_visible` / `resolve_focus`, `resize` redirects stranded
  focus, `is_focus_visible`.
- T4 stale never fake-live: `must_badge_stale` / `data_badge` derived from
  `shows_live_data`; `set_view` forces Stale on non-actionable.
- T5 coalesced idle-bounded: `FrameScheduler` unchanged + `MAX_HISTORY`-bounded
  history (pre-existing, pinned by tests).

## Deviations
- `git pull --rebase` skipped: 97 dirty files repo-wide (other lanes);
  HEAD == origin/main (0 ahead/behind), fetch shows nothing new — no rebase needed.
- RED/GREEN primary runner is standalone `rustc --test` (sibling working-tree
  `native_shell.rs` references lane APIs not in HEAD, so `cargo test` cannot
  compile a HEAD-baseline RED; in-crate GREEN run AFTER impl compiles clean).
- Commit scope: owned file + scratchpad + claims.json only.
