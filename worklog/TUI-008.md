# TUI-008 worklog (re-verify lane → completion-ready)

## Claim
- Ledger `tasks/completion/claims.json`: TUI-008 `not-started` (reclaimed by
  `ses_f420f8732ffe5Rv2Nxle3iSLgV`) → claimed `in-progress` by
  `ses_f41fada5affetzDNrbwZm4HSRt`, scratchpad `worklog/TUI-008.md`.
- Owned file ONLY: `crates/cli/src/native_approvals.rs` (580 lines, 10 tests).

## Source evidence
- HEAD `f231a37`, file `crates/cli/src/native_approvals.rs:1-580`, clean
  (`git diff --stat` empty for owned file), zero `todo!/unimplemented!/stub`
  markers, `mod native_approvals;` wired at `crates/cli/src/main.rs:35`
  (integrator-owned, pre-existing commit `e2ea500`).
- Card TUI-008 (`python3 tools/completion_plan.py --card TUI-008`): T01 digest/
  workspace/requester/expiry/policy binding; T02 stale/replay rejection; T03
  interrupt cancels in-flight op + updates clients; T04 safe-vs-ambiguous
  retry; T05 focus cannot accidentally approve destructive/human-only.
- Prior lane report `worklog/TUI-008-appr.md` (RED sha `818ad64e…`, GREEN sha
  `b9994406…`, 577 lines); file has since grown to 580 lines via integrator
  commits (`ce3fd76`, `e2ea500`) — re-verifying at current HEAD, zero edits.
- `docs/TDD.md` §3 (RED compiles + fails), `docs/SECURITY.md` §1/§5
  (human-only never auto-approved, expiring capability grants).

## Observed scenario
- Re-verify only: owned file untouched (`sha256 1076589993d3547c…`),
  no implementation change needed. Frozen in-file tests (10):
  `approval_binds_digest_workspace_requester_expiry_policy`,
  `stale_unknown_digest_rejected_without_side_effects`,
  `replayed_digest_rejected_after_decision`,
  `expired_grant_settles_as_non_effect_denial`,
  `focus_activation_cannot_approve_destructive`,
  `human_only_never_bypasses_two_step`,
  `denial_is_single_step_and_records_no_approval`,
  `interrupt_cancels_in_flight_and_bumps_broadcast`,
  `retry_distinguishes_safe_from_ambiguous_side_effects`,
  `bounded_queue_and_history_drop_oldest`.
- Bin names: `oc2` + `opencode-rk` (compat alias, `crates/cli/Cargo.toml:9-19`).

## Target boundary
OWNED FILE ONLY: `crates/cli/src/native_approvals.rs`. No test edits, no other
lanes, no plan/verifier edits, no force-push. Dirty-tree files
(`ci_run.rs`, `tui_entry.rs`, server `lib.rs`, `REVIEW_ITERATION_2.md`,
untracked lane files) are other lanes' — never staged.

## Tests
- `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2 timeout 180 cargo test -p opencode-rk-cli --bin oc2 native_approvals` →
  exit 0, `test result: ok. 10 passed; 0 failed; 0 ignored; 0 measured; 292 filtered out`
  (log `/tmp/opencode/tui008.log:829-841`). Zero test edits; owned file
  byte-identical to HEAD (`sha256 1076589993d3…`).

## Decisions
- `Confirm{Unconfirmed, Confirmed}` enum + per-request `armed` state:
  focus/Enter maps to `Unconfirmed`, never approves destructive/human-only.
- Expiry/policy bound per grant via `ApprovalScope`; replay rejected twice
  (absent digest → `StaleOrReplayed`; `offer` rejects pending/history dups).
- `interrupt()` returns receipt with `broadcast_seq`; every mutation bumps seq
  so clients poll updates. `classify_retry()` → `AmbiguousSideEffects` iff
  interrupted, else `SafeToRetry` under a fresh digest.

## Remaining unknowns
- None for this lane. Renderer/focus-key mapping is integrator-owned.
