# SDK-001 worklog

## Claim
Typed SDK HTTP client (`crates/server/src/sdk_client.rs`): GET/POST with
`x-workspace-directory` header + `?directory=` rewrite, 30s timeout bound,
abort reclaim, redacted errors. No I/O, no spawn, no persistence.

## Source evidence
- `tasks/SDK-001.md:1-93` — full contract, 5 frozen tests, owns only
  `crates/server/src/sdk_client.rs`; lib.rs/Cargo.toml off-limits.
- `PLAN.md:36` pin `95daf90` vs `sources/upstream.lock.json` `07619a0` —
  reconciliation outstanding; no parity claims made.
- Local absence verified: `crates/server/src/` has no `sdk_client.rs`;
  `app_client.rs` (endpoint config only) + `clients.rs` (registry only) cover
  no typed codec/rewrite/timeout/abort/redaction.
- `docs/TDD.md:2-5`, `docs/SECURITY.md:1-6` — compiling RED, frozen hash,
  capability discipline, no secret logging.

## Observed scenario
New module. Tests use `#[path = "../src/sdk_client.rs"]` include (same as
`tests/event_stream.rs`, `tests/route_table.rs`) since integrator owns lib.rs.

## Target boundary
Own: `src/sdk_client.rs`, `tests/sdk_client.rs`, this worklog. Excluded:
process spawn (SDK-002), ACP framing (ACP-001), retries, real sockets.

## Tests
- RED: stub `new`=always-Ok + get/post=always-`Err(Decode)`; suite compiles,
  T01/T02/T04/T05 + negatives fail (only const check passes).
- GREEN: full native impl; `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2
  timeout 120 rtk cargo test -p opencode-rk-server --test sdk_client`.

## Evidence
- RED (stub, `tests/sdk_client.rs` frozen): `cargo test -p opencode-rk-server
  --test sdk_client` => 0 passed, 12 failed (compile OK).
- GREEN (impl): same command => 12 passed, 0 failed.
- `cargo test -p opencode-rk-server --test sdk_client --no-run`: exit 0
  (compiles; only pre-existing warnings in other crates remain).
- `cargo check -p opencode-rk-server --tests`: fails only on sibling
  `tests/acp_bridge.rs` include of missing `src/acp_bridge.rs` (not owned by
  SDK-001); sdk_client target itself compiles and passes.
- Forbidden-surface scan: no `unsafe` (file has `#![forbid(unsafe_code)]`),
  no `std::fs/net/process`, `tokio::fs/net/process`, `reqwest`, `Command`
  in owned files.
- Integration: lib.rs wiring intentionally untouched (integrator owns it);
  test uses `#[path = "../src/sdk_client.rs"]` like other lanes.

## Decisions
- `TypedBody` wraps `serde_json::Value` (existing dep, real typed decode);
  manual redacted `Debug`, no `Display`.
- `SdkError` carries no strings — only `BadStatus{status: u16}` numeric;
  Debug/Display clean by construction.
- `AbortFlag::flag()` name follows card `abort.flag()` wording.
- Timeout enforced by caller port; module exposes `SDK_TIMEOUT_SECS=30` and
  propagates `Timeout` without retention.
- pct-encode: RFC3986 unreserved kept, all else `%XX` upper hex.

## Remaining unknowns
- None for this slice. Integrator wires `mod sdk_client;` into lib.rs.
