# INT-010 worklog

## Claim

Own exactly one narrow pure sub-partition of the heterogeneous
enterprise-remote surface: deterministic share-descriptor validation +
last-write-wins merge (`ShareSync::apply`/`get`/`list`). All five hosted
partitions refused by type (`SyncOp::ProbeHosted` => `HostedPartition`).
No network, persistence, token exchange, relay, deployment, secret bytes.

## Source evidence

- Candidate base revision: `248f519` (workdir HEAD at implement time).
- Pinned OpenCode commit: `95daf90670b7c039c436c85537da5fbfe2205b41`
  (`sources/upstream.lock.json`).
- Finding: `sources/disc-003-reconciliation.json`
  `opencode.enterprise-remote` (partial/partial): heterogeneous
  enterprise/function surface; residual WEB-004/INT-010/SHARE-003 not
  separable by path family alone.
- Spec gap: `sources/enterprise-remote-spec-gap.json`
  (`searched-no-qualifying-in-surface-spec`): no qualifying spec for
  SyncServer/fanout, share routes, token exchange, support relay,
  installation lookup, deployment lifecycle. Required partitions:
  `enterprise-share-http`, `function-syncserver-websocket-r2`,
  `function-support-relay`,
  `function-github-token-exchange-installation`,
  `deployment-resource-lifecycle`. This lane owns ONLY the pure
  validation/merge sub-partition of `enterprise-share-http`.
- Guard: `sources/integrations-ownership-gap.json` INT-010
  `guarded-enterprise-remote-spec-gap` closure criteria.
- Classification: partial upstream + deliberate safer deviation
  (secret-by-reference merge; hosted refused).
- `docs/TDD.md` 2-5 + `docs/SECURITY.md` read before product work.

## Ownership decision

- `SHARE-003`/`WEB-004` transport/persistence/auth stay separate owners;
  accepted `PROV-006` untouched. Hosted partitions stay refused until a
  qualifying in-surface spec or source-grounded decomposition assigns them.

## Target boundary before RED

- Test owner: `crates/providers/tests/share_descriptor.rs` (5 tests).
  Wired via `#[path]`; shared `lib.rs` untouched by lane.
- Product candidate: `crates/providers/src/share_descriptor.rs` +
  additive `pub mod share_descriptor;` in
  `crates/providers/src/lib.rs:53`.
- No Cargo dependency change.

## Contract and resource decisions before RED

- Bounds frozen: `MAX_SHARES = 256`, `MAX_ID_LEN = 64`,
  `MAX_KEYREF_LEN = 64` (upstream supplies none; frozen RED owns values).
- `key_ref` opaque reference only (never secret bytes); digest exactly
  64 hex; `seq` caller-supplied monotonic per id.
- `ShareSync` sync on `&mut`, no threads, no clock, no I/O; `Debug`
  redacts `key_ref`/`digest`; errors carry no caller material.
- `BTreeMap` gives deterministic `list()` sorted by id.

## Tests

- Frozen test SHA-256:
  `6b241c47913c7fa8a2342e14658a6e258aea4284f16d3d25916169c4c53cdfa2`.
- Frozen test git object: `fdcb189701f2eada5147309d5c9e2b3ff8f643de`.
- Frozen command:
  `cargo test -p opencode-rk-providers --test share_descriptor`.
- RED history: impl + tests landed together in `42b471c`, so compile-RED
  (`E0432` on gutted impl) is authoring feedback only. Valid behavioral RED
  reconstructed this session by stubbing `apply` to `Err(Overflow)`:
  `cargo test -p opencode-rk-providers --test share_descriptor` =>
  **0 passed / 5 failed** (impl restored byte-identical after).
- Frozen impl SHA-256:
  `d55ce6874751450d593aa5438c88b0e6675e383799223a3271273ba0aa8348d3`.
- Focused GREEN (this session, `CARGO_BUILD_JOBS=2 RUST_TEST_THREADS=2`,
  `--test-threads=2`): 5 passed / 0 failed.
- No `#[ignore]`, no stub, no test edit after RED.
- Purity grep: no `std::{env,process,fs,net}`, `Command`, `tokio`,
  `sqlite/rusqlite`, `SystemTime/Instant` in impl.

## Implementation

- Token validation (alnum-first, `[A-Za-z0-9._-]`), 64-hex digest check,
  seq-gated upsert/remove, new-id-only overflow check, unknown-remove
  `Ok(None)`, every `ProbeHosted(kind)` => `HostedPartition`.

## Remaining unknowns

- All five hosted partitions (HTTP transport, SyncServer/WS/R2, support
  relay, GitHub exchange/installation, deployment lifecycle): refused by
  design here; need qualifying spec or decomposition before owned.
- Verifier/controller acceptance external; not claimed.
