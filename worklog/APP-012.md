# APP-012 installed local application golden journey

## Claim

- Task/session: `APP-012` / `ses_f3c4de578ffelQv59xDXmOs03B`.
- Claimed revision: `a83a56327dfd15e96dd2e9c5faf6bf231548746f` on
  `lane/PHASE1-product-spine-20260923`.
- Canonical owned path: `tests/e2e/local_application.rs`.
- Parent contract: install -> bare `oc2` -> setup -> coding turn -> real broker
  approval/tool edit -> concurrent client -> daemon restart/resume, using only a
  deterministic provider fixture and disposable data.

## Source evidence and observed scenario

- `tasks/completion/local.json` APP-012 declares this exact installed journey;
  dependencies APP-008, APP-010, APP-011 and TUI-010 are ledger-completed.
- `docs/CONVERGENCE.md:10-24` requires a clean installed no-subcommand journey
  through the real daemon, provider, broker, tool, persistence and two clients.
- `crates/cli/src/main.rs::run` computes `DefaultLaunch` but consumes only
  `plan.mode`; `plan.role` and `plan.view` are not wired to the caller.
- `crates/cli/src/tui_entry.rs::native_interactive_loop` constructs
  `NativeHost` with `skip_onboarding: true`.
- `crates/cli/src/tui_entry.rs::fetch_snapshot` returns a manual-session error
  for an empty daemon instead of atomically selecting/creating the first session.
- `worklog/APP-001.md:51-54` explicitly records integrator wiring and E2E PTY
  proof as remaining work even though the claim ledger marks APP-001 completed.
- `worklog/LANE-ONBOARD-SETUP.md:8-13` likewise records `main.rs` wiring as
  outside that lane. Its setup driver is pure/in-memory and is not a persistent
  secure-store caller.
- Packaged static OpenTUI and explicit-session PTY are already proven in
  `worklog/TUI-011.md`; they do not satisfy the bare fresh-HOME parent boundary.

## Target boundary and resource contract

- New trusted RED must use the real executable under a PTY, a disposable HOME
  and data directory, loopback-only fixtures, bounded captures/timeouts and
  deterministic child/daemon cleanup.
- No user's database, credentials or broad host state may be read or changed.
- Provider fixture is the only substitute; no mocked renderer, daemon, broker,
  tool executor or persistence success is acceptable.
- Product repair children may not be called complete until this parent test is
  frozen, runs GREEN against the installed artifact, and the exact integrated
  revision is rerun.

## Current blockers

- Trusted APP-012 RED is being authored independently before implementation.
- Existing frozen `installed_default_entrypoint.rs` remains immutable and has
  five literal `todo!()` bodies; it requires controller contract review and is
  not editable by this lane.
- APP-001 and APP-005 controller rows conflict with their own admitted unwired
  boundaries; product fixes require explicit repair-child leases rather than
  rewriting those accepted rows.
- Unsigned CI runners are authorized. Signing/notarization identities are not
  available and must remain an external release blocker, never fabricated.

## APP-001 repair integration candidate

- The macOS bare-entrypoint RED is frozen at SHA-256
  `f5689f0f5b53fd7aacd0934b104a4e0fd5c38bdcdf1943f9f16f03e1535283ab`.
- After wiring the planned startup view and authenticated first-session
  creation, it is GREEN `3/3`; `app_start` is GREEN `16/16` and native parity
  remains GREEN `13/13`.
- This proves setup routing, sequential singleton reuse, first-session creation,
  daemon survival and terminal restoration on this macOS source-built binary.
  It is not packaged APP-012 acceptance and does not prove persistent secure
  onboarding, simultaneous empty-daemon clients, tools, restart/resume, Linux,
  Windows, or clean-machine installation.

## Additional whole-journey wiring findings

- `crates/providers/src/account_setup.rs:1-8` explicitly defines only a pure,
  in-memory setup boundary and says the caller owns persistence/transport. No
  production caller or OS credential-store dependency exists, so APP-005's
  secure persistent setup journey is not delivered by its unit-state GREEN.
- `crates/server/src/lib.rs:1192-1223` advertises built-in registry tools, but
  `crates/tools/src/executor.rs:105-119` actually executes only `bash`/`shell`
  and `echo`; advertised `read`, `file`, `grep`, `write`, and `edit` return
  unknown-tool failures through this executor.
- `crates/server/src/lib.rs:1388-1438,1585-1643` does invoke the real permission
  broker before shell/non-shell dispatch, but `RequireHuman` is converted
  directly into a terminal error. There is no approval-resume channel in this
  turn caller, so a human-gated tool cannot be approved and continued.
- `crates/tools/src/file_ops.rs:173-200` authorizes writes only; read/list pass
  directly to filesystem I/O. It therefore cannot be wired as the APP-012 read
  tool without adding path-scoped broker authorization and byte/resource bounds.
- These are repair-child requirements, not reasons to weaken the golden test:
  the provider fixture must request a real bounded file operation, observe a
  broker decision, execute only after the applicable authorization, receive the
  tool result in round two, and verify the persisted result after daemon restart.
- `scripts/install-oc2.ps1:54-66` expands an archive and copies only `oc2.exe`;
  it neither validates nor installs the Windows native `opentui.dll` closure,
  and unlike the POSIX installer it has no member-count/member-size/aggregate
  expanded-payload bounds. No `pwsh` exists on this macOS host. The authorized
  unsigned Windows CI runner must therefore run a new frozen PowerShell RED and
  prove native closure, traversal rejection and 128 MiB limits on Windows; local
  source inspection is not platform evidence.
- `.github/workflows/ci.yml:29-55` runs generic workspace check/test/clippy on
  `windows-latest`, but it does not build/package/install the native-feature
  Windows GNU release closure. There is no release workflow. Changes under
  `.github/` require `@rashidtvmr` CODEOWNER review and the canonical repository
  guard; `gh` is not installed in this environment, so no PR/workflow-run or
  protected-host evidence can be fabricated locally.
