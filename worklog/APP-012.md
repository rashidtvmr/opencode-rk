# APP-012 installed local application golden journey

## Claim

- Task/session: `APP-012` / `ses_f3c4de578ffelQv59xDXmOs03B`.
- Claimed revision: `a83a56327dfd15e96dd2e9c5faf6bf231548746f` on
  `lane/PHASE1-product-spine-20260923`.
- Canonical owned path: `tests/e2e/local_application.rs`.
- Parent contract: install -> bare `oc2` -> setup -> coding turn -> real broker
  approval/tool edit -> concurrent client -> daemon restart/resume, using only a
  deterministic provider fixture and disposable data.

## Source evidence and observed scenario

- `tasks/completion/local.json` APP-012 declares this exact installed journey;
  dependencies APP-008, APP-010, APP-011 and TUI-010 are ledger-completed.
- `docs/CONVERGENCE.md:10-24` requires a clean installed no-subcommand journey
  through the real daemon, provider, broker, tool, persistence and two clients.
- `crates/cli/src/main.rs::run` computes `DefaultLaunch` but consumes only
  `plan.mode`; `plan.role` and `plan.view` are not wired to the caller.
- `crates/cli/src/tui_entry.rs::native_interactive_loop` constructs
  `NativeHost` with `skip_onboarding: true`.
- `crates/cli/src/tui_entry.rs::fetch_snapshot` returns a manual-session error
  for an empty daemon instead of atomically selecting/creating the first session.
- `worklog/APP-001.md:51-54` explicitly records integrator wiring and E2E PTY
  proof as remaining work even though the claim ledger marks APP-001 completed.
- `worklog/LANE-ONBOARD-SETUP.md:8-13` likewise records `main.rs` wiring as
  outside that lane. Its setup driver is pure/in-memory and is not a persistent
  secure-store caller.
- Packaged static OpenTUI and explicit-session PTY are already proven in
  `worklog/TUI-011.md`; they do not satisfy the bare fresh-HOME parent boundary.

## Target boundary and resource contract

- New trusted RED must use the real executable under a PTY, a disposable HOME
  and data directory, loopback-only fixtures, bounded captures/timeouts and
  deterministic child/daemon cleanup.
- No user's database, credentials or broad host state may be read or changed.
- Provider fixture is the only substitute; no mocked renderer, daemon, broker,
  tool executor or persistence success is acceptable.
- Product repair children may not be called complete until this parent test is
  frozen, runs GREEN against the installed artifact, and the exact integrated
  revision is rerun.

## Current blockers

- Trusted APP-012 RED is being authored independently before implementation.
- Existing frozen `installed_default_entrypoint.rs` remains immutable and has
  five literal `todo!()` bodies; it requires controller contract review and is
  not editable by this lane.
- APP-001 and APP-005 controller rows conflict with their own admitted unwired
  boundaries; product fixes require explicit repair-child leases rather than
  rewriting those accepted rows.
- Unsigned CI runners are authorized. Signing/notarization identities are not
  available and must remain an external release blocker, never fabricated.
