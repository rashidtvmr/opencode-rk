# WSX-001 worklog

## Claim
Workspace HTTP/WebSocket proxy bridge: caller-owned route + header strip + bounded FIFO queue. Slice GREEN 5/5 on fresh impl. lib.rs wiring left to integrator.

## Source evidence
- Repo commit `248f519d53ed29cbf7fef7ceb2b5010fb1a4224b`.
- tasks/WSX-001.md:1-91 contract (ProxyTarget, RouteHint, ProxyQueue, ProxyItem, ProxyItemKind, ProxyError, caps 4MiB/256/256KiB, strip set, failure states).
- crates/server/src/remote_sync.rs:1-204 WSX-002 pattern mirrored (bounded queue, variant-only errors, #[path] test include).
- crates/providers/src/proxy_route.rs:1-134 comparison point only (outbound network-proxy routing, no workspace bridge).
- worklog/UPSTREAM-V2-web-acp-integrations.md:19-26 upstream proxy.ts / workspace.ts summary (unverified, never equated).
- Absence verified: `ls crates/server/src/wsx*` no match pre-impl; `grep workspace_proxy lib.rs` empty.

## Observed scenario
- Pre-existing impl: none. tests/workspace_proxy.rs absent; src/workspace_proxy.rs absent.
- Wrote RED tests first (5 tests, 300 lines). RED run compiled test harness, failed only on missing `src/workspace_proxy.rs` include (no-such-file error), i.e. missing behavior. Frozen test sha256 `427c13d6...02dcc35`.
- Implemented src/workspace_proxy.rs (~230 lines) native std-only, forbid(unsafe_code).
- GREEN: 5 passed 0 failed. `cargo check -p opencode-rk-server` clean.

## Target boundary
- Owns: crates/server/src/workspace_proxy.rs (route, strip_headers, ProxyQueue, ProxyTarget, RouteHint, ProxyItem, ProxyItemKind, ProxyError, 6 public consts).
- Test: crates/server/tests/workspace_proxy.rs via `#[path]` include; lib.rs untouched (integrator wires).
- Excluded per card: socket I/O, TLS, reconnect/backoff (WSX-002), SSE (WEB-005), auth/CORS, ShareNext.
- Contract decisions: explicit valid remote_endpoint always wins (even on local-looking path); bridge path `/__workspace_ws` (+ / ? # suffixes) without endpoint => BadEndpoint; all other endpoint-less hints => Local (loopback default); header caps 256B name / 8KiB value fail whole call with BadHeader; exact caps accepted (2048-char endpoint, 256-char name, 8KiB value, 256KiB item).
- T02 local-status-without-directory verdict: Local (documented default, asserted exact).
- Memory: no unbounded queue, no threads, no I/O, no globals. Sync fns only.

## Tests
- RED: `cargo test -p opencode-rk-server --test workspace_proxy` => compile error missing module (fail for missing behavior, 0 passed).
- GREEN log: /tmp/opencode/wsx001-green.log => 5 passed 0 failed:
  - wsx001_t01 happy path (local/remote route, exact strip set + order, 3-item FIFO drain)
  - wsx001_t02 routing table + bad/oversize/empty endpoints, boundary 2048 ok, bridge-no-endpoint BadEndpoint, status-no-dir Local
  - wsx001_t03 queue caps atomic (256 items, 4MiB bytes, 256KiB+1 TooLarge, len/bytes unchanged)
  - wsx001_t04 strip safety (secret absent from output/debug/Display, over-cap BadHeader, input unchanged, edge caps ok)
  - wsx001_t05 isolation + determinism (disposable tempdir empty, banned-api scan clean, repeat runs identical)
- Check log: /tmp/opencode/wsx001-check.log, `cargo check -p opencode-rk-server` exit 0.

## Decisions
- Mirrored remote_sync.rs queue idiom (saturating_add, atomic reject, drain resets bytes) for cross-lane consistency.
- Scheme check case-insensitive; require non-empty authority after `://`.
- Error Display carries variant names only, no payload bytes.
- No lib.rs edit (ownership lock); noted for integrator.

## Remaining unknowns
- Upstream pin reconciliation (DISC-003) still open; no parity claimed.
- lib.rs wiring pending integrator (expected lane-gate note).
- Full `cargo check --workspace` not run here (scope + memory budget); ran `-p opencode-rk-server` check only.

## Hashes
- tests: `427c13d62fb05e4f6dc2817800ea7d4c195dadb4330f1205f8f5c7bb802dcc35`
- src: `66dc480459a88bb3123158770d70739243733f28eb0973b94fd3804086177964`

## Confirm verify (2026-09-16 senior QA)
- Paths actual: crates/server/src/workspace_proxy.rs (5975B) + crates/server/tests/workspace_proxy.rs (9675B). Lane card said tests/workspace_proxy.rs; real path crates/server/tests/. Exists y/y.
- sha256 src 66dc480459a88bb3123158770d70739243733f28eb0973b94fd3804086177964 match claimed 66dc4804... yes.
- sha256 tests 427c13d62fb05e4f6dc2817800ea7d4c195dadb4330f1205f8f5c7bb802dcc35 match claimed 427c13d6... yes.
- cargo test -p opencode-rk-server --test workspace_proxy: 5 passed 0 failed. Log /tmp/opencode/wsx001_confirm.log.
- cargo check -p opencode-rk-server: exit 0. Log /tmp/opencode/wsx001_confirm_check.log.
- Mem pre-run: 6.2Gi total, avail 2.8Gi. JOBS=2 THREADS=2 timeout 120 obeyed. No product edits.
