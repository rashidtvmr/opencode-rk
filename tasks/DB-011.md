# DB-011: Enhance gc_v2 tests with orphan collection and bounded work coverage

## Role
Senior Rust database engineer.

## Scope
File: `crates/storage/src/gc_v2.rs` (tests only). No production code changes.

## Goal
Add two tests covering additional GC lifecycle behaviors:
1. `orphan_blob_collected` -- unreferenced blob tombstoned then fully deleted.
2. `gc_bounded_work` -- single GC pass honors the LIMIT clamp (2 rows).

## Deliverable
`crates/storage/src/gc_v2.rs` with 10 tests total (8 existing + 2 added).

## Context
- `GcV2::claim_unreferenced_for_deletion` tombstones ready blobs (`state 0 -> 1`) older than cutoff, bounded by `limit` (clamped 1..=500).
- `GcV2::finish_deletion` deletes a tombstoned blob (`state 1`) after re-verifying it is still unreferenced.
- Existing helpers: `workspace`, `insert_blob`, `state_of`, `retention_counts`, `finish_deletion`, `claim_unreferenced_for_deletion`.

## Constraints
- No emojis, no em dashes.
- Run `cargo fmt` after editing.
- Use the shared test schema (`SchemaV2`) via `workspace()`.

## Verification
- `cargo test -p opencode-rk-storage --lib gc_v2` -- 10 passed.
- `cargo check --workspace` -- 0 errors.

## Status
Complete. Both tests pass. cargo fmt clean. cargo check clean (1 pre-existing dead-code warning in fork_v2.rs).
