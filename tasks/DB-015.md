# DB-015 - schema_v2 reopen validation tests

Status: TODO. Kind: verification. Runtime optional: False.
Mandatory for full declared release: no.
Requirements: none.

## User-observable outcome

Two new tests added to `crates/storage/src/schema_v2.rs` to verify reopen
marker validation and checksum tamper detection.

## Source evidence

- `crates/storage/src/schema_v2.rs` line 98: `open_existing` validates
  `application_id`, `user_version`, and `schema_migrations` checksum before
  handing out a connection.
- `crates/storage/src/schema_v2.rs` lines 13-16: `APP_ID`, `USER_VERSION`,
  `MIGRATION_VERSION` constants.
- `crates/storage/src/schema_v2.rs` line 58: `workspace_checksum` computes
  the blake3 digest of `WORKSPACE_SQL`.
- `crates/storage/src/schema_v2.rs` line 119: `open_existing` queries
  `schema_migrations` for version and checksum.

## Observable contract

- `reopen_validates_markers`: After initialize, reopen succeeds. After
  tampering with `application_id`, reopen fails. After tampering with
  `user_version`, reopen fails.
- `checksum_tamper_detected`: After flipping the stored checksum bytes in
  `schema_migrations`, reopen fails with a checksum mismatch error.

## Acceptance criteria

- 5 tests in schema_v2 pass (up from 3).
- `cargo check --workspace` clean.

## Test-first execution

1. GREEN: Run cargo test; both new tests pass.

## Evidence

- Status: COMPLETE.
- `cargo test -p opencode-rk-storage --lib schema_v2`: 5 passed.
- `cargo check --workspace`: 0 errors, 0 warnings in target.
