# DB-014: Add Tests to snapshot_v2.rs

## Summary
Enhanced test coverage for `crates/storage/src/snapshot_v2.rs` with 2 new tests.

## Changes

### Added Tests

1. **export_includes_all_tables**
   - Verifies snapshot export covers sessions, messages, message_parts, payloads, context_epochs, compaction_checkpoints, retained_payloads, and event_outbox
   - Tests cross-table relationships and data integrity across the snapshot system

2. **epoch_boundary_respected**
   - Verifies reads are bounded by context epoch order
   - Tests that `list_epochs` returns epochs newest-first
   - Validates epoch created_at_us and closed_at_us timestamps
   - Confirms context_epochs table accurately records epoch boundaries

## Test Results
```
cargo test -p opencode-rk-storage --lib snapshot_v2
test result: 13 passed, 0 failed
```

## Verification
- `cargo fmt` pass
- All 13 snapshot_v2 tests pass
- Original 11 tests remain unchanged

## Files Modified
- `crates/storage/src/snapshot_v2.rs` - Added 2 tests (555 - 726 lines)