# DB-016

Status: COMPLETED. Kind: product. Runtime optional: False.
Mandatory for full declared release: yes. Requirements: REQ-033.
Dependencies: none.

## User-observable outcome

Large data import safety - verify import_v2 handles oversized rows correctly.

## Source evidence

- crates/storage/src/import_v2.rs:15-16 fail-closed behavior on blob-backed/oversized rows
- crates/storage/src/lib.rs:43 MAX_EVENT_PAYLOAD_BYTES = 64KB

## Observable contract

### DB-016-T01: oversize inline payload > 64KB exceeds event ceiling and is rejected

- `db016_oversize_inline_skipped` test: inline payload at MAX_EVENT_PAYLOAD_BYTES + 1 (65537) is rejected with `StorageError::InlinePayloadTooLarge`
- No zero-byte placeholder is written; `verify_counts` confirms 0 messages imported

### DB-016-T02: import processes rows in bounded pages, not unbounded

- `db016_paged_import_bounded` test: import of 600 rows processes in bounded PAGE_BUDGET (500) pages
- First page returns exactly 500 rows
- Cursor advances correctly between pages
- Total verified via `verify_counts` reaches all 600 rows

## Implementation

### Deliverables

- `crates/storage/src/import_v2.rs` - Added tests `db016_oversize_inline_skipped` and `db016_paged_import_bounded`

## Verification

```bash
cargo fmt
cargo test -p opencode-rk-storage --lib import_v2
cargo check --workspace
```