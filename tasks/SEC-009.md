# SEC-009 integration credential lifecycle management

Scope: crates/security/src/credentials.rs only (plus pub mod line).

Impl: Credential {id, provider, token, expires_at} with is_expired/is_expired_at. CredentialStore backed by HashMap: add, get, remove, is_expired (missing id counts expired), prune_expired (removes t<=now, returns count), list_providers (sorted deduped). No secret logging (skipped zeroize, add when dep accepted).

Tests: add_and_get, expired_detected, prune_removes_expired, remove_works, list_providers.

Verify: cargo test -p opencode-rk-security && cargo check --workspace
