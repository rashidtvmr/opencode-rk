# CAT-001 - Catalog plugin/skills registry

Status: IMPLEMENTED. Kind: product. Runtime optional: False.
Mandatory for full declared release: yes.
Requirements: none.
Dependencies: none.
Ownership locks: crates/catalog/src/registry.rs, this card.
Suggested module: `crates/catalog/src/registry.rs`.

## User-observable outcome

In-memory registry mapping plugin/skill IDs to their metadata. Supports
registration, lookup by ID, listing by category, searching by name/tags,
removal, and count. Tests verify all five operations.

## Source evidence

- `crates/catalog/src/lib.rs` line 3 (`pub mod registry;`): module pre-wired.
- `crates/catalog/src/registry.rs` (stub): this lane replaces it.

## Observable contract

- `CatalogEntry`: fields id, name, version, path, category, tags Vec<String>.
  Clone, Debug, PartialEq, Eq derived.
- `CatalogRegistry`: HashMap<String, CatalogEntry> backing store.
- `new()`: empty registry.
- `register(entry)`: insert/replace by entry.id.
- `get(id) -> Option<&CatalogEntry>`: lookup by id.
- `list_by_category(cat) -> Vec<&CatalogEntry>`: exact category match.
- `search(query) -> Vec<&CatalogEntry>`: case-insensitive match on
  name and any tag.
- `unregister(id) -> bool`: true if removed.
- `count() -> usize`: number of entries.

## Acceptance criteria

- DB-001-T01 register_and_get green.
- DB-001-T02 list_by_category green.
- DB-001-T03 search_finds_matching green.
- DB-001-T04 unregister_removes green.
- DB-001-T05 count_correct green.
- `cargo check --workspace` clean.

## Test-first execution

1. RED: tests written inline; stub does not provide methods.
2. GREEN: full implementation; 5 tests pass.

## Evidence

- `cargo test -p opencode-rk-catalog`:
  `test result: ok. 5 passed; 0 failed; 0 ignored.`
- `cargo check --workspace`: Finished dev profile, EXIT 0.
