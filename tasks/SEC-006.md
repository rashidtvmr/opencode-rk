# SEC-006: System files readable but not writable

Status: done. `SystemFilePolicy` in `crates/security/src/sysfiles.rs`.
Untrusted reads limited to allowlist; trusted reads all of /etc/;
writes to system prefixes always denied.
