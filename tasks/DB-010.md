# DB-010: fork_v2.rs test enhancements

## Status
DONE

## Summary
Enhanced `crates/storage/src/fork_v2.rs` test suite from 5 to 7 tests:
- `fork_inherits_history` - forked session has parent's messages
- `fork_depth_bounded` - max fork depth enforced

## Implementation
Added `MAX_FORK_DEPTH = 8` constant and depth enforcement logic in `fork_session`:
- Walks parent chain counting existing forks
- Rejects fork if depth >= MAX_FORK_DEPTH
- Uses error reuse (`invalid_input()`) per ponytail convention

## Tests (7 passing)
1. `fork_copies_metadata_and_shares_payload_pk`
2. `fork_beyond_cap_errors`
3. `verify_copy_detects_tamper`
4. `parent_delete_keeps_fork_payloads`
5. `fork_provenance_cols_set`
6. `fork_inherits_history` (new)
7. `fork_depth_bounded` (new)

## Verification
- `cargo test -p opencode-rk-storage --lib fork_v2` - 7 passed
- `cargo check --workspace` - passed

## Files modified
- `crates/storage/src/fork_v2.rs`