# DB-012: Enhance import_v2 tests with resume and corruption detection

## Role
Senior Rust database engineer.

## Scope
File: `crates/storage/src/import_v2.rs` (tests only). No production code changes.

## Goal
Add two tests covering additional import lifecycle behaviors:
1. `import_resumes_after_interrupt` - partial import can be resumed via cursor
2. `import_rejects_corrupt_source` - corrupted/unknown role in source rejected

## Deliverable
`crates/storage/src/import_v2.rs` with 9 tests total (7 existing + 2 added).

## Context
- `ImportV2::import_session` copies all messages from format-1 to format-2
- `ImportV2::import_is_resumable` accepts `after_message_rowid` cursor for resume
- `encode_role` returns `None` for unknown roles, mapped to `StorageError::Sqlite(InvalidQuery)`
- Fail-closed importer: unknown roles are rejected before any write

## Constraints
- No emojis, no em dashes
- Run `cargo fmt` after editing
- Use existing test helpers: `source_db`, `dest_db`, `now_rfc3339`

## Verification
- `cargo test -p opencode-rk-storage --lib import_v2` -- 9 passed
- `cargo check --workspace` -- 0 errors

## Status
TODO