# SEC-005 filesystem sandbox policy

Scope: crates/security/src/sandbox.rs only. lib.rs already exports pub mod sandbox.

Impl: FileAction {Read,Write,Execute} local to module. SandboxDenied {path,action,reason}. SandboxPolicy {allowed_read,allowed_write,denied} with Default: cwd r/w, home read, /tmp read+write. SandboxCheck::is_allowed denies-first, prefix match after resolution. resolve_path: absolutize from cwd, canonicalize if exists, else canonicalize nearest existing ancestor then lexical join of remainder, symlink read_link walk for dangling links.

Tests: default_allows_cwd, denied_overrides_allowed, traversal_blocked, symlink_resolved, outside_sandbox_denied.

Verify: cargo test -p opencode-rk-security (39 passed) && cargo check -p opencode-rk-security (clean). cargo check --workspace: 6 pre-existing errors in opencode-rk-storage migrations.rs (E0061/E0596/E0599), unrelated to this lane.
