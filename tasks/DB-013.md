# DB-013 - retention_v2 test enhancement

Status: TODO. Kind: verification. Runtime optional: False.
Mandatory for full declared release: no.
Requirements: none.

## User-observable outcome

Two new tests added to `crates/storage/src/retention_v2.rs` to verify bounded sweep behavior and terminal approval cleanup.

## Source evidence

- `crates/storage/src/retention_v2.rs`: RetentionV2 struct with sweep methods.
- `crates/storage/src/schema/v2/workspace.sql` line 231: approvals.state enum (0-4: pending, allowed-once, denied, expired, consumed).
- `crates/storage/src/schema/v2/workspace.sql` line 236: approvals.resolved_at_us nullable.
- `crates/storage/src/schema/v2/workspace.sql` line 245: approval_resources ON DELETE CASCADE.

## Observable contract

- `sweep_bounded_by_limit`: A single sweep deletes at most `limit` rows, verified via multiple sequential calls with limit 2.
- `resolved_approval_cleanup`: Terminal approvals (states 1, 2, 4) with resolved_at_us <= cutoff are deleted and their resources cascade-deleted by foreign key.

## Acceptance criteria

- 8 tests in retention_v2 pass (up from 6).
- `cargo check --workspace` clean.

## Test-first execution

1. RED: N/A - enhancement to existing test suite only.
2. GREEN: Run cargo test; both new tests pass.

## Evidence

- Status: COMPLETE.
- `cargo test -p opencode-rk-storage --lib retention_v2`: 8 passed, 101 filtered out (was 6, now 8).
- `cargo check --workspace`: 0 errors, 1 pre-existing warning in fork_v2.rs (unrelated dead_code).