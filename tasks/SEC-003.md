# SEC-003 trusted user toggle

Scope: crates/security/src/trusted.rs only. Done.

Impl: Trust enum (Untrusted, SessionTrust, FullTrust), TrustLevel { level, granted_at, duration } with is_live(), TrustedUserToggle with Mutex<TrustLevel>. new() Untrusted. grant/revoke/current/is_expired. can_bypass_for_path: trusted AND !is_secret_path. can_bypass_for_command: trusted AND classify_destructive_argv is None.

Verify: cargo test -p opencode-rk-security: 29 passed (24 lib + 5 trusted). cargo check -p opencode-rk-security: clean. cargo check --workspace: fails in opencode-rk-storage (6 pre-existing errors, Migration clone/run_pending/conn borrow), unrelated to this lane.
