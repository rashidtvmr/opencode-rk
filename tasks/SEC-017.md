# SEC-017 - Star-proof validator: wildcard cannot bypass mandatory controls

Status: DONE. Kind: product. Lane-owned file: crates/security/src/star_proof.rs.

Scope: StarProofValidator over PermissionSet. Five mandatory controls star cannot override: SecretFileProtection, DestructiveCommandBlock, SystemFileWriteBlock, SsrfBlock, SqlInjectionBlock. validate returns Vec<Violation{control, description}>.

Evidence: lib.rs authorize enforces baseline mandatory first, rules apply only on Allow. Targeted Allow cannot lift mandatory.

Verify: cargo test -p opencode-rk-security && cargo check --workspace
