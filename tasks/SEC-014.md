# SEC-014 OS-level env var restrictions for child processes

Status: COMPLETE. Scope: crates/security/src/env_restrict.rs only (plus pub mod line in lib.rs).

Impl: EnvRestriction struct holds InheritPolicy (InheritAll, InheritNone, InheritAllowlist(Vec<String>)). apply(policy, current_env) -> filtered_env. Default blocklist: *_KEY, *_SECRET, *_TOKEN, *_PASSWORD, AWS_*, OPENAI_*, ANTHROPIC_*. No new deps, #![forbid(unsafe_code)].

Tests: inherit_none_empty, inherit_all_passes, allowlist_filters, default_blocks_secrets, custom_blocklist.

Verify: cargo test -p opencode-rk-security && cargo check --workspace