# REL-006

Status: NOT STARTED. Kind: product. Runtime optional: True.
Mandatory for full declared release: yes.
Requirements: REQ-039 (proposed, telemetry report renderer).
Dependencies: none.
Test obligations: REL-006-T01, REL-006-T02, REL-006-T03, REL-006-T04, REL-006-T05.

## User-observable outcome

Bounded text renderer for the `stats` command: totals, savings, recent
entries. Exact deterministic golden text format. Secrets never emitted
(absence asserted, not format-only). Default-on with opt-out; opt-out
prints a fixed message and no data. Pure function over a
caller-supplied snapshot; no store I/O, no env read.

## Source evidence

- PLAN.md sections 5-6: slice template + mandatory RED/GREEN lifecycle.
- docs/TDD.md sections 2-5: contract, RED, freeze, GREEN rules.
- docs/SECURITY.md section 2: no secret logging, no inherited env, fixtures only.
- tasks/TOOL-014.md: task-card model (bounded store, byte budget, eviction).
- tasks/OPS-010.md: doctor-report precedent (diagnostics without log spelunking).
- tasks/REL-005.md: sibling collector card (REQ-039 proposed, runtime-optional precedent).
- docs/proposals/HR-SLICE-02-report-spec.md: report spec, HR-RPT-T01..T05 asserts mirrored below.

## Observable contract

- `TelemetryEntry { label: String, saved_tokens: u64 }`: caller-supplied.
- `TelemetrySnapshot { total: u64, saved_tokens: u64, total_tokens: u64, recent: Vec<TelemetryEntry> }`.
- `ReportConfig { enabled: bool, max_entries: usize, max_bytes: usize }`:
  `Default` is `enabled: true, max_entries: 5, max_bytes: 8192`.
- `render(snapshot: &TelemetrySnapshot, cfg: &ReportConfig) -> String`.
- Golden format (exact, `\n` terminated, no trailing spaces):
```text
telemetry: {total} compressions, {saved} tokens saved ({pct}%)
recent:
- {label}: {saved} saved
```
- `pct = 100 * saved / max(total_tokens, 1)` (integer math, no float).
- Empty store (`total == 0`): output is exactly `no telemetry recorded yet\n`;
  no header, no recent block.
- Disabled (`enabled == false`): output is exactly
  `telemetry disabled (opt-out set)\n`; snapshot ignored, no totals,
  no entries, no secrets.
- Redaction first: every `label` scrubbed before format; secret bytes never
  reach output. Recent capped to `max_entries` oldest-dropped, then
  byte-capped with marker.

## Failure states

- Empty store: fixed message, no header, no recent block.
- Opt-out: fixed message, no totals, no entries, no secrets.
- Secret-like label (`sk-`, `ak-`, `bearer `, `api[_-]?key`, `-----BEGIN`):
  replaced with `[redacted]`; raw substring absent from output.
- Over `max_entries`: keep newest N, append `\n... [truncated {dropped} entries]`.
- Over `max_bytes`: cut at `char_boundary`, append
  `\n... [truncated {dropped} bytes]`; output `len <= max_bytes + MARKER_MAX`.
- Non-UTF8 impossible (String inputs); empty label renders as `[empty]`.

## Resource bounds

- Pure library: no I/O, no network, no clock, no env read inside `render`
  (caller maps env to `enabled`). Time O(recent), memory O(max_bytes).
- Deterministic: same snapshot + cfg => byte-identical output.
- No unbounded retained output: recent capped by `max_entries`, output
  capped by `max_bytes` plus a bounded marker.

## Suggested module boundary

- Proposed owner: `crates/headroom/src/report.rs` (new module `report`).
- Integrator wires `pub use` / CLI call site; worker does not edit shared
  `lib.rs`, `Cargo.toml`, schemas. Additive only.

## Frozen test obligations (exact asserts)

- REL-006-T01 golden identical (mirrors HR-RPT-T01): fixed 2-entry fixture
  (total=3, saved=300, total_tokens=1000):
  `assert_eq!(render(..), "telemetry: 3 compressions, 300 tokens saved (30%)\nrecent:\n- gzip: 200 saved\n- rtk: 100 saved\n")`.
- REL-006-T02 empty message (mirrors HR-RPT-T02): `total=0, recent=[]`:
  `assert_eq!(render(..), "no telemetry recorded yet\n")`.
- REL-006-T03 secret-absence (mirrors HR-RPT-T03): labels `sk-live-abc123`,
  `api_key=ZZZ`, `-----BEGIN PRIVATE KEY-----`: for each secret `s`:
  `assert!(!out.contains(s))`, `assert!(out.contains("[redacted]"))`.
- REL-006-T04 truncation marker (mirrors HR-RPT-T04): 10 entries,
  `max_entries=5`:
  `assert_eq!(out.lines().filter(|l| l.starts_with("- ")).count(), 5)`,
  `assert!(out.ends_with("... [truncated 5 entries]\n"))`.
- REL-006-T05 opt-out message (mirrors HR-RPT-T05): `enabled=false` with
  non-empty snapshot:
  `assert_eq!(render(..), "telemetry disabled (opt-out set)\n")`.

## TDD steps (RED-then-GREEN)

1. Inspect PLAN.md 5-6, docs/TDD.md 2-5, SECURITY.md 2, TOOL-014 card;
   cite commit + path:line.
2. Define contract/failures/bounds (above); open discovery proposal for gaps.
3. Author RED: 5 tests (REL-006-T01..T05) compile, fail on missing
   `headroom::report::render`. Freeze test hash + command manifest;
   controller verifies on disk.
4. Implement minimum native Rust; GREEN; refactor; rerun full suite.
5. Regressions: pct div-zero, char-boundary cut, `[empty]` label, determinism.
6. Submit evidence + patch, never acceptance.
7. Determinism: no clock/env/I/O in renderer
   (`grep -rn SystemTime` and `grep -rn std::env` over the module empty);
   suite passes with `--nocapture` twice, byte-identical output.

## Verification

- `cargo test -p headroom` passes REL-006-T01..T05 on frozen hash.
- `cargo check --workspace` clean; `cargo fmt --check` clean.
- Determinism: `cargo test -p headroom -- --nocapture` shows golden text byte-identical twice.
