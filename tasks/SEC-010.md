# SEC-010 - Pre-tool hook bus

Status: GREEN. Kind: product. Requirements: REQ-020.

## Outcome

Bounded pre/post tool hook bus, cap 100. `HookBus::new(cap)` clamps to MAX_HOOKS. `register_pre/post` fail AtCapacity when total hits cap. `run_pre_hooks` first Deny wins, else first Modify, else Allow; filter via crate glob_match (non-matching skipped). `run_post_hooks` calls all. `unregister` removes from both lists.

## Module

- Owned: `crates/security/src/hooks.rs` (`pub mod hooks;` pre-existing in lib.rs). Std only + serde/serde_json/thiserror.
- Types: `HookId(u64)`, `HookDecision` (Allow/Deny/Modify), `ToolInvocation`, `ToolResult`, `PreHook` (id/filter/handler), `PostHook` (id/handler), `HookError::AtCapacity`, `HookBus`, `MAX_HOOKS=100`.
- Extra dep: added `serde_json.workspace = true` to crates/security/Cargo.toml (spec requires serde_json::Value; dep pre-existing in workspace).
- Style: compressed single-line, no fmt, no emojis, no em dashes.

## Tests (in-module, 5)

- pre_hook_can_block, post_hook_observes (Arc<AtomicBool>), bounded_to_capacity (100 ok, 101st pre+post fail), first_deny_wins, unregister_removes.

## Commands

- `cargo test -p opencode-rk-security`: 44 passed.
- `cargo check --workspace`: FAILS on pre-existing other-lane errors in crates/storage/src/migrations.rs (untouched by this lane); `cargo check -p opencode-rk-security`: clean.
