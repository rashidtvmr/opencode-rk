# SEC-007 cross-project file access control

Scope: crates/security/src/project_boundary.rs only. lib.rs already exports pub mod project_boundary.

Impl: ProjectBoundary {root, approved_external} with new(root) lexical-normalizing root. is_within_project normalizes abs or root-joined rel, starts_with root. check_access Ok if within root or under approved_external, else Err(NeedsApproval {path, reason}). approve_external pushes normalized if absent. revoke_external retains, returns removed bool. NeedsApproval Display + Error. Traversal handled by lexical normalize pop on ParentDir.

Tests: within_project_allowed, outside_project_needs_approval, approved_external_works, revoke_removes_access, parent_traversal_detected.

Verify: cargo test -p opencode-rk-security project_boundary (5 passed). Full suite: 70 passed, 1 pre-existing fail ssrf::tests::blocks_localhost (check_url localhost, unrelated, fails on stashed stub too). cargo check --workspace clean.
