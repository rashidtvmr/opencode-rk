# TASK: SEC-016 - Extended destructive command patterns

**Status:** implemented
**Owner:** subagent (SEC-016 lane)
**Crate:** opencode-rk-security
**File:** crates/security/src/cmd_patterns.rs (+ `pub mod cmd_patterns;` in lib.rs)
**Verification:** `cargo test -p opencode-rk-security && cargo check --workspace`

## Implementation

- `CommandPattern { program_glob, arg_all, arg_any, reason }`, `new` / `program_only` / `matches(program, args)`. Program matches lowercased basename via crate `glob_match`; arg needles all-lowercase; alphanumeric needles match whole tokens, others substring.
- `CommandPatternMatcher { patterns }`: `new`, `with_defaults`, `with_pattern`, `add`, `patterns`, `is_destructive(program, args) -> Option<String>` (first match wins).
- `default_patterns()`: curl|wget piped to shell (`curl|wget` + `|` + sh/bash/zsh/dash/fish), `*sh -c` + eval/exec, `dd` + `if=/dev/zero`, shred/wipefs/lvremove/vgremove program-only, `zpool` + destroy, `btrfs` + subvolume+delete.

## Tests (5)

- curl_pipe_sh_detected, dd_zero_detected, shred_detected, safe_curl_allowed, custom_pattern_works.

## Notes

- Std + serde only. No new deps. Style: compressed, no fmt.
