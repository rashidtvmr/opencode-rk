# DB-002 - V2 writer module

Status: COMPLETE. Kind: product.
Ownership locks: crates/storage/src/writer_v2.rs, this card.
Suggested module: `crates/storage/src/writer_v2.rs`.

## Outcome

Bounded writers for format-2 workspace: create_session, append_message
(inline payload, one part), append_outbox_event, list_recent_sessions,
quota-gated checked variants. 5 tests green.

## Source evidence

- `crates/storage/src/writer_v2.rs` (V2Writer, insert_message,
  insert_outbox_event, tests): this lane.
- `crates/storage/src/schema_v2.rs` (SchemaV2::initialize_workspace): init protocol.
- `crates/storage/schema/v2/workspace.sql` (sessions/messages/message_parts/payloads DDL).

## Observable contract

- create_session rejects title over 1024 bytes, inserts row.
- append_message rejects missing session (SessionNotFound), oversize/blob
  payloads (InlinePayloadTooLarge); assigns seq from sessions.next_message_seq,
  writes message + payload + single message_part atomically.
- Duplicate message id rejected (UNIQUE on messages.id); message count unchanged.
- list_recent_sessions pages by (updated_at_us, pk), clamped limit 1..500.
- Quota-gated variants admit under budget else Io error, nothing written.

## Acceptance criteria

- DB-002-T01 create_session_generates_id green.
- DB-002-T02 append_message_with_parts green.
- DB-002-T03 duplicate_message_rejected green.
- DB-002-T04 checked_append_generous_budget green.
- DB-002-T05 checked_append_zero_budget_rejects green.
- `cargo check --workspace` clean.

## Test-first execution

1. RED: n/a, module shipped with 2 quota tests.
2. GREEN: added 3 tests reusing file-backed workspace helper
   (SchemaV2::initialize_workspace takes Path; no memory init exists
   on SchemaV2; facade init_memory is private). 5 pass.

## Evidence

- `cargo test -p opencode-rk-storage --lib writer_v2`:
  5 passed, 0 failed.
- `cargo check --workspace`: clean.
