# DB-007: Admission V2 Edge Cases

## Status
DONE

## Scope
`crates/storage/src/admission_v2.rs` test-only enhancement (8 to 10 tests).

## Added tests
1. `expired_receipt_pruned` - receipt expired at lookup returns None, then
   `RetentionV2::sweep_expired_receipts` deletes it (count goes to 0).
2. `idempotent_admission` - repeated `receipt_lookup` on same op returns same
   result; duplicate `receipt_store` rejected (PK conflict), original preserved.

## Verification
- `cargo test -p opencode-rk-storage --lib admission_v2` - 10 passed
- `cargo check --workspace` - OK
